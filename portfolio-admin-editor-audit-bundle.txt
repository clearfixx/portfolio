PORTFOLIO ADMIN EDITOR AUDIT BUNDLE
ROOT: /Users/andrijkulagin/Developer/Portfolio/portfolio
FILES: 31

====================================================================================================
FILE: src/app/(payload)/custom.scss
====================================================================================================
/* Portfolio Admin Experience — foundation */

:root {
  --portfolio-admin-bg: #081018;
  --portfolio-admin-bg-soft: #0b141d;
  --portfolio-admin-surface: #0f1923;
  --portfolio-admin-surface-raised: #121f2b;
  --portfolio-admin-surface-hover: #172633;
  --portfolio-admin-border: #20303d;
  --portfolio-admin-border-strong: #2a3d4c;
  --portfolio-admin-text: #f2f6f8;
  --portfolio-admin-text-soft: #a8b5bf;
  --portfolio-admin-text-muted: #73828e;
  --portfolio-admin-cyan: #32d7e7;
  --portfolio-admin-cyan-soft: rgba(50, 215, 231, 0.14);
  --portfolio-admin-purple: #a867ff;
  --portfolio-admin-purple-soft: rgba(168, 103, 255, 0.14);
  --portfolio-admin-green: #50dc89;
  --portfolio-admin-green-soft: rgba(80, 220, 137, 0.13);
  --portfolio-admin-amber: #f1b93f;
  --portfolio-admin-amber-soft: rgba(241, 185, 63, 0.13);
  --portfolio-admin-red: #ff655f;
  --portfolio-admin-red-soft: rgba(255, 101, 95, 0.13);
  --portfolio-admin-radius-sm: 8px;
  --portfolio-admin-radius-md: 12px;
  --portfolio-admin-radius-lg: 16px;
  --portfolio-admin-shadow: 0 18px 50px rgba(0, 0, 0, 0.2);
}

html[data-theme='dark'] {
  --theme-bg: var(--portfolio-admin-bg);
  --theme-text: var(--portfolio-admin-text);
  --theme-input-bg: var(--portfolio-admin-surface);
  --theme-elevation-0: #081018;
  --theme-elevation-50: #0b141d;
  --theme-elevation-100: #0f1923;
  --theme-elevation-150: #121f2b;
  --theme-elevation-200: #172633;
  --theme-elevation-250: #1b2b38;
  --theme-elevation-300: #20303d;
  --theme-elevation-350: #2a3d4c;
  --theme-elevation-400: #3b4d5a;
  --theme-elevation-500: #61717c;
  --theme-elevation-600: #87959e;
  --theme-elevation-700: #a8b5bf;
  --theme-elevation-800: #d4dce1;
  --theme-elevation-900: #f2f6f8;
  --theme-elevation-1000: #ffffff;
  --theme-success-500: var(--portfolio-admin-green);
  --theme-warning-500: var(--portfolio-admin-amber);
  --theme-error-500: var(--portfolio-admin-red);
}

html,
body {
  background: var(--portfolio-admin-bg);
}

body {
  color: var(--portfolio-admin-text);
}

::selection {
  background: var(--portfolio-admin-cyan-soft);
  color: var(--portfolio-admin-text);
}

*:focus-visible {
  outline: 2px solid var(--portfolio-admin-cyan) !important;
  outline-offset: 2px;
}

/* Payload shell */

.template-default,
.template-default__wrap,
.payload-admin {
  background:
    radial-gradient(circle at 70% -20%, rgba(50, 215, 231, 0.045), transparent 34rem),
    var(--portfolio-admin-bg);
}

.template-default__nav,
.nav {
  background: #071019;
  border-right: 1px solid var(--portfolio-admin-border);
}

.app-header {
  background: rgba(8, 16, 24, 0.9);
  border-bottom: 1px solid var(--portfolio-admin-border);
  backdrop-filter: blur(18px);
}

.btn,
button,
input,
textarea,
select,
.rs__control,
.card,
.table {
  border-radius: var(--portfolio-admin-radius-sm);
}

.btn--style-primary,
button[type='submit'] {
  background: var(--portfolio-admin-cyan);
  border-color: var(--portfolio-admin-cyan);
  color: #031014;
  font-weight: 700;
}

.btn--style-primary:hover,
button[type='submit']:hover {
  filter: brightness(1.05);
}

input,
textarea,
select,
.rs__control {
  border-color: var(--portfolio-admin-border) !important;
  background: var(--portfolio-admin-surface) !important;
}

/* Brand graphics */

.portfolio-admin-icon {
  display: block;
  width: 42px;
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 14px;
  color: var(--portfolio-admin-text);
}

.portfolio-admin-logo__mark {
  width: 58px;
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-logo__copy {
  display: grid;
  gap: 2px;
  text-align: left;
}

.portfolio-admin-logo__copy strong {
  font-size: 18px;
  letter-spacing: 0.13em;
  text-transform: uppercase;
}

.portfolio-admin-logo__copy small {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  letter-spacing: 0.18em;
  text-transform: uppercase;
}

/* Login */

.portfolio-admin-login-intro {
  margin: 0 0 28px;
  text-align: center;
}

.portfolio-admin-login-intro__eyebrow,
.portfolio-admin-dashboard__eyebrow {
  display: inline-flex;
  color: var(--portfolio-admin-cyan);
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 11px;
  letter-spacing: 0.13em;
  text-transform: uppercase;
}

.portfolio-admin-login-intro h1 {
  margin: 10px 0 8px;
  font-size: 28px;
  letter-spacing: -0.03em;
}

.portfolio-admin-login-intro p {
  max-width: 420px;
  margin: 0 auto;
  color: var(--portfolio-admin-text-soft);
  line-height: 1.6;
}

.login__form,
.auth-fields {
  border: 1px solid var(--portfolio-admin-border);
  border-radius: var(--portfolio-admin-radius-lg);
  background: rgba(15, 25, 35, 0.82);
  box-shadow: var(--portfolio-admin-shadow);
}

/* Navigation */

.portfolio-admin-nav {
  display: flex;
  width: 100%;
  min-height: 100%;
  flex-direction: column;
  color: var(--portfolio-admin-text-soft);
}

.portfolio-admin-nav__brand {
  padding: 22px 18px 16px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-nav__brand > a {
  display: flex;
  align-items: center;
  gap: 12px;
  color: var(--portfolio-admin-text);
  text-decoration: none;
}

.portfolio-admin-nav__brand-mark {
  display: grid;
  width: 46px;
  height: 42px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 10px;
  color: var(--portfolio-admin-cyan);
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 12px;
  letter-spacing: -0.08em;
}

.portfolio-admin-nav__brand strong,
.portfolio-admin-nav__brand small {
  display: block;
}

.portfolio-admin-nav__brand strong {
  color: var(--portfolio-admin-text);
  font-size: 14px;
  letter-spacing: 0.13em;
  text-transform: uppercase;
}

.portfolio-admin-nav__brand small {
  margin-top: 3px;
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  letter-spacing: 0.17em;
  text-transform: uppercase;
}

.portfolio-admin-nav__environment {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 16px;
  padding: 6px 9px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  color: var(--portfolio-admin-text-soft);
  font-size: 10px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.portfolio-admin-nav__environment i {
  width: 6px;
  height: 6px;
  border-radius: 999px;
  background: var(--portfolio-admin-green);
  box-shadow: 0 0 10px rgba(80, 220, 137, 0.6);
}

.portfolio-admin-nav__scroll {
  overflow-y: auto;
  flex: 1;
  padding: 14px 10px 24px;
  scrollbar-width: thin;
  scrollbar-color: var(--portfolio-admin-border) transparent;
}

.portfolio-admin-nav__group {
  padding: 11px 0;
  border-bottom: 1px solid rgba(32, 48, 61, 0.75);
}

.portfolio-admin-nav__group:last-child {
  border-bottom: 0;
}

.portfolio-admin-nav__group h2 {
  margin: 0 10px 7px;
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  font-weight: 600;
  letter-spacing: 0.11em;
  text-transform: uppercase;
}

.portfolio-admin-nav__group ul {
  display: grid;
  gap: 2px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.portfolio-admin-nav__group a {
  position: relative;
  display: flex;
  min-height: 38px;
  align-items: center;
  gap: 11px;
  padding: 8px 10px;
  border-radius: 8px;
  color: var(--portfolio-admin-text-soft);
  font-size: 13px;
  text-decoration: none;
  transition: 140ms ease;
}

.portfolio-admin-nav__group a:hover {
  background: var(--portfolio-admin-surface);
  color: var(--portfolio-admin-text);
}

.portfolio-admin-nav__group a.is-active {
  background: linear-gradient(90deg, var(--portfolio-admin-cyan-soft), rgba(50, 215, 231, 0.035));
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-nav__group a.is-active::before {
  position: absolute;
  top: 7px;
  bottom: 7px;
  left: -10px;
  width: 2px;
  border-radius: 999px;
  background: var(--portfolio-admin-cyan);
  content: '';
}

.portfolio-admin-nav__group svg {
  flex: 0 0 auto;
  opacity: 0.86;
}

.portfolio-admin-nav__account {
  display: grid;
  grid-template-columns: 36px minmax(0, 1fr) auto;
  align-items: center;
  gap: 10px;
  padding: 14px;
  border-top: 1px solid var(--portfolio-admin-border);
  background: rgba(8, 16, 24, 0.92);
}

.portfolio-admin-nav__avatar {
  display: grid;
  width: 36px;
  height: 36px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 999px;
  background: var(--portfolio-admin-surface-raised);
  color: var(--portfolio-admin-text);
  font-weight: 700;
}

.portfolio-admin-nav__account-copy {
  min-width: 0;
}

.portfolio-admin-nav__account-copy strong,
.portfolio-admin-nav__account-copy small {
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-nav__account-copy strong {
  color: var(--portfolio-admin-text);
  font-size: 12px;
}

.portfolio-admin-nav__account-copy small {
  margin-top: 2px;
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
}

.portfolio-admin-nav__account button {
  padding: 6px 7px;
  border: 0;
  background: transparent;
  color: var(--portfolio-admin-text-muted);
  cursor: pointer;
  font-size: 10px;
}

.portfolio-admin-nav__account button:hover {
  color: var(--portfolio-admin-red);
}

/* Dashboard */

.portfolio-admin-dashboard {
  width: min(100%, 1500px);
  margin: 0 auto;
  padding: clamp(24px, 3vw, 42px);
  color: var(--portfolio-admin-text);
}

.portfolio-admin-dashboard__hero {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 24px;
}

.portfolio-admin-dashboard__hero h1 {
  margin: 8px 0 6px;
  font-size: clamp(28px, 3vw, 40px);
  letter-spacing: -0.045em;
  line-height: 1.05;
}

.portfolio-admin-dashboard__hero p {
  margin: 0;
  color: var(--portfolio-admin-text-soft);
  font-size: 14px;
}

.portfolio-admin-dashboard__actions {
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-end;
  gap: 10px;
}

.portfolio-admin-button {
  display: inline-flex;
  min-height: 40px;
  align-items: center;
  justify-content: center;
  padding: 9px 16px;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: var(--portfolio-admin-radius-sm);
  background: var(--portfolio-admin-surface);
  color: var(--portfolio-admin-text);
  font-size: 12px;
  font-weight: 600;
  text-decoration: none;
  transition: 140ms ease;
}

.portfolio-admin-button:hover {
  border-color: #3c5364;
  background: var(--portfolio-admin-surface-hover);
}

.portfolio-admin-button.is-primary {
  border-color: var(--portfolio-admin-cyan);
  background: var(--portfolio-admin-cyan);
  color: #041116;
}

.portfolio-admin-dashboard__metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
  margin-bottom: 14px;
}

.portfolio-admin-metric {
  position: relative;
  display: grid;
  min-height: 128px;
  align-content: start;
  overflow: hidden;
  padding: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: var(--portfolio-admin-radius-md);
  background: linear-gradient(145deg, rgba(18, 31, 43, 0.96), rgba(11, 20, 29, 0.96));
  color: var(--portfolio-admin-text);
  text-decoration: none;
  transition: 150ms ease;
}

.portfolio-admin-metric:hover {
  border-color: var(--portfolio-admin-border-strong);
  transform: translateY(-1px);
}

.portfolio-admin-metric > span {
  color: var(--portfolio-admin-text-soft);
  font-size: 10px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.portfolio-admin-metric strong {
  margin-top: 11px;
  font-size: 30px;
  letter-spacing: -0.04em;
}

.portfolio-admin-metric small {
  margin-top: 7px;
  color: var(--portfolio-admin-text-muted);
  font-size: 11px;
}

.portfolio-admin-metric i {
  position: absolute;
  right: -22px;
  bottom: -34px;
  width: 130px;
  height: 82px;
  border: 1px solid currentColor;
  border-radius: 50%;
  opacity: 0.12;
}

.portfolio-admin-metric.is-cyan {
  color: var(--portfolio-admin-cyan);
}
.portfolio-admin-metric.is-purple {
  color: var(--portfolio-admin-purple);
}
.portfolio-admin-metric.is-green {
  color: var(--portfolio-admin-green);
}
.portfolio-admin-metric.is-amber {
  color: var(--portfolio-admin-amber);
}
.portfolio-admin-metric strong,
.portfolio-admin-metric small,
.portfolio-admin-metric > span {
  color: inherit;
}
.portfolio-admin-metric small,
.portfolio-admin-metric > span {
  opacity: 0.72;
}

.portfolio-admin-dashboard__grid {
  display: grid;
  grid-template-columns: minmax(0, 1.55fr) minmax(260px, 0.8fr) minmax(300px, 0.95fr);
  gap: 14px;
}

.portfolio-admin-panel {
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: var(--portfolio-admin-radius-md);
  background: rgba(15, 25, 35, 0.9);
  box-shadow: 0 8px 28px rgba(0, 0, 0, 0.08);
}

.portfolio-admin-panel > header {
  padding: 15px 17px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-panel h2 {
  margin: 0;
  color: var(--portfolio-admin-text-soft);
  font-size: 11px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.portfolio-admin-panel--attention,
.portfolio-admin-panel--activity {
  grid-column: span 1;
}

.portfolio-admin-panel__footer-link {
  display: block;
  padding: 13px 17px;
  border-top: 1px solid var(--portfolio-admin-border);
  color: #9fd9ff;
  font-size: 11px;
  text-decoration: none;
}

.portfolio-admin-panel__note {
  margin: 0;
  padding: 13px 17px;
  border-top: 1px solid var(--portfolio-admin-border);
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  line-height: 1.5;
}

.portfolio-admin-attention-list,
.portfolio-admin-activity-list,
.portfolio-admin-inbox-list,
.portfolio-admin-integration-list {
  margin: 0;
  padding: 0;
  list-style: none;
}

.portfolio-admin-attention-list li {
  display: grid;
  grid-template-columns: 10px minmax(0, 1fr) auto;
  align-items: center;
  gap: 11px;
  min-height: 46px;
  padding: 10px 17px;
  border-bottom: 1px solid rgba(32, 48, 61, 0.72);
  font-size: 11px;
}

.portfolio-admin-attention-list li:last-child {
  border-bottom: 0;
}
.portfolio-admin-attention-list i {
  width: 7px;
  height: 7px;
  border-radius: 999px;
  background: var(--portfolio-admin-amber);
}
.portfolio-admin-attention-list .is-danger i {
  background: var(--portfolio-admin-red);
}
.portfolio-admin-attention-list .is-message i {
  background: var(--portfolio-admin-purple);
}
.portfolio-admin-attention-list .is-info i {
  background: var(--portfolio-admin-cyan);
}
.portfolio-admin-attention-list a {
  color: var(--portfolio-admin-text);
  text-decoration: none;
}
.portfolio-admin-attention-list span {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
}

.portfolio-admin-status-list {
  margin: 0;
}

.portfolio-admin-status-list > div {
  display: flex;
  min-height: 44px;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: 10px 17px;
  border-bottom: 1px solid rgba(32, 48, 61, 0.72);
}

.portfolio-admin-status-list dt {
  color: var(--portfolio-admin-text-soft);
  font-size: 10px;
}
.portfolio-admin-status-list dd {
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: 10px;
  text-align: right;
}
.portfolio-admin-status-list dd.is-success {
  color: var(--portfolio-admin-green);
}
.portfolio-admin-status-list dd.is-warning {
  color: var(--portfolio-admin-amber);
}

.portfolio-admin-integration-list li {
  display: grid;
  grid-template-columns: 34px minmax(0, 1fr) auto;
  align-items: center;
  gap: 10px;
  padding: 12px 17px;
  border-bottom: 1px solid rgba(32, 48, 61, 0.72);
}

.portfolio-admin-provider-icon {
  display: grid;
  width: 32px;
  height: 32px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 9px;
  background: var(--portfolio-admin-surface-raised);
  color: var(--portfolio-admin-text);
  font-size: 10px;
  font-weight: 700;
}

.portfolio-admin-integration-list strong,
.portfolio-admin-integration-list small {
  display: block;
}
.portfolio-admin-integration-list strong {
  font-size: 11px;
}
.portfolio-admin-integration-list small {
  margin-top: 2px;
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
}
.portfolio-admin-integration-list .is-success {
  color: var(--portfolio-admin-green);
  font-size: 9px;
}

.portfolio-admin-activity-list li,
.portfolio-admin-inbox-list li {
  display: grid;
  grid-template-columns: 28px minmax(0, 1fr) auto;
  align-items: center;
  gap: 10px;
  min-height: 53px;
  padding: 10px 17px;
  border-bottom: 1px solid rgba(32, 48, 61, 0.72);
}

.portfolio-admin-activity-list li > i {
  display: block;
  width: 28px;
  height: 28px;
  border-radius: 8px;
  background: var(--portfolio-admin-cyan-soft);
}
.portfolio-admin-activity-list li > i.is-green {
  background: var(--portfolio-admin-green-soft);
}
.portfolio-admin-activity-list li > i.is-purple {
  background: var(--portfolio-admin-purple-soft);
}
.portfolio-admin-activity-list a,
.portfolio-admin-inbox-list a {
  color: var(--portfolio-admin-text);
  font-size: 11px;
  text-decoration: none;
}
.portfolio-admin-activity-list small,
.portfolio-admin-inbox-list small {
  display: block;
  margin-top: 3px;
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
}
.portfolio-admin-activity-list time,
.portfolio-admin-inbox-list time {
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
}

.portfolio-admin-inbox-list__avatar {
  display: grid;
  width: 28px;
  height: 28px;
  place-items: center;
  border-radius: 999px;
  background: var(--portfolio-admin-purple-soft);
  color: var(--portfolio-admin-purple);
  font-size: 10px;
  font-weight: 700;
}

.portfolio-admin-quick-actions {
  display: grid;
}

.portfolio-admin-quick-actions a {
  position: relative;
  display: block;
  padding: 13px 34px 13px 17px;
  border-bottom: 1px solid rgba(32, 48, 61, 0.72);
  color: var(--portfolio-admin-text);
  text-decoration: none;
}

.portfolio-admin-quick-actions a::after {
  position: absolute;
  top: 50%;
  right: 17px;
  color: var(--portfolio-admin-text-muted);
  content: '›';
  transform: translateY(-50%);
}

.portfolio-admin-quick-actions a:last-child {
  border-bottom: 0;
}
.portfolio-admin-quick-actions strong,
.portfolio-admin-quick-actions small {
  display: block;
}
.portfolio-admin-quick-actions strong {
  font-size: 11px;
}
.portfolio-admin-quick-actions small {
  margin-top: 3px;
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
}

.portfolio-admin-empty-state {
  display: grid;
  gap: 5px;
  padding: 26px 17px;
  color: var(--portfolio-admin-text-soft);
}
.portfolio-admin-empty-state strong {
  color: var(--portfolio-admin-green);
  font-size: 12px;
}
.portfolio-admin-empty-state span {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
}

@media (max-width: 1180px) {
  .portfolio-admin-dashboard__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  .portfolio-admin-dashboard__grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  .portfolio-admin-panel--attention,
  .portfolio-admin-panel--activity {
    grid-column: span 2;
  }
}

@media (max-width: 760px) {
  .portfolio-admin-dashboard {
    padding: 20px 14px 80px;
  }
  .portfolio-admin-dashboard__hero {
    align-items: flex-start;
    flex-direction: column;
  }
  .portfolio-admin-dashboard__actions {
    justify-content: flex-start;
  }
  .portfolio-admin-dashboard__metrics,
  .portfolio-admin-dashboard__grid {
    grid-template-columns: 1fr;
  }
  .portfolio-admin-panel--attention,
  .portfolio-admin-panel--activity {
    grid-column: span 1;
  }
  .portfolio-admin-metric {
    min-height: 112px;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    transition-duration: 0.01ms !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
  }
}

/* Portfolio Admin Experience — login refinement */

.portfolio-admin-brand-mark {
  display: block;
  flex: none;
  line-height: 0;
}

.portfolio-admin-brand-mark > a,
.portfolio-admin-brand-mark .logo,
.portfolio-admin-brand-mark .logo__mark {
  display: block;
  width: 100%;
  height: 100%;
}

.portfolio-admin-brand-mark .logo__content {
  display: none !important;
}

.portfolio-admin-brand-mark svg {
  display: block;
  width: 100%;
  height: 100%;
}

.portfolio-admin-icon {
  width: 42px;
  height: 32px;
}

.portfolio-admin-logo__mark {
  width: 68px;
  height: 48px;
}

.portfolio-admin-nav__brand-identity {
  display: flex;
  align-items: center;
  gap: 12px;
}

.portfolio-admin-nav__brand-mark {
  display: block;
  width: 54px;
  height: 38px;
  border: 0;
  border-radius: 0;
  color: inherit;
}

.portfolio-admin-nav__brand-copy {
  color: var(--portfolio-admin-text);
  text-decoration: none;
}

.portfolio-admin-visually-hidden {
  position: absolute !important;
  width: 1px !important;
  height: 1px !important;
  padding: 0 !important;
  margin: -1px !important;
  overflow: hidden !important;
  clip: rect(0, 0, 0, 0) !important;
  white-space: nowrap !important;
  border: 0 !important;
}

.login__form,
.auth-fields {
  border: 0 !important;
  border-radius: 0 !important;
  background: transparent !important;
  box-shadow: none !important;
}

.login__form {
  width: min(100%, 500px);
}

.auth-fields {
  display: grid;
  gap: 18px;
  padding: 0 !important;
}

.auth-fields .field-type,
.login__form .field-type {
  margin: 0;
}

.portfolio-admin-auth-input {
  position: relative;
  display: flex;
  min-height: 54px;
  align-items: center;
  border: 1px solid rgba(115, 130, 142, 0.3);
  border-radius: 12px;
  background: rgba(15, 25, 35, 0.72);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.025);
  transition:
    border-color 150ms ease,
    background-color 150ms ease,
    box-shadow 150ms ease;
}

.portfolio-admin-auth-input:hover {
  border-color: rgba(168, 181, 191, 0.42);
  background: rgba(18, 31, 43, 0.86);
}

.portfolio-admin-auth-input:focus-within {
  border-color: rgba(50, 215, 231, 0.78);
  background: rgba(18, 31, 43, 0.96);
  box-shadow:
    0 0 0 3px rgba(50, 215, 231, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.035);
}

.portfolio-admin-auth-input__icon {
  position: absolute;
  left: 17px;
  z-index: 1;
  display: grid;
  width: 19px;
  height: 19px;
  place-items: center;
  color: var(--portfolio-admin-text-muted);
  pointer-events: none;
  transition: color 150ms ease;
}

.portfolio-admin-auth-input:focus-within .portfolio-admin-auth-input__icon {
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-auth-input__icon svg,
.portfolio-admin-auth-input__toggle svg {
  display: block;
  width: 100%;
  height: 100%;
}

.portfolio-admin-auth-input .portfolio-admin-auth-input__control,
.portfolio-admin-auth-input input {
  width: 100%;
  min-height: 52px;
  padding: 0 48px !important;
  border: 0 !important;
  border-radius: 11px !important;
  outline: 0 !important;
  background: transparent !important;
  box-shadow: none !important;
  color: var(--portfolio-admin-text) !important;
  font-size: 15px;
}

.portfolio-admin-auth-input input::placeholder {
  color: #6f7e89;
  opacity: 1;
}

.portfolio-admin-auth-input input:-webkit-autofill,
.portfolio-admin-auth-input input:-webkit-autofill:hover,
.portfolio-admin-auth-input input:-webkit-autofill:focus {
  -webkit-text-fill-color: var(--portfolio-admin-text);
  box-shadow: 0 0 0 1000px var(--portfolio-admin-surface) inset !important;
  transition: background-color 9999s ease-out 0s;
}

.portfolio-admin-auth-input__toggle {
  position: absolute;
  right: 9px;
  z-index: 2;
  display: grid;
  width: 36px;
  height: 36px;
  padding: 9px;
  place-items: center;
  border: 0;
  border-radius: 9px;
  background: transparent;
  color: var(--portfolio-admin-text-muted);
  cursor: pointer;
  transition:
    color 150ms ease,
    background-color 150ms ease;
}

.portfolio-admin-auth-input__toggle:hover {
  background: rgba(50, 215, 231, 0.09);
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-auth-input__toggle:focus-visible {
  outline: 2px solid var(--portfolio-admin-cyan) !important;
  outline-offset: 1px;
}

.portfolio-admin-auth-input input::-ms-reveal,
.portfolio-admin-auth-input input::-ms-clear {
  display: none;
}

.login__form .forgot-password,
.login__form a[href*='forgot'] {
  display: inline-flex;
  margin-top: -4px;
  color: var(--portfolio-admin-text-soft);
  font-size: 13px;
  text-underline-offset: 3px;
}

.login__form button[type='submit'] {
  min-height: 52px;
  margin-top: 4px;
  border-radius: 11px;
}

/* Portfolio Admin Experience — shell polish:start */

/* Keep the custom sidebar inside the viewport instead of extending page height. */
.template-default__nav {
  position: sticky !important;
  top: 0 !important;
  align-self: start;
  height: 100vh !important;
  min-height: 0 !important;
  max-height: 100vh !important;
  overflow: hidden !important;
}

.template-default__nav > .nav,
.template-default__nav .nav {
  height: 100%;
  min-height: 0;
  overflow: hidden;
}

.portfolio-admin-nav {
  height: 100%;
  min-height: 0;
}

.portfolio-admin-nav__scroll {
  min-height: 0;
  overscroll-behavior: contain;
}

/* The dashboard already has its own brand in the sidebar. */
.app-header__logo,
.app-header .graphic-icon,
.app-header .portfolio-admin-icon {
  display: none !important;
}

.portfolio-admin-nav__brand {
  padding-right: 58px;
}

.portfolio-admin-brand-mark > a,
.portfolio-admin-brand-mark .logo {
  color: inherit;
  text-decoration: none;
}

.portfolio-admin-nav__brand-mark {
  width: 58px;
  height: 41px;
}

.portfolio-admin-nav__brand-copy {
  min-width: 0;
}

.portfolio-admin-nav__environment {
  margin-top: 14px;
}

/* The flat grid stretched short cards to the height of adjacent cards. */
.portfolio-admin-dashboard__columns {
  display: grid;
  grid-template-columns: minmax(0, 1.55fr) minmax(260px, 0.8fr) minmax(300px, 0.95fr);
  align-items: start;
  gap: 14px;
}

.portfolio-admin-dashboard__column {
  display: grid;
  min-width: 0;
  align-content: start;
  gap: 14px;
}

.portfolio-admin-panel {
  min-height: 0;
}

@media (min-width: 761px) {
  .nav-toggler {
    position: fixed !important;
    top: 14px !important;
    right: auto !important;
    left: 238px !important;
    z-index: 100 !important;
  }
}

@media (max-width: 1180px) {
  .portfolio-admin-dashboard__columns {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .portfolio-admin-dashboard__column.is-wide {
    grid-column: 1 / -1;
  }
}

@media (max-width: 760px) {
  .template-default__nav {
    position: fixed !important;
  }

  .portfolio-admin-dashboard__columns {
    grid-template-columns: 1fr;
  }

  .portfolio-admin-dashboard__column.is-wide {
    grid-column: auto;
  }
}

/* Portfolio Admin Experience — shell polish:end */

/* Portfolio Admin Experience — topbar and metrics refinement:start */

:root {
  --portfolio-admin-topbar-height: 72px;
}

/* Replace the anonymous Payload header with a first-class control-room top bar. */
.app-header {
  display: none !important;
}

.nav-toggler {
  position: fixed !important;
  width: 1px !important;
  height: 1px !important;
  padding: 0 !important;
  overflow: hidden !important;
  opacity: 0 !important;
  pointer-events: none !important;
}

.portfolio-admin-topbar {
  position: sticky;
  top: 0;
  z-index: 120;
  display: grid;
  width: 100%;
  min-height: var(--portfolio-admin-topbar-height);
  grid-template-columns: minmax(0, 1fr) minmax(250px, 420px) auto;
  align-items: center;
  gap: 20px;
  padding: 0 clamp(18px, 2.2vw, 32px);
  border-bottom: 1px solid var(--portfolio-admin-border);
  background:
    linear-gradient(90deg, rgba(50, 215, 231, 0.025), transparent 34%), rgba(8, 16, 24, 0.94);
  box-sizing: border-box;
  backdrop-filter: blur(18px);
}

.portfolio-admin-topbar__left,
.portfolio-admin-topbar__right {
  display: flex;
  min-width: 0;
  align-items: center;
}

.portfolio-admin-topbar__left {
  gap: 14px;
}

.portfolio-admin-topbar__right {
  justify-content: flex-end;
  gap: 9px;
}

.portfolio-admin-topbar__nav-toggle,
.portfolio-admin-topbar__icon-button {
  display: grid;
  width: 38px;
  height: 38px;
  flex: none;
  padding: 9px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 10px;
  background: rgba(15, 25, 35, 0.72);
  color: var(--portfolio-admin-text-soft);
  cursor: pointer;
  text-decoration: none;
  transition:
    border-color 150ms ease,
    background-color 150ms ease,
    color 150ms ease;
}

.portfolio-admin-topbar__nav-toggle:hover,
.portfolio-admin-topbar__icon-button:hover {
  border-color: rgba(50, 215, 231, 0.5);
  background: var(--portfolio-admin-cyan-soft);
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-topbar__nav-toggle svg,
.portfolio-admin-topbar__icon-button svg,
.portfolio-admin-topbar__search svg,
.portfolio-admin-operator__chevron svg {
  display: block;
  width: 100%;
  height: 100%;
}

.portfolio-admin-topbar__breadcrumbs {
  min-width: 0;
}

.portfolio-admin-topbar__breadcrumbs ol {
  display: flex;
  min-width: 0;
  align-items: center;
  gap: 9px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.portfolio-admin-topbar__breadcrumbs li {
  overflow: hidden;
  color: var(--portfolio-admin-text-muted);
  font-size: 12px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-topbar__breadcrumbs li + li {
  color: var(--portfolio-admin-text);
  font-weight: 600;
}

.portfolio-admin-topbar__breadcrumbs li + li::before {
  margin-right: 9px;
  color: var(--portfolio-admin-border-strong);
  content: '/';
}

.portfolio-admin-topbar__environment {
  display: inline-flex;
  flex: none;
  align-items: center;
  gap: 7px;
  padding: 5px 8px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 9px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-topbar__environment i {
  width: 6px;
  height: 6px;
  border-radius: 999px;
  background: var(--portfolio-admin-green);
  box-shadow: 0 0 9px rgba(80, 220, 137, 0.55);
}

.portfolio-admin-topbar__search {
  display: grid;
  width: 100%;
  min-height: 38px;
  grid-template-columns: 17px minmax(0, 1fr) auto;
  align-items: center;
  gap: 10px;
  padding: 0 10px 0 12px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 10px;
  background: rgba(15, 25, 35, 0.62);
  color: var(--portfolio-admin-text-muted);
  cursor: pointer;
  text-align: left;
  transition:
    border-color 150ms ease,
    background-color 150ms ease;
}

.portfolio-admin-topbar__search:hover {
  border-color: var(--portfolio-admin-border-strong);
  background: rgba(18, 31, 43, 0.84);
}

.portfolio-admin-topbar__search span {
  overflow: hidden;
  font-size: 11px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-topbar kbd,
.portfolio-admin-command kbd {
  padding: 3px 6px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 6px;
  background: var(--portfolio-admin-bg-soft);
  color: var(--portfolio-admin-text-muted);
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 9px;
  font-weight: 500;
  line-height: 1;
}

.portfolio-admin-topbar__site-link {
  display: inline-flex;
  min-height: 38px;
  align-items: center;
  gap: 8px;
  padding: 0 13px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 10px;
  background: rgba(15, 25, 35, 0.62);
  color: var(--portfolio-admin-text-soft);
  font-size: 11px;
  font-weight: 600;
  text-decoration: none;
  transition:
    border-color 150ms ease,
    color 150ms ease;
}

.portfolio-admin-topbar__site-link:hover {
  border-color: rgba(50, 215, 231, 0.48);
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-topbar__icon-button {
  position: relative;
}

.portfolio-admin-topbar__icon-button > i {
  position: absolute;
  top: 7px;
  right: 7px;
  width: 5px;
  height: 5px;
  border: 1px solid var(--portfolio-admin-bg);
  border-radius: 999px;
  background: var(--portfolio-admin-purple);
}

/* Technological operator control instead of the default anonymous avatar. */
.portfolio-admin-operator {
  position: relative;
}

.portfolio-admin-operator__trigger {
  display: grid;
  min-width: 190px;
  min-height: 44px;
  grid-template-columns: 36px minmax(0, 1fr) 18px;
  align-items: center;
  gap: 10px;
  padding: 4px 8px 4px 5px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background:
    linear-gradient(90deg, rgba(50, 215, 231, 0.055), transparent 46%), rgba(15, 25, 35, 0.76);
  color: var(--portfolio-admin-text);
  cursor: pointer;
  text-align: left;
  transition:
    border-color 150ms ease,
    background-color 150ms ease,
    box-shadow 150ms ease;
}

.portfolio-admin-operator__trigger:hover,
.portfolio-admin-operator__trigger[aria-expanded='true'] {
  border-color: rgba(50, 215, 231, 0.48);
  background:
    linear-gradient(90deg, rgba(50, 215, 231, 0.09), transparent 55%), rgba(18, 31, 43, 0.92);
  box-shadow: 0 0 0 3px rgba(50, 215, 231, 0.045);
}

.portfolio-admin-operator__mark {
  position: relative;
  display: grid;
  width: 34px;
  height: 34px;
  place-items: center;
  overflow: hidden;
  border: 1px solid rgba(50, 215, 231, 0.46);
  border-radius: 9px 3px 9px 3px;
  background:
    linear-gradient(135deg, rgba(50, 215, 231, 0.18), rgba(168, 103, 255, 0.09)),
    var(--portfolio-admin-bg-soft);
  color: var(--portfolio-admin-cyan);
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: -0.03em;
}

.portfolio-admin-operator__mark::before,
.portfolio-admin-operator__mark::after {
  position: absolute;
  width: 7px;
  height: 7px;
  content: '';
}

.portfolio-admin-operator__mark::before {
  top: 3px;
  left: 3px;
  border-top: 1px solid var(--portfolio-admin-cyan);
  border-left: 1px solid var(--portfolio-admin-cyan);
}

.portfolio-admin-operator__mark::after {
  right: 3px;
  bottom: 3px;
  border-right: 1px solid var(--portfolio-admin-purple);
  border-bottom: 1px solid var(--portfolio-admin-purple);
}

.portfolio-admin-operator__copy {
  display: grid;
  min-width: 0;
  gap: 2px;
}

.portfolio-admin-operator__copy strong {
  overflow: hidden;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 10px;
  letter-spacing: 0.04em;
  text-overflow: ellipsis;
  text-transform: uppercase;
  white-space: nowrap;
}

.portfolio-admin-operator__copy small {
  display: flex;
  align-items: center;
  gap: 5px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.portfolio-admin-operator__copy small i {
  width: 5px;
  height: 5px;
  border-radius: 999px;
  background: var(--portfolio-admin-green);
  box-shadow: 0 0 8px rgba(80, 220, 137, 0.55);
}

.portfolio-admin-operator__chevron {
  width: 17px;
  height: 17px;
  color: var(--portfolio-admin-text-muted);
  transition: transform 150ms ease;
}

.portfolio-admin-operator__trigger[aria-expanded='true'] .portfolio-admin-operator__chevron {
  transform: rotate(180deg);
}

.portfolio-admin-operator__menu {
  position: absolute;
  top: calc(100% + 10px);
  right: 0;
  z-index: 300;
  width: min(310px, calc(100vw - 28px));
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 13px;
  background: linear-gradient(145deg, rgba(50, 215, 231, 0.035), transparent 42%), #0b151f;
  box-shadow: 0 22px 60px rgba(0, 0, 0, 0.42);
}

.portfolio-admin-operator__identity {
  display: grid;
  gap: 4px;
  padding: 16px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-operator__identity > span {
  color: var(--portfolio-admin-cyan);
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 9px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-operator__identity strong {
  margin-top: 3px;
  color: var(--portfolio-admin-text);
  font-size: 13px;
}

.portfolio-admin-operator__identity small {
  overflow: hidden;
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-operator__menu-links {
  display: grid;
  padding: 6px;
}

.portfolio-admin-operator__menu-links a,
.portfolio-admin-operator__logout {
  display: grid;
  gap: 3px;
  padding: 10px 11px;
  border: 0;
  border-radius: 8px;
  background: transparent;
  color: var(--portfolio-admin-text);
  cursor: pointer;
  text-align: left;
  text-decoration: none;
  transition: background-color 140ms ease;
}

.portfolio-admin-operator__menu-links a:hover {
  background: var(--portfolio-admin-cyan-soft);
}

.portfolio-admin-operator__menu-links span,
.portfolio-admin-operator__logout span {
  font-size: 11px;
  font-weight: 600;
}

.portfolio-admin-operator__menu-links small,
.portfolio-admin-operator__logout small {
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
}

.portfolio-admin-operator__logout {
  width: calc(100% - 12px);
  margin: 0 6px 6px;
  border-top: 1px solid var(--portfolio-admin-border);
  border-radius: 0 0 8px 8px;
  color: var(--portfolio-admin-red);
}

.portfolio-admin-operator__logout:hover {
  background: var(--portfolio-admin-red-soft);
}

/* Command palette */
.portfolio-admin-command {
  position: fixed;
  inset: 0;
  z-index: 1000;
  display: grid;
  place-items: start center;
  padding: min(13vh, 120px) 18px 40px;
  background: rgba(3, 8, 13, 0.72);
  backdrop-filter: blur(10px);
}

.portfolio-admin-command__dialog {
  width: min(640px, 100%);
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 15px;
  background: #0b151f;
  box-shadow: 0 30px 90px rgba(0, 0, 0, 0.55);
}

.portfolio-admin-command__input-shell {
  display: grid;
  min-height: 60px;
  grid-template-columns: 20px minmax(0, 1fr) auto;
  align-items: center;
  gap: 12px;
  padding: 0 15px;
  border-bottom: 1px solid var(--portfolio-admin-border);
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-command__input-shell > svg {
  width: 20px;
  height: 20px;
}

.portfolio-admin-command__input-shell input {
  width: 100%;
  height: 58px;
  padding: 0 !important;
  border: 0 !important;
  outline: 0 !important;
  background: transparent !important;
  box-shadow: none !important;
  color: var(--portfolio-admin-text) !important;
  font-size: 14px;
}

.portfolio-admin-command__input-shell input::placeholder {
  color: var(--portfolio-admin-text-muted);
}

.portfolio-admin-command__results {
  max-height: min(54vh, 470px);
  overflow-y: auto;
  padding: 7px;
}

.portfolio-admin-command__results button {
  display: flex;
  width: 100%;
  min-height: 51px;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: 8px 11px;
  border: 0;
  border-radius: 9px;
  background: transparent;
  color: var(--portfolio-admin-text);
  cursor: pointer;
  text-align: left;
}

.portfolio-admin-command__results button:hover,
.portfolio-admin-command__results button:focus-visible {
  background: var(--portfolio-admin-cyan-soft);
}

.portfolio-admin-command__results button > span:first-child {
  display: grid;
  gap: 3px;
}

.portfolio-admin-command__results small {
  color: var(--portfolio-admin-cyan);
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 8px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-command__results strong {
  font-size: 12px;
}

.portfolio-admin-command__results button > span:last-child {
  color: var(--portfolio-admin-text-muted);
}

.portfolio-admin-command__empty {
  display: grid;
  gap: 5px;
  padding: 28px 18px;
  text-align: center;
}

.portfolio-admin-command__empty strong {
  font-size: 12px;
}

.portfolio-admin-command__empty span {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
}

.portfolio-admin-command footer {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 16px;
  padding: 10px 14px;
  border-top: 1px solid var(--portfolio-admin-border);
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
}

.portfolio-admin-command footer span {
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

/* Align the sidebar brand row with the new top bar. */
.portfolio-admin-nav__brand {
  display: flex;
  height: var(--portfolio-admin-topbar-height);
  min-height: var(--portfolio-admin-topbar-height);
  align-items: center;
  padding: 0 18px !important;
  box-sizing: border-box;
}

.portfolio-admin-nav__brand-identity {
  width: 100%;
  gap: 10px;
}

.portfolio-admin-nav__brand-mark {
  width: 58px;
  height: 41px;
}

.portfolio-admin-nav__brand-copy strong {
  font-size: 13px;
}

.portfolio-admin-nav__brand-copy small {
  font-size: 8px;
}

.portfolio-admin-nav__environment,
.portfolio-admin-nav__account {
  display: none !important;
}

.portfolio-admin-nav__scroll {
  padding-top: 11px;
}

/* Distinct instrument-style KPI cards. */
.portfolio-admin-dashboard__metrics {
  gap: 12px;
  margin-bottom: 12px;
}

.portfolio-admin-metric {
  --metric-accent: var(--portfolio-admin-cyan);
  --metric-soft: var(--portfolio-admin-cyan-soft);
  position: relative;
  display: flex;
  min-height: 118px;
  flex-direction: column;
  justify-content: space-between;
  overflow: hidden;
  padding: 14px 15px 13px;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 15px 4px 15px 4px;
  background:
    linear-gradient(135deg, var(--metric-soft), transparent 43%),
    repeating-linear-gradient(
      90deg,
      transparent 0,
      transparent 23px,
      rgba(255, 255, 255, 0.018) 24px
    ),
    #0c1721;
  box-shadow:
    inset 3px 0 0 var(--metric-accent),
    inset 0 1px 0 rgba(255, 255, 255, 0.025),
    0 9px 28px rgba(0, 0, 0, 0.1);
  color: var(--portfolio-admin-text) !important;
}

.portfolio-admin-metric::before {
  position: absolute;
  top: 0;
  left: 0;
  width: 44%;
  height: 1px;
  background: linear-gradient(90deg, var(--metric-accent), transparent);
  content: '';
}

.portfolio-admin-metric::after {
  position: absolute;
  right: 8px;
  bottom: 8px;
  width: 16px;
  height: 16px;
  border-right: 1px solid var(--metric-accent);
  border-bottom: 1px solid var(--metric-accent);
  opacity: 0.26;
  content: '';
}

.portfolio-admin-metric:hover {
  border-color: color-mix(in srgb, var(--metric-accent) 48%, var(--portfolio-admin-border));
  box-shadow:
    inset 3px 0 0 var(--metric-accent),
    0 12px 32px rgba(0, 0, 0, 0.17),
    0 0 0 3px color-mix(in srgb, var(--metric-accent) 6%, transparent);
  transform: translateY(-2px);
}

.portfolio-admin-metric.is-cyan {
  --metric-accent: var(--portfolio-admin-cyan);
  --metric-soft: rgba(50, 215, 231, 0.1);
}

.portfolio-admin-metric.is-purple {
  --metric-accent: var(--portfolio-admin-purple);
  --metric-soft: rgba(168, 103, 255, 0.1);
}

.portfolio-admin-metric.is-green {
  --metric-accent: var(--portfolio-admin-green);
  --metric-soft: rgba(80, 220, 137, 0.085);
}

.portfolio-admin-metric.is-amber {
  --metric-accent: var(--portfolio-admin-amber);
  --metric-soft: rgba(241, 185, 63, 0.09);
}

.portfolio-admin-metric__header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
}

.portfolio-admin-metric__header > span:first-child {
  padding-top: 2px;
  color: var(--portfolio-admin-text-soft) !important;
  font-size: 9px;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-metric__icon {
  display: grid;
  width: 32px;
  height: 32px;
  flex: none;
  padding: 7px;
  place-items: center;
  border: 1px solid color-mix(in srgb, var(--metric-accent) 34%, transparent);
  border-radius: 9px 3px 9px 3px;
  background: color-mix(in srgb, var(--metric-accent) 10%, transparent);
  color: var(--metric-accent) !important;
}

.portfolio-admin-metric__icon svg {
  width: 100%;
  height: 100%;
}

.portfolio-admin-metric__body {
  display: grid;
  gap: 2px;
}

.portfolio-admin-metric__body strong {
  margin: 0;
  color: var(--metric-accent) !important;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 31px;
  font-weight: 700;
  letter-spacing: -0.065em;
  line-height: 1;
}

.portfolio-admin-metric__body strong em {
  margin-left: 2px;
  font-size: 15px;
  font-style: normal;
  letter-spacing: -0.02em;
}

.portfolio-admin-metric__body small {
  margin: 0;
  color: var(--portfolio-admin-text-muted) !important;
  font-size: 9px;
  opacity: 1 !important;
}

.portfolio-admin-metric__code {
  position: absolute;
  right: 15px;
  bottom: 14px;
  color: var(--metric-accent) !important;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 7px !important;
  letter-spacing: 0.14em !important;
  opacity: 0.58 !important;
}

.portfolio-admin-metric__signal {
  position: absolute;
  right: 15px;
  bottom: 29px;
  display: flex;
  align-items: flex-end;
  gap: 2px;
  color: var(--metric-accent) !important;
  opacity: 1 !important;
}

.portfolio-admin-metric .portfolio-admin-metric__signal i {
  position: static;
  display: block;
  width: 2px;
  height: 5px;
  border: 0;
  border-radius: 2px;
  background: var(--metric-accent);
  opacity: 0.32;
}

.portfolio-admin-metric .portfolio-admin-metric__signal i:nth-child(2) {
  height: 8px;
  opacity: 0.48;
}

.portfolio-admin-metric .portfolio-admin-metric__signal i:nth-child(3) {
  height: 11px;
  opacity: 0.7;
}

.portfolio-admin-metric .portfolio-admin-metric__signal i:nth-child(4) {
  height: 14px;
  opacity: 0.95;
}

/* Compact the supporting panels without changing their visual language. */
.portfolio-admin-dashboard {
  padding-top: clamp(22px, 2.5vw, 34px);
}

.portfolio-admin-dashboard__hero {
  margin-bottom: 19px;
}

.portfolio-admin-panel > header {
  padding: 12px 15px;
}

.portfolio-admin-panel__footer-link,
.portfolio-admin-panel__note {
  padding: 11px 15px;
}

.portfolio-admin-attention-list li {
  min-height: 40px;
  padding: 8px 15px;
}

.portfolio-admin-status-list > div {
  min-height: 39px;
  padding: 8px 15px;
}

.portfolio-admin-integration-list li {
  padding: 9px 15px;
}

.portfolio-admin-activity-list li,
.portfolio-admin-inbox-list li {
  min-height: 47px;
  padding: 8px 15px;
}

.portfolio-admin-quick-actions a {
  padding: 10px 32px 10px 15px;
}

.portfolio-admin-empty-state {
  padding: 20px 15px;
}

@media (max-width: 1280px) {
  .portfolio-admin-topbar {
    grid-template-columns: minmax(0, 1fr) minmax(210px, 320px) auto;
    gap: 13px;
  }

  .portfolio-admin-topbar__environment {
    display: none;
  }

  .portfolio-admin-operator__trigger {
    min-width: 164px;
  }
}

@media (max-width: 1040px) {
  .portfolio-admin-topbar {
    grid-template-columns: minmax(0, 1fr) auto;
  }

  .portfolio-admin-topbar__search {
    display: none;
  }

  .portfolio-admin-operator__copy {
    display: none;
  }

  .portfolio-admin-operator__trigger {
    min-width: 58px;
    grid-template-columns: 34px 14px;
  }
}

@media (max-width: 760px) {
  :root {
    --portfolio-admin-topbar-height: 62px;
  }

  .portfolio-admin-topbar {
    grid-template-columns: minmax(0, 1fr) auto;
    padding: 0 12px;
  }

  .portfolio-admin-topbar__breadcrumbs li:first-child,
  .portfolio-admin-topbar__site-link,
  .portfolio-admin-topbar__icon-button {
    display: none;
  }

  .portfolio-admin-topbar__breadcrumbs li + li::before {
    display: none;
  }

  .portfolio-admin-operator__trigger {
    min-width: 52px;
    grid-template-columns: 32px 12px;
    gap: 5px;
  }

  .portfolio-admin-operator__mark {
    width: 32px;
    height: 32px;
  }

  .portfolio-admin-nav__brand {
    padding: 0 14px !important;
  }

  .portfolio-admin-dashboard__metrics {
    gap: 10px;
  }

  .portfolio-admin-metric {
    min-height: 112px;
  }

  .portfolio-admin-command {
    padding: 76px 10px 20px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .portfolio-admin-metric,
  .portfolio-admin-operator__chevron,
  .portfolio-admin-topbar__nav-toggle,
  .portfolio-admin-topbar__icon-button,
  .portfolio-admin-operator__trigger {
    transition: none;
  }

  .portfolio-admin-metric:hover {
    transform: none;
  }
}

/* Portfolio Admin Experience — topbar and metrics refinement:end */

/* Portfolio Admin Experience — auth actions and topbar lint fix:start */

.portfolio-admin-auth-options {
  display: flex;
  width: 100%;
  min-height: 34px;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  margin: 2px 0 0;
}

.portfolio-admin-auth-remember {
  position: relative;
  display: inline-flex;
  min-height: 32px;
  align-items: center;
  gap: 9px;
  color: var(--portfolio-admin-text-soft);
  cursor: pointer;
  font-size: 13px;
  line-height: 1;
  user-select: none;
  transition: color 150ms ease;
}

.portfolio-admin-auth-remember:hover {
  color: var(--portfolio-admin-text);
}

.portfolio-admin-auth-remember__input {
  position: absolute;
  width: 1px;
  height: 1px;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  opacity: 0;
}

.portfolio-admin-auth-remember__box {
  display: grid;
  width: 18px;
  height: 18px;
  flex: 0 0 18px;
  place-items: center;
  border: 1px solid rgba(115, 130, 142, 0.46);
  border-radius: 5px;
  background:
    linear-gradient(145deg, rgba(255, 255, 255, 0.025), transparent 60%), rgba(14, 25, 35, 0.76);
  color: transparent;
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.025);
  transition:
    border-color 150ms ease,
    background-color 150ms ease,
    box-shadow 150ms ease,
    color 150ms ease;
}

.portfolio-admin-auth-remember__box svg {
  width: 13px;
  height: 13px;
}

.portfolio-admin-auth-remember__input:checked + .portfolio-admin-auth-remember__box {
  border-color: rgba(50, 215, 231, 0.82);
  background: rgba(50, 215, 231, 0.13);
  color: var(--portfolio-admin-cyan);
  box-shadow:
    0 0 0 3px rgba(50, 215, 231, 0.07),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);
}

.portfolio-admin-auth-remember__input:focus-visible + .portfolio-admin-auth-remember__box {
  outline: 2px solid var(--portfolio-admin-cyan);
  outline-offset: 2px;
}

.login__form .portfolio-admin-auth-options .portfolio-admin-auth-forgot,
.login__form .portfolio-admin-auth-options a[href*='forgot'] {
  position: relative;
  display: inline-flex;
  min-height: 32px;
  align-items: center;
  margin: 0 !important;
  padding: 0;
  color: var(--portfolio-admin-text-soft);
  font-size: 13px;
  font-weight: 500;
  text-decoration: none;
  transition: color 150ms ease;
}

.login__form .portfolio-admin-auth-options .portfolio-admin-auth-forgot::after,
.login__form .portfolio-admin-auth-options a[href*='forgot']::after {
  position: absolute;
  right: 0;
  bottom: 4px;
  left: 0;
  height: 1px;
  background: var(--portfolio-admin-cyan);
  content: '';
  opacity: 0.7;
  transform: scaleX(0);
  transform-origin: right;
  transition: transform 150ms ease;
}

.login__form .portfolio-admin-auth-options .portfolio-admin-auth-forgot:hover,
.login__form .portfolio-admin-auth-options a[href*='forgot']:hover {
  color: var(--portfolio-admin-cyan);
}

.login__form .portfolio-admin-auth-options .portfolio-admin-auth-forgot:hover::after,
.login__form .portfolio-admin-auth-options a[href*='forgot']:hover::after {
  transform: scaleX(1);
  transform-origin: left;
}

.login__form .portfolio-admin-auth-options .portfolio-admin-auth-forgot:focus-visible,
.login__form .portfolio-admin-auth-options a[href*='forgot']:focus-visible {
  border-radius: 3px;
  outline: 2px solid var(--portfolio-admin-cyan);
  outline-offset: 3px;
}

.login__form button[type='submit'] {
  margin-top: 12px !important;
}

@media (max-width: 520px) {
  .portfolio-admin-auth-options {
    align-items: flex-start;
    flex-direction: column;
    gap: 2px;
  }
}

/* Portfolio Admin Experience — auth actions and topbar lint fix:end */

/* Portfolio Admin Experience — viewport shell geometry:start */

/*
 * Stable workspace geometry.
 *
 * Payload's default template uses its own responsive grid. Because our Nav is
 * fixed, the template must be taken out of that grid explicitly; otherwise the
 * workspace can collapse to the width of the remaining grid track.
 */
:root {
  --portfolio-admin-sidebar-width: 285px;
}

.template-default {
  display: block !important;
  width: 100% !important;
  min-width: 0 !important;
  min-height: 100vh;
  min-height: 100dvh;
}

.template-default__wrap {
  display: block !important;
  width: auto !important;
  min-width: 0 !important;
  min-height: 100vh !important;
  min-height: 100dvh !important;
  margin-left: var(--portfolio-admin-sidebar-width) !important;
  padding-top: var(--portfolio-admin-topbar-height) !important;
  box-sizing: border-box;
  transition: margin-left 220ms cubic-bezier(0.22, 1, 0.36, 1);
}

.template-default__wrap > * {
  min-width: 0;
}

.portfolio-admin-nav {
  position: fixed !important;
  inset: 0 auto 0 0;
  z-index: 160;
  display: flex;
  width: var(--portfolio-admin-sidebar-width) !important;
  height: 100vh !important;
  height: 100dvh !important;
  min-height: 0 !important;
  overflow: hidden;
  border-right: 1px solid var(--portfolio-admin-border);
  background: #071019;
  box-shadow: 12px 0 34px rgba(0, 0, 0, 0.08);
  opacity: 1;
  transform: translate3d(0, 0, 0);
  visibility: visible;
  transition:
    transform 220ms cubic-bezier(0.22, 1, 0.36, 1),
    opacity 150ms ease,
    visibility 0s linear 0s;
  will-change: transform;
}

.portfolio-admin-nav__brand {
  position: relative;
  z-index: 1;
  flex: 0 0 var(--portfolio-admin-topbar-height);
  background: linear-gradient(90deg, rgba(50, 215, 231, 0.025), transparent 64%), #071019;
}

.portfolio-admin-nav__scroll {
  flex: 1 1 auto;
  min-height: 0;
  overflow-x: hidden;
  overflow-y: auto;
}

.portfolio-admin-topbar {
  position: fixed !important;
  inset: 0 0 auto var(--portfolio-admin-sidebar-width);
  z-index: 150;
  width: auto !important;
  max-width: none !important;
  transition:
    left 220ms cubic-bezier(0.22, 1, 0.36, 1),
    box-shadow 180ms ease;
}

/* Closed desktop state. */
html[data-portfolio-admin-nav='closed'] .portfolio-admin-nav {
  pointer-events: none;
  opacity: 0;
  transform: translate3d(calc(-100% - 1px), 0, 0);
  visibility: hidden;
  transition:
    transform 220ms cubic-bezier(0.22, 1, 0.36, 1),
    opacity 140ms ease,
    visibility 0s linear 220ms;
}

html[data-portfolio-admin-nav='closed'] .portfolio-admin-topbar {
  left: 0;
}

html[data-portfolio-admin-nav='closed'] .template-default__wrap {
  margin-left: 0 !important;
}

/* Toggle motion. */
.portfolio-admin-topbar__nav-toggle {
  overflow: hidden;
  transform: translateZ(0);
  transition:
    border-color 150ms ease,
    background-color 150ms ease,
    color 150ms ease,
    transform 120ms ease,
    box-shadow 150ms ease;
}

.portfolio-admin-topbar__nav-toggle:active {
  transform: scale(0.94);
}

.portfolio-admin-topbar__nav-toggle[aria-expanded='true'] {
  border-color: rgba(50, 215, 231, 0.34);
  background: rgba(50, 215, 231, 0.065);
  color: var(--portfolio-admin-cyan);
  box-shadow: inset 0 0 0 1px rgba(50, 215, 231, 0.025);
}

.portfolio-admin-sidebar-toggle__frame,
.portfolio-admin-sidebar-toggle__rail,
.portfolio-admin-sidebar-toggle__chevron {
  transform-box: fill-box;
  transform-origin: center;
  transition:
    transform 220ms cubic-bezier(0.22, 1, 0.36, 1),
    opacity 150ms ease;
}

.portfolio-admin-topbar__nav-toggle[aria-expanded='true'] .portfolio-admin-sidebar-toggle__chevron {
  transform: rotate(180deg);
}

.portfolio-admin-topbar__nav-toggle[aria-expanded='false'] .portfolio-admin-sidebar-toggle__rail {
  transform: scaleY(0.72);
}

.portfolio-admin-topbar__nav-toggle[aria-expanded='false'] .portfolio-admin-sidebar-toggle__frame {
  transform: scaleX(0.94);
}

/* Mobile: sidebar becomes an overlay and does not reserve workspace width. */
@media (max-width: 768px) {
  :root {
    --portfolio-admin-sidebar-width: min(88vw, 320px);
  }

  .portfolio-admin-topbar {
    left: 0;
  }

  .template-default__wrap,
  html[data-portfolio-admin-nav='closed'] .template-default__wrap {
    margin-left: 0 !important;
  }

  .portfolio-admin-nav {
    box-shadow: 24px 0 60px rgba(0, 0, 0, 0.34);
  }
}

@media (prefers-reduced-motion: reduce) {
  .template-default__wrap,
  .portfolio-admin-topbar,
  .portfolio-admin-nav,
  .portfolio-admin-topbar__nav-toggle,
  .portfolio-admin-sidebar-toggle__frame,
  .portfolio-admin-sidebar-toggle__rail,
  .portfolio-admin-sidebar-toggle__chevron {
    transition: none !important;
  }
}

/* Portfolio Admin Experience — viewport shell geometry:end */

/* Portfolio Admin Experience - projects list visual system:start */

.portfolio-admin-projects-overview {
  width: 100%;
  box-sizing: border-box;
  padding: 30px clamp(28px, 4vw, 64px) 18px;
}

.portfolio-admin-projects-overview__header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin: 0 0 20px;
}

.portfolio-admin-projects-overview__header > div {
  min-width: 0;
}

.portfolio-admin-projects-overview__eyebrow {
  display: block;
  margin-bottom: 8px;
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.16em;
  line-height: 1;
  text-transform: uppercase;
}

.portfolio-admin-projects-overview__header h2 {
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(24px, 2.3vw, 34px);
  font-weight: 600;
  letter-spacing: -0.035em;
  line-height: 1.05;
}

.portfolio-admin-projects-overview__header p {
  max-width: 760px;
  margin: 10px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 14px;
  line-height: 1.55;
}

.portfolio-admin-projects-overview .portfolio-admin-button {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-height: 42px;
  padding: 0 18px;
  border: 1px solid rgba(50, 215, 231, 0.5);
  border-radius: 9px;
  background: var(--portfolio-admin-cyan);
  color: #031016;
  font-size: 13px;
  font-weight: 700;
  text-decoration: none;
  box-shadow: 0 12px 28px rgba(50, 215, 231, 0.08);
  transition:
    transform 130ms ease,
    background-color 150ms ease,
    border-color 150ms ease,
    box-shadow 150ms ease;
}

.portfolio-admin-projects-overview .portfolio-admin-button:hover {
  background: #5ee5f0;
  border-color: #5ee5f0;
  box-shadow: 0 14px 34px rgba(50, 215, 231, 0.14);
  transform: translateY(-1px);
}

.portfolio-admin-projects-overview .portfolio-admin-button:active {
  transform: translateY(0) scale(0.98);
}

.portfolio-admin-projects-overview__metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
}

.portfolio-admin-project-stat {
  position: relative;
  min-width: 0;
  min-height: 132px;
  overflow: hidden;
  padding: 18px 18px 16px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(
      135deg,
      color-mix(in srgb, var(--project-stat-accent) 8%, transparent),
      transparent 58%
    ),
    var(--portfolio-admin-surface);
  box-shadow: inset 3px 0 0 var(--project-stat-accent);
}

.portfolio-admin-project-stat::after {
  position: absolute;
  right: -28px;
  bottom: -42px;
  width: 120px;
  height: 120px;
  border: 1px solid color-mix(in srgb, var(--project-stat-accent) 22%, transparent);
  border-radius: 50%;
  content: '';
  opacity: 0.65;
}

.portfolio-admin-project-stat.is-cyan {
  --project-stat-accent: var(--portfolio-admin-cyan);
}

.portfolio-admin-project-stat.is-purple {
  --project-stat-accent: var(--portfolio-admin-violet);
}

.portfolio-admin-project-stat.is-green {
  --project-stat-accent: var(--portfolio-admin-success);
}

.portfolio-admin-project-stat.is-amber {
  --project-stat-accent: var(--portfolio-admin-warning);
}

.portfolio-admin-project-stat__topline {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.portfolio-admin-project-stat__topline span {
  overflow: hidden;
  color: var(--portfolio-admin-text-muted);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.11em;
  text-overflow: ellipsis;
  text-transform: uppercase;
  white-space: nowrap;
}

.portfolio-admin-project-stat__topline code {
  color: color-mix(in srgb, var(--project-stat-accent) 78%, white);
  font-family: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 10px;
  letter-spacing: 0.08em;
}

.portfolio-admin-project-stat > strong {
  position: relative;
  z-index: 1;
  display: block;
  margin-top: 15px;
  color: var(--project-stat-accent);
  font-family: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 29px;
  font-weight: 650;
  letter-spacing: -0.05em;
  line-height: 1;
}

.portfolio-admin-project-stat > p {
  position: relative;
  z-index: 1;
  max-width: 82%;
  margin: 8px 0 0;
  color: var(--portfolio-admin-text-dim);
  font-size: 11px;
  line-height: 1.45;
}

.portfolio-admin-project-stat__signal {
  position: absolute;
  right: 16px;
  bottom: 15px;
  z-index: 1;
  display: flex;
  align-items: flex-end;
  gap: 3px;
  height: 22px;
}

.portfolio-admin-project-stat__signal i {
  display: block;
  width: 2px;
  border-radius: 999px;
  background: var(--project-stat-accent);
  opacity: 0.78;
}

.portfolio-admin-project-stat__signal i:nth-child(1) {
  height: 7px;
}

.portfolio-admin-project-stat__signal i:nth-child(2) {
  height: 14px;
}

.portfolio-admin-project-stat__signal i:nth-child(3) {
  height: 21px;
}

.portfolio-admin-project-cell {
  display: flex;
  align-items: center;
  gap: 12px;
  min-width: 220px;
}

.portfolio-admin-project-cell__preview {
  flex: 0 0 42px;
  display: grid;
  width: 42px;
  height: 42px;
  place-items: center;
  overflow: hidden;
  border: 1px solid rgba(50, 215, 231, 0.18);
  border-radius: 8px;
  background: linear-gradient(135deg, rgba(50, 215, 231, 0.13), rgba(155, 92, 255, 0.12)), #0d1924;
  color: var(--portfolio-admin-cyan);
  background-position: center;
  background-size: cover;
  font-family: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.04em;
}

.portfolio-admin-project-cell__preview.has-image {
  color: transparent;
}

.portfolio-admin-project-cell__copy {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 4px;
}

.portfolio-admin-project-cell__copy strong {
  overflow: hidden;
  color: var(--portfolio-admin-text);
  font-size: 13px;
  font-weight: 600;
  line-height: 1.25;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-project-cell__copy code {
  overflow: hidden;
  color: var(--portfolio-admin-text-dim);
  font-family: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 10px;
  line-height: 1.2;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-stage {
  --stage-color: #7890a4;
  display: inline-flex;
  align-items: center;
  gap: 7px;
  width: max-content;
  min-height: 26px;
  padding: 0 10px;
  border: 1px solid color-mix(in srgb, var(--stage-color) 28%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--stage-color) 10%, transparent);
  color: color-mix(in srgb, var(--stage-color) 84%, white);
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.055em;
  line-height: 1;
  text-transform: uppercase;
}

.portfolio-admin-stage i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--stage-color);
  box-shadow: 0 0 9px color-mix(in srgb, var(--stage-color) 62%, transparent);
}

.portfolio-admin-stage.is-idea,
.portfolio-admin-stage.is-planning {
  --stage-color: #7890a4;
}

.portfolio-admin-stage.is-development {
  --stage-color: var(--portfolio-admin-violet);
}

.portfolio-admin-stage.is-testing {
  --stage-color: var(--portfolio-admin-warning);
}

.portfolio-admin-stage.is-released {
  --stage-color: var(--portfolio-admin-cyan);
}

.portfolio-admin-stage.is-maintenance {
  --stage-color: var(--portfolio-admin-success);
}

.portfolio-admin-stage.is-archived {
  --stage-color: #697581;
}

.portfolio-admin-progress-cell {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 138px;
}

.portfolio-admin-progress-cell__track {
  position: relative;
  flex: 1 1 auto;
  display: block;
  min-width: 86px;
  height: 5px;
  overflow: hidden;
  border-radius: 999px;
  background: rgba(125, 148, 167, 0.13);
}

.portfolio-admin-progress-cell__track > span {
  display: block;
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--portfolio-admin-cyan), var(--portfolio-admin-violet));
  box-shadow: 0 0 10px rgba(50, 215, 231, 0.18);
}

.portfolio-admin-progress-cell code {
  min-width: 34px;
  color: var(--portfolio-admin-text);
  font-family: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 10px;
  text-align: right;
}

.portfolio-admin-featured-cell {
  display: inline-grid;
  width: 30px;
  height: 30px;
  place-items: center;
  border: 1px solid transparent;
  border-radius: 8px;
  color: #647789;
}

.portfolio-admin-featured-cell svg {
  display: block !important;
  width: 17px !important;
  height: 17px !important;
  max-width: 17px !important;
  max-height: 17px !important;
}

.portfolio-admin-featured-cell.is-featured {
  border-color: rgba(50, 215, 231, 0.22);
  background: rgba(50, 215, 231, 0.07);
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-publication-cell {
  display: inline-flex;
  align-items: center;
  gap: 9px;
  min-width: 128px;
}

.portfolio-admin-publication-cell > i {
  flex: 0 0 7px;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--portfolio-admin-warning);
  box-shadow: 0 0 9px rgba(245, 179, 66, 0.28);
}

.portfolio-admin-publication-cell > span {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 3px;
}

.portfolio-admin-publication-cell strong {
  color: var(--portfolio-admin-text);
  font-size: 11px;
  font-weight: 600;
  line-height: 1.15;
}

.portfolio-admin-publication-cell small {
  color: var(--portfolio-admin-text-dim);
  font-family: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 9px;
  line-height: 1.15;
}

.portfolio-admin-publication-cell.is-published > i {
  background: var(--portfolio-admin-success);
  box-shadow: 0 0 9px rgba(69, 214, 139, 0.28);
}

.collection-list .table {
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background: var(--portfolio-admin-surface);
}

.collection-list .table table {
  border-collapse: separate;
  border-spacing: 0;
}

.collection-list .table thead {
  background: rgba(255, 255, 255, 0.012);
}

.collection-list .table tbody tr {
  transition:
    background-color 130ms ease,
    box-shadow 130ms ease;
}

.collection-list .table tbody tr:hover {
  background: rgba(50, 215, 231, 0.025);
  box-shadow: inset 2px 0 0 rgba(50, 215, 231, 0.42);
}

.collection-list .table td,
.collection-list .table th {
  vertical-align: middle;
}

@media (max-width: 1120px) {
  .portfolio-admin-projects-overview__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 768px) {
  .portfolio-admin-projects-overview {
    padding: 22px 18px 14px;
  }

  .portfolio-admin-projects-overview__header {
    align-items: stretch;
    flex-direction: column;
  }

  .portfolio-admin-projects-overview .portfolio-admin-button {
    align-self: flex-start;
  }

  .portfolio-admin-projects-overview__metrics {
    grid-template-columns: 1fr;
  }

  .portfolio-admin-project-stat {
    min-height: 116px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .portfolio-admin-projects-overview .portfolio-admin-button,
  .collection-list .table tbody tr {
    transition: none !important;
  }
}

/* Portfolio Admin Experience - projects list visual system:end */

/* Portfolio Admin Experience - projects duplicate create action:start */

/*
 * ProjectsListHeader already renders the primary "New project" action.
 * Hide Payload's duplicate title action only on a list view that contains
 * our Projects overview component. The native list heading remains visible.
 */
.template-default__wrap:has(.portfolio-admin-projects-overview) .list-header__title-actions {
  display: none !important;
}

/* Compatibility fallback for Payload builds where the title action wrapper
 * is flattened but the create link remains inside the list header.
 */
.template-default__wrap:has(.portfolio-admin-projects-overview)
  .list-header
  a[href*='/collections/projects/create'] {
  display: none !important;
}

/* Portfolio Admin Experience - projects duplicate create action:end */

/* Portfolio Admin Experience - project editor foundation:start */

.portfolio-admin-project-health {
  --project-health-score: 0%;
  display: block;
  overflow: hidden;
  margin: 8px 0 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(145deg, rgba(50, 215, 231, 0.045), transparent 48%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-project-health__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 17px 17px 13px;
}

.portfolio-admin-project-health__header > div {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 5px;
}

.portfolio-admin-project-health__header span:first-child {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-project-health__header strong {
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
  font-size: 27px;
  font-weight: 650;
  letter-spacing: -0.05em;
  line-height: 1;
}

.portfolio-admin-project-health__meter {
  position: relative;
  flex: 0 0 44px;
  display: grid;
  width: 44px;
  height: 44px;
  place-items: center;
  border-radius: 50%;
  background:
    radial-gradient(circle at center, var(--portfolio-admin-surface) 57%, transparent 59%),
    conic-gradient(
      var(--portfolio-admin-cyan) var(--project-health-score),
      rgba(118, 143, 163, 0.14) 0
    );
}

.portfolio-admin-project-health__meter::after {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--portfolio-admin-cyan);
  box-shadow: 0 0 10px rgba(50, 215, 231, 0.35);
  content: '';
}

.portfolio-admin-project-health__summary {
  margin: 0;
  padding: 0 17px 15px;
  color: var(--portfolio-admin-text-dim);
  font-size: 11px;
  line-height: 1.5;
}

.portfolio-admin-project-health__checks {
  border-top: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-project-health__check {
  display: flex;
  align-items: center;
  gap: 10px;
  min-height: 50px;
  padding: 8px 16px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-project-health__check > span {
  flex: 0 0 25px;
  display: grid;
  width: 25px;
  height: 25px;
  place-items: center;
  border: 1px solid rgba(122, 145, 164, 0.18);
  border-radius: 7px;
  color: #6f8495;
  background: rgba(122, 145, 164, 0.035);
}

.portfolio-admin-project-health__check > span svg {
  display: block;
  width: 15px;
  height: 15px;
}

.portfolio-admin-project-health__check.is-ready > span {
  border-color: rgba(69, 214, 139, 0.24);
  color: var(--portfolio-admin-success);
  background: rgba(69, 214, 139, 0.055);
}

.portfolio-admin-project-health__check > div {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 3px;
}

.portfolio-admin-project-health__check strong {
  color: var(--portfolio-admin-text);
  font-size: 11px;
  font-weight: 600;
  line-height: 1.2;
}

.portfolio-admin-project-health__check small {
  color: var(--portfolio-admin-text-dim);
  font-size: 9px;
  line-height: 1.3;
}

.portfolio-admin-project-health__preview {
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 42px;
  padding: 0 16px;
  color: #9edfee;
  font-size: 10px;
  font-weight: 600;
  text-decoration: none;
  transition:
    background-color 140ms ease,
    color 140ms ease;
}

.portfolio-admin-project-health__preview:hover {
  color: var(--portfolio-admin-cyan);
  background: rgba(50, 215, 231, 0.035);
}

.portfolio-admin-project-health__preview.is-disabled {
  color: var(--portfolio-admin-text-dim);
  cursor: default;
}

.collection-edit:has(.portfolio-admin-project-health) .tabs-field__tabs {
  position: sticky;
  top: calc(var(--portfolio-admin-topbar-height) + 8px);
  z-index: 12;
  display: flex;
  gap: 4px;
  overflow-x: auto;
  margin-bottom: 24px;
  padding: 5px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background: rgba(7, 16, 25, 0.94);
  box-shadow: 0 12px 26px rgba(0, 0, 0, 0.08);
  backdrop-filter: blur(16px);
}

.collection-edit:has(.portfolio-admin-project-health) .tabs-field__tab-button {
  min-height: 36px;
  margin: 0;
  padding: 0 13px;
  border: 1px solid transparent;
  border-radius: 7px;
  color: var(--portfolio-admin-text-dim);
  font-size: 11px;
  font-weight: 600;
  white-space: nowrap;
  transition:
    background-color 140ms ease,
    border-color 140ms ease,
    color 140ms ease;
}

.collection-edit:has(.portfolio-admin-project-health) .tabs-field__tab-button:hover {
  color: var(--portfolio-admin-text);
  background: rgba(255, 255, 255, 0.025);
}

.collection-edit:has(.portfolio-admin-project-health) .tabs-field__tab-button--active {
  border-color: rgba(50, 215, 231, 0.22);
  color: var(--portfolio-admin-cyan);
  background: rgba(50, 215, 231, 0.065);
}

.collection-edit:has(.portfolio-admin-project-health) .document-fields__main {
  min-width: 0;
}

.collection-edit:has(.portfolio-admin-project-health) .document-fields__sidebar {
  border-left-color: var(--portfolio-admin-border);
  background:
    linear-gradient(180deg, rgba(50, 215, 231, 0.018), transparent 220px), rgba(7, 16, 25, 0.32);
}

.collection-edit:has(.portfolio-admin-project-health) .document-fields__sidebar .field-type {
  margin-bottom: 18px;
}

@media (max-width: 1024px) {
  .collection-edit:has(.portfolio-admin-project-health) .tabs-field__tabs {
    position: static;
  }
}

@media (prefers-reduced-motion: reduce) {
  .portfolio-admin-project-health__preview,
  .collection-edit:has(.portfolio-admin-project-health) .tabs-field__tab-button {
    transition: none !important;
  }
}

/* Portfolio Admin Experience - project editor foundation:end */

/* Portfolio Admin Experience - project edit link:start */

.portfolio-admin-project-cell {
  position: relative;
  color: inherit;
  text-decoration: none;
  outline: none;
}

.portfolio-admin-project-cell__open {
  flex: 0 0 27px;
  display: grid;
  width: 27px;
  height: 27px;
  margin-left: auto;
  place-items: center;
  border: 1px solid transparent;
  border-radius: 7px;
  color: var(--portfolio-admin-text-dim);
  background: transparent;
  opacity: 0;
  transform: translateX(-4px);
  transition:
    opacity 130ms ease,
    transform 130ms ease,
    color 130ms ease,
    border-color 130ms ease,
    background-color 130ms ease;
}

.portfolio-admin-project-cell__open svg {
  display: block;
  width: 14px;
  height: 14px;
}

.portfolio-admin-project-cell:hover .portfolio-admin-project-cell__copy strong,
.portfolio-admin-project-cell:focus-visible .portfolio-admin-project-cell__copy strong {
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-project-cell:hover .portfolio-admin-project-cell__preview,
.portfolio-admin-project-cell:focus-visible .portfolio-admin-project-cell__preview {
  border-color: rgba(50, 215, 231, 0.42);
  box-shadow: 0 0 0 3px rgba(50, 215, 231, 0.045);
}

.portfolio-admin-project-cell:hover .portfolio-admin-project-cell__open,
.portfolio-admin-project-cell:focus-visible .portfolio-admin-project-cell__open {
  border-color: rgba(50, 215, 231, 0.2);
  color: var(--portfolio-admin-cyan);
  background: rgba(50, 215, 231, 0.055);
  opacity: 1;
  transform: translateX(0);
}

.portfolio-admin-project-cell:focus-visible {
  border-radius: 9px;
  box-shadow: 0 0 0 2px rgba(50, 215, 231, 0.38);
}

@media (pointer: coarse) {
  .portfolio-admin-project-cell__open {
    opacity: 1;
    transform: none;
  }
}

@media (prefers-reduced-motion: reduce) {
  .portfolio-admin-project-cell__open {
    transition: none !important;
  }
}

/* Portfolio Admin Experience - project edit link:end */

/* Portfolio Admin Experience - project editor operational polish:start */

.portfolio-admin-project-command {
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 0;
  margin-right: 6px;
}

.portfolio-admin-project-command__stage {
  --command-color: #7890a4;
  display: inline-flex;
  align-items: center;
  gap: 7px;
  min-height: 32px;
  padding: 0 10px;
  border: 1px solid color-mix(in srgb, var(--command-color) 28%, transparent);
  border-radius: 8px;
  background: color-mix(in srgb, var(--command-color) 8%, transparent);
  color: color-mix(in srgb, var(--command-color) 84%, white);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.07em;
  text-transform: uppercase;
  white-space: nowrap;
}

.portfolio-admin-project-command__stage i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--command-color);
  box-shadow: 0 0 9px color-mix(in srgb, var(--command-color) 58%, transparent);
}

.portfolio-admin-project-command__stage.is-development {
  --command-color: var(--portfolio-admin-violet);
}

.portfolio-admin-project-command__stage.is-testing {
  --command-color: var(--portfolio-admin-warning);
}

.portfolio-admin-project-command__stage.is-released {
  --command-color: var(--portfolio-admin-cyan);
}

.portfolio-admin-project-command__stage.is-maintenance {
  --command-color: var(--portfolio-admin-success);
}

.portfolio-admin-project-command__version {
  display: inline-flex;
  align-items: baseline;
  gap: 6px;
  min-height: 32px;
  padding: 0 10px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
  white-space: nowrap;
}

.portfolio-admin-project-command__version small {
  color: var(--portfolio-admin-text-dim);
  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.portfolio-admin-project-command__version code,
.portfolio-admin-project-command__updated time {
  color: var(--portfolio-admin-text);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-project-command__updated {
  color: var(--portfolio-admin-text-dim);
  font-size: 9px;
  white-space: nowrap;
}

.portfolio-admin-project-command__preview {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  min-height: 32px;
  padding: 0 11px;
  border: 1px solid rgba(50, 215, 231, 0.24);
  border-radius: 8px;
  background: rgba(50, 215, 231, 0.045);
  color: #a9e6ef;
  font-size: 10px;
  font-weight: 650;
  text-decoration: none;
  white-space: nowrap;
}

.portfolio-admin-project-command__preview:hover {
  border-color: rgba(50, 215, 231, 0.48);
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-project-command__preview.is-disabled {
  border-color: var(--portfolio-admin-border);
  color: var(--portfolio-admin-text-dim);
  cursor: not-allowed;
}

.portfolio-admin-project-progress-field {
  margin-bottom: 18px;
}

.portfolio-admin-project-progress-field__head {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 9px;
}

.portfolio-admin-project-progress-field__head label {
  color: var(--portfolio-admin-text);
  font-size: 12px;
}

.portfolio-admin-project-progress-field__head output {
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), monospace;
  font-size: 12px;
  font-weight: 650;
}

.portfolio-admin-project-progress-field__controls {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 74px;
  align-items: center;
  gap: 12px;
}

.portfolio-admin-project-progress-field input[type='range'] {
  width: 100%;
  height: 22px;
  margin: 0;
  appearance: none;
  cursor: pointer;
  background: transparent;
}

.portfolio-admin-project-progress-field input[type='range']::-webkit-slider-runnable-track {
  height: 5px;
  border-radius: 999px;
  background: linear-gradient(
    90deg,
    var(--portfolio-admin-cyan) 0,
    var(--portfolio-admin-violet) var(--project-progress),
    rgba(119, 143, 163, 0.15) var(--project-progress),
    rgba(119, 143, 163, 0.15) 100%
  );
}

.portfolio-admin-project-progress-field input[type='range']::-webkit-slider-thumb {
  width: 17px;
  height: 17px;
  margin-top: -6px;
  appearance: none;
  border: 3px solid #0b1620;
  border-radius: 50%;
  background: var(--portfolio-admin-cyan);
  box-shadow: 0 0 0 1px rgba(50, 215, 231, 0.45);
}

.portfolio-admin-project-progress-field input[type='range']::-moz-range-track {
  height: 5px;
  border-radius: 999px;
  background: rgba(119, 143, 163, 0.15);
}

.portfolio-admin-project-progress-field input[type='range']::-moz-range-progress {
  height: 5px;
  border-radius: 999px;
  background: var(--portfolio-admin-cyan);
}

.portfolio-admin-project-progress-field__number {
  display: grid;
  grid-template-columns: 1fr auto;
  align-items: center;
  min-height: 40px;
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: #0d1924;
}

.portfolio-admin-project-progress-field__number:focus-within {
  border-color: rgba(50, 215, 231, 0.48);
  box-shadow: 0 0 0 2px rgba(50, 215, 231, 0.07);
}

.portfolio-admin-project-progress-field__number input {
  min-width: 0;
  width: 100%;
  height: 38px;
  padding: 0 4px 0 11px;
  border: 0 !important;
  outline: 0 !important;
  background: transparent !important;
  color: var(--portfolio-admin-text);
  font-family: var(--font-mono), monospace;
  font-size: 11px;
  box-shadow: none !important;
}

.portfolio-admin-project-progress-field__number > span:last-child {
  padding-right: 10px;
  color: var(--portfolio-admin-text-dim);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-project-progress-field > p {
  margin: 7px 0 0;
  color: var(--portfolio-admin-text-dim);
  font-size: 10px;
}

.collection-edit:has(.portfolio-admin-project-health) .document-fields__sidebar {
  display: flex;
  flex-direction: column;
}

.collection-edit:has(.portfolio-admin-project-health)
  .document-fields__sidebar
  .portfolio-admin-project-health {
  order: -100;
  margin-top: 0;
}

.collection-edit:has(.portfolio-admin-project-health) .tabs-field__tab-button {
  box-shadow: none !important;
}

.collection-edit:has(.portfolio-admin-project-health) .tabs-field__tab-button::before,
.collection-edit:has(.portfolio-admin-project-health) .tabs-field__tab-button::after {
  display: none !important;
}

.collection-edit:has(.portfolio-admin-project-health) .doc-header {
  border-bottom-color: var(--portfolio-admin-border);
  background: linear-gradient(90deg, rgba(50, 215, 231, 0.018), transparent 52%), transparent;
}

.collection-edit:has(.portfolio-admin-project-health) .doc-controls {
  border-color: var(--portfolio-admin-border);
  background: rgba(7, 16, 25, 0.52);
  backdrop-filter: blur(14px);
}

@media (max-width: 1180px) {
  .portfolio-admin-project-command__updated {
    display: none;
  }
}

@media (max-width: 980px) {
  .portfolio-admin-project-command__version {
    display: none;
  }
}

@media (max-width: 768px) {
  .portfolio-admin-project-command__stage {
    display: none;
  }
}

/* Portfolio Admin Experience - project editor operational polish:end */

/* Portfolio Admin Experience - project rich text typography:start */

/*
 * Payload's Lexical canvas has its own content typography.
 * Keep project case-study editing aligned with the Control Room sans-serif UI,
 * while preserving monospace for code content.
 */
.collection-edit:has(.portfolio-admin-project-health)
  :is(.rich-text-lexical, .lexical-editor)
  :is([contenteditable='true'], [data-lexical-editor='true'], .ContentEditable__root) {
  font-family:
    var(--font-body, Inter),
    ui-sans-serif,
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    'Segoe UI',
    sans-serif !important;
  font-size: 15px;
  font-weight: 400;
  letter-spacing: -0.006em;
  line-height: 1.72;
}

.collection-edit:has(.portfolio-admin-project-health)
  :is(.rich-text-lexical, .lexical-editor)
  :is([contenteditable='true'], [data-lexical-editor='true'], .ContentEditable__root)
  :where(p, li, div:not([class])) {
  font-family: inherit !important;
}

.collection-edit:has(.portfolio-admin-project-health)
  :is(.rich-text-lexical, .lexical-editor)
  :is([contenteditable='true'], [data-lexical-editor='true'], .ContentEditable__root)
  :where(h1, h2, h3, h4, h5, h6) {
  font-family: inherit !important;
  font-weight: 650;
  letter-spacing: -0.025em;
  line-height: 1.25;
}

.collection-edit:has(.portfolio-admin-project-health)
  :is(.rich-text-lexical, .lexical-editor)
  :is([contenteditable='true'], [data-lexical-editor='true'], .ContentEditable__root)
  :where(blockquote) {
  font-family: inherit !important;
  font-style: normal;
}

.collection-edit:has(.portfolio-admin-project-health)
  :is(.rich-text-lexical, .lexical-editor)
  :is([contenteditable='true'], [data-lexical-editor='true'], .ContentEditable__root)
  :where(code, pre, kbd, samp) {
  font-family:
    var(--font-mono), 'SFMono-Regular', Consolas, 'Liberation Mono', monospace !important;
  letter-spacing: 0;
}

/* Fallback for Payload builds where Lexical does not expose the wrapper class. */
.collection-edit:has(.portfolio-admin-project-health)
  .field-type.rich-text
  :is([contenteditable='true'], [data-lexical-editor='true']) {
  font-family:
    var(--font-body, Inter),
    ui-sans-serif,
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    'Segoe UI',
    sans-serif !important;
}

/* Portfolio Admin Experience - project rich text typography:end */

/* Portfolio Admin Experience - blog posts list:start */

.portfolio-admin-blog-overview {
  width: 100%;
  box-sizing: border-box;
  padding: 30px clamp(28px, 4vw, 64px) 18px;
}

.portfolio-admin-blog-overview__header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 20px;
}

.portfolio-admin-blog-overview__eyebrow {
  display: block;
  margin-bottom: 8px;
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 11px;
  font-weight: 650;
  letter-spacing: 0.16em;
  line-height: 1;
  text-transform: uppercase;
}

.portfolio-admin-blog-overview__header h2 {
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(24px, 2.3vw, 34px);
  font-weight: 600;
  letter-spacing: -0.035em;
  line-height: 1.05;
}

.portfolio-admin-blog-overview__header p {
  max-width: 760px;
  margin: 10px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 14px;
  line-height: 1.55;
}

.portfolio-admin-blog-overview__metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
}

.portfolio-admin-blog-stat {
  --blog-stat-accent: var(--portfolio-admin-cyan);
  position: relative;
  min-width: 0;
  min-height: 126px;
  overflow: hidden;
  padding: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(
      135deg,
      color-mix(in srgb, var(--blog-stat-accent) 7%, transparent),
      transparent 60%
    ),
    var(--portfolio-admin-surface);
  box-shadow: inset 0 2px 0 color-mix(in srgb, var(--blog-stat-accent) 75%, transparent);
}

.portfolio-admin-blog-stat::after {
  position: absolute;
  right: -36px;
  bottom: -48px;
  width: 132px;
  height: 132px;
  border: 1px solid color-mix(in srgb, var(--blog-stat-accent) 18%, transparent);
  border-radius: 50%;
  content: '';
}

.portfolio-admin-blog-stat.is-purple {
  --blog-stat-accent: var(--portfolio-admin-purple);
}

.portfolio-admin-blog-stat.is-green {
  --blog-stat-accent: var(--portfolio-admin-green);
}

.portfolio-admin-blog-stat.is-amber {
  --blog-stat-accent: var(--portfolio-admin-amber);
}

.portfolio-admin-blog-stat__topline {
  position: relative;
  z-index: 1;
  display: flex;
  justify-content: space-between;
  gap: 12px;
}

.portfolio-admin-blog-stat__topline span {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.11em;
  text-transform: uppercase;
}

.portfolio-admin-blog-stat__topline code {
  color: color-mix(in srgb, var(--blog-stat-accent) 78%, white);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-blog-stat > strong {
  position: relative;
  z-index: 1;
  display: block;
  margin-top: 15px;
  color: var(--blog-stat-accent);
  font-family: var(--font-mono), monospace;
  font-size: 29px;
  letter-spacing: -0.05em;
  line-height: 1;
}

.portfolio-admin-blog-stat > p {
  position: relative;
  z-index: 1;
  max-width: 84%;
  margin: 8px 0 0;
  color: var(--portfolio-admin-text-dim, var(--portfolio-admin-text-muted));
  font-size: 11px;
  line-height: 1.45;
}

.portfolio-admin-blog-stat__signal {
  position: absolute;
  right: 16px;
  bottom: 15px;
  z-index: 1;
  display: flex;
  align-items: flex-end;
  gap: 3px;
}

.portfolio-admin-blog-stat__signal i {
  width: 2px;
  border-radius: 999px;
  background: var(--blog-stat-accent);
}

.portfolio-admin-blog-stat__signal i:nth-child(1) {
  height: 7px;
}

.portfolio-admin-blog-stat__signal i:nth-child(2) {
  height: 14px;
}

.portfolio-admin-blog-stat__signal i:nth-child(3) {
  height: 21px;
}

.template-default__wrap:has(.portfolio-admin-blog-overview) .list-header__title-actions,
.template-default__wrap:has(.portfolio-admin-blog-overview)
  .list-header
  a[href*='/collections/blog-posts/create'] {
  display: none !important;
}

.portfolio-admin-blog-cell {
  display: flex;
  min-width: 270px;
  align-items: center;
  gap: 12px;
  color: inherit;
  text-decoration: none;
}

.portfolio-admin-blog-cell__preview {
  flex: 0 0 48px;
  display: grid;
  width: 48px;
  height: 38px;
  place-items: center;
  overflow: hidden;
  border: 1px solid rgba(168, 103, 255, 0.22);
  border-radius: 8px;
  background: linear-gradient(135deg, rgba(168, 103, 255, 0.16), rgba(50, 215, 231, 0.1)), #0d1924;
  color: var(--portfolio-admin-purple);
  background-position: center;
  background-size: cover;
  font-family: var(--font-mono), monospace;
  font-size: 10px;
  font-weight: 700;
}

.portfolio-admin-blog-cell__preview.has-image {
  color: transparent;
}

.portfolio-admin-blog-cell__copy {
  display: flex;
  min-width: 0;
  flex: 1;
  flex-direction: column;
  gap: 5px;
}

.portfolio-admin-blog-cell__copy strong {
  overflow: hidden;
  color: var(--portfolio-admin-text);
  font-size: 13px;
  font-weight: 600;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-blog-cell__copy > span {
  display: flex;
  min-width: 0;
  align-items: center;
  gap: 7px;
}

.portfolio-admin-blog-cell__copy code,
.portfolio-admin-blog-cell__copy small {
  overflow: hidden;
  color: var(--portfolio-admin-text-dim, var(--portfolio-admin-text-muted));
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-blog-cell__copy i {
  flex: 0 0 3px;
  width: 3px;
  height: 3px;
  border-radius: 50%;
  background: var(--portfolio-admin-border-strong);
}

.portfolio-admin-blog-cell__open {
  flex: 0 0 27px;
  display: grid;
  width: 27px;
  height: 27px;
  margin-left: auto;
  place-items: center;
  border: 1px solid transparent;
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  opacity: 0;
  transform: translateX(-4px);
  transition: 130ms ease;
}

.portfolio-admin-blog-cell:hover .portfolio-admin-blog-cell__copy strong,
.portfolio-admin-blog-cell:focus-visible .portfolio-admin-blog-cell__copy strong {
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-blog-cell:hover .portfolio-admin-blog-cell__open,
.portfolio-admin-blog-cell:focus-visible .portfolio-admin-blog-cell__open {
  border-color: rgba(168, 103, 255, 0.24);
  background: rgba(168, 103, 255, 0.06);
  color: var(--portfolio-admin-purple);
  opacity: 1;
  transform: none;
}

.portfolio-admin-blog-status {
  --blog-status-color: var(--portfolio-admin-amber);
  display: inline-flex;
  min-height: 26px;
  align-items: center;
  gap: 7px;
  padding: 0 10px;
  border: 1px solid color-mix(in srgb, var(--blog-status-color) 28%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--blog-status-color) 9%, transparent);
  color: color-mix(in srgb, var(--blog-status-color) 82%, white);
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.055em;
  text-transform: uppercase;
}

.portfolio-admin-blog-status i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--blog-status-color);
}

.portfolio-admin-blog-status.is-published {
  --blog-status-color: var(--portfolio-admin-green);
}

.portfolio-admin-blog-status.is-archived {
  --blog-status-color: #71808c;
}

.portfolio-admin-blog-category {
  display: inline-flex;
  width: max-content;
  min-height: 26px;
  align-items: center;
  padding: 0 10px;
  border: 1px solid rgba(168, 103, 255, 0.22);
  border-radius: 8px;
  background: rgba(168, 103, 255, 0.055);
  color: #d4b6ff;
  font-size: 10px;
  font-weight: 600;
}

.portfolio-admin-blog-category.is-empty {
  border-color: var(--portfolio-admin-border);
  background: transparent;
  color: var(--portfolio-admin-text-muted);
}

.portfolio-admin-blog-date {
  display: inline-flex;
  min-width: 138px;
  align-items: center;
  gap: 9px;
}

.portfolio-admin-blog-date > i {
  flex: 0 0 7px;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--portfolio-admin-amber);
}

.portfolio-admin-blog-date.is-published > i {
  background: var(--portfolio-admin-green);
}

.portfolio-admin-blog-date.is-archived > i {
  background: #71808c;
}

.portfolio-admin-blog-date > span {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.portfolio-admin-blog-date strong {
  color: var(--portfolio-admin-text);
  font-size: 11px;
}

.portfolio-admin-blog-date small {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

.template-default__wrap:has(.portfolio-admin-blog-overview) .collection-list .table {
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background: var(--portfolio-admin-surface);
}

.template-default__wrap:has(.portfolio-admin-blog-overview) .collection-list .table tbody tr:hover {
  background: rgba(168, 103, 255, 0.025);
  box-shadow: inset 2px 0 0 rgba(168, 103, 255, 0.46);
}

@media (max-width: 1120px) {
  .portfolio-admin-blog-overview__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 768px) {
  .portfolio-admin-blog-overview {
    padding: 22px 18px 14px;
  }

  .portfolio-admin-blog-overview__header {
    align-items: stretch;
    flex-direction: column;
  }

  .portfolio-admin-blog-overview__metrics {
    grid-template-columns: 1fr;
  }
}

/* Portfolio Admin Experience - blog posts list:end */

/* Portfolio Admin Experience - blog post editor:start */

.portfolio-admin-article-readiness {
  --article-readiness-score: 0%;
  overflow: hidden;
  margin: 0 0 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(145deg, rgba(168, 103, 255, 0.055), transparent 48%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-article-readiness__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 17px 17px 13px;
}

.portfolio-admin-article-readiness__header > div {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.portfolio-admin-article-readiness__header span:first-child {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-article-readiness__header strong {
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 27px;
  font-weight: 650;
  letter-spacing: -0.05em;
  line-height: 1;
}

.portfolio-admin-article-readiness__meter {
  display: grid;
  width: 44px;
  height: 44px;
  place-items: center;
  border-radius: 50%;
  background:
    radial-gradient(circle at center, var(--portfolio-admin-surface) 57%, transparent 59%),
    conic-gradient(
      var(--portfolio-admin-purple) var(--article-readiness-score),
      rgba(118, 143, 163, 0.14) 0
    );
}

.portfolio-admin-article-readiness__meter::after {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--portfolio-admin-purple);
  box-shadow: 0 0 10px rgba(168, 103, 255, 0.4);
  content: '';
}

.portfolio-admin-article-readiness__reading {
  display: grid;
  grid-template-columns: 1fr 1fr;
  margin: 0 16px 14px;
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 9px;
  background: rgba(255, 255, 255, 0.014);
}

.portfolio-admin-article-readiness__reading > span {
  display: flex;
  min-height: 48px;
  align-items: baseline;
  justify-content: center;
  gap: 6px;
  padding: 0 8px;
}

.portfolio-admin-article-readiness__reading > span + span {
  border-left: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-article-readiness__reading strong {
  color: var(--portfolio-admin-text);
  font-family: var(--font-mono), monospace;
  font-size: 15px;
}

.portfolio-admin-article-readiness__reading small {
  color: var(--portfolio-admin-text-dim);
  font-size: 9px;
}

.portfolio-admin-article-readiness__summary {
  margin: 0;
  padding: 0 17px 15px;
  color: var(--portfolio-admin-text-dim);
  font-size: 11px;
  line-height: 1.5;
}

.portfolio-admin-article-readiness__checks {
  border-top: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-article-readiness__check {
  display: flex;
  min-height: 50px;
  align-items: center;
  gap: 10px;
  padding: 8px 16px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-article-readiness__check > span {
  flex: 0 0 25px;
  display: grid;
  width: 25px;
  height: 25px;
  place-items: center;
  border: 1px solid rgba(122, 145, 164, 0.18);
  border-radius: 7px;
  color: #6f8495;
  background: rgba(122, 145, 164, 0.035);
}

.portfolio-admin-article-readiness__check > span svg {
  width: 15px;
  height: 15px;
}

.portfolio-admin-article-readiness__check.is-ready > span {
  border-color: rgba(69, 214, 139, 0.24);
  color: var(--portfolio-admin-green);
  background: rgba(69, 214, 139, 0.055);
}

.portfolio-admin-article-readiness__check > div {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 3px;
}

.portfolio-admin-article-readiness__check strong {
  color: var(--portfolio-admin-text);
  font-size: 11px;
  font-weight: 600;
}

.portfolio-admin-article-readiness__check small {
  color: var(--portfolio-admin-text-dim);
  font-size: 9px;
  line-height: 1.3;
}

.portfolio-admin-article-readiness__preview {
  display: flex;
  min-height: 42px;
  align-items: center;
  justify-content: space-between;
  padding: 0 16px;
  color: #d4b6ff;
  font-size: 10px;
  font-weight: 600;
  text-decoration: none;
}

.portfolio-admin-article-readiness__preview:hover {
  color: var(--portfolio-admin-purple);
  background: rgba(168, 103, 255, 0.04);
}

.portfolio-admin-article-readiness__preview.is-disabled {
  color: var(--portfolio-admin-text-dim);
  cursor: default;
}

.portfolio-admin-article-command {
  display: flex;
  min-width: 0;
  align-items: center;
  gap: 8px;
  margin-right: 6px;
}

.portfolio-admin-article-command__status {
  --article-status: var(--portfolio-admin-amber);
  display: inline-flex;
  min-height: 32px;
  align-items: center;
  gap: 7px;
  padding: 0 10px;
  border: 1px solid color-mix(in srgb, var(--article-status) 28%, transparent);
  border-radius: 8px;
  background: color-mix(in srgb, var(--article-status) 8%, transparent);
  color: color-mix(in srgb, var(--article-status) 84%, white);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.07em;
  text-transform: uppercase;
}

.portfolio-admin-article-command__status i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--article-status);
}

.portfolio-admin-article-command__status.is-published {
  --article-status: var(--portfolio-admin-green);
}

.portfolio-admin-article-command__status.is-archived {
  --article-status: #71808c;
}

.portfolio-admin-article-command__reading {
  display: inline-flex;
  min-height: 32px;
  align-items: baseline;
  gap: 5px;
  padding: 0 10px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
}

.portfolio-admin-article-command__reading strong {
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 11px;
}

.portfolio-admin-article-command__reading small,
.portfolio-admin-article-command__updated {
  color: var(--portfolio-admin-text-dim);
  font-size: 9px;
  white-space: nowrap;
}

.portfolio-admin-article-command__updated time {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
}

.portfolio-admin-article-command__preview {
  display: inline-flex;
  min-height: 32px;
  align-items: center;
  gap: 6px;
  padding: 0 11px;
  border: 1px solid rgba(168, 103, 255, 0.25);
  border-radius: 8px;
  background: rgba(168, 103, 255, 0.045);
  color: #d4b6ff;
  font-size: 10px;
  font-weight: 650;
  text-decoration: none;
}

.portfolio-admin-article-command__preview:hover {
  border-color: rgba(168, 103, 255, 0.48);
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-article-command__preview.is-disabled {
  border-color: var(--portfolio-admin-border);
  color: var(--portfolio-admin-text-dim);
  cursor: not-allowed;
}

.collection-edit:has(.portfolio-admin-article-readiness) .document-fields__sidebar {
  display: flex;
  flex-direction: column;
  border-left-color: var(--portfolio-admin-border);
  background:
    linear-gradient(180deg, rgba(168, 103, 255, 0.02), transparent 220px), rgba(7, 16, 25, 0.32);
}

.collection-edit:has(.portfolio-admin-article-readiness)
  .document-fields__sidebar
  .portfolio-admin-article-readiness {
  order: -100;
}

.collection-edit:has(.portfolio-admin-article-readiness) .tabs-field__tabs {
  position: sticky;
  top: calc(var(--portfolio-admin-topbar-height) + 8px);
  z-index: 12;
  display: flex;
  gap: 4px;
  overflow-x: auto;
  margin-bottom: 24px;
  padding: 5px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background: rgba(7, 16, 25, 0.94);
  backdrop-filter: blur(16px);
}

.collection-edit:has(.portfolio-admin-article-readiness) .tabs-field__tab-button {
  min-height: 36px;
  margin: 0;
  padding: 0 13px;
  border: 1px solid transparent;
  border-radius: 7px;
  color: var(--portfolio-admin-text-dim);
  font-size: 11px;
  font-weight: 600;
  white-space: nowrap;
  box-shadow: none !important;
}

.collection-edit:has(.portfolio-admin-article-readiness) .tabs-field__tab-button::before,
.collection-edit:has(.portfolio-admin-article-readiness) .tabs-field__tab-button::after {
  display: none !important;
}

.collection-edit:has(.portfolio-admin-article-readiness) .tabs-field__tab-button--active {
  border-color: rgba(168, 103, 255, 0.28);
  color: var(--portfolio-admin-purple);
  background: rgba(168, 103, 255, 0.075);
}

.collection-edit:has(.portfolio-admin-article-readiness)
  :is(.rich-text-lexical, .lexical-editor)
  :is([contenteditable='true'], [data-lexical-editor='true'], .ContentEditable__root) {
  font-family:
    var(--font-body, Inter),
    ui-sans-serif,
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    'Segoe UI',
    sans-serif !important;
  font-size: 15px;
  line-height: 1.72;
}

.collection-edit:has(.portfolio-admin-article-readiness)
  :is(.rich-text-lexical, .lexical-editor)
  :is([contenteditable='true'], [data-lexical-editor='true'], .ContentEditable__root)
  :where(p, li, div:not([class]), h1, h2, h3, h4, h5, h6, blockquote) {
  font-family: inherit !important;
}

.collection-edit:has(.portfolio-admin-article-readiness)
  :is(.rich-text-lexical, .lexical-editor)
  :is([contenteditable='true'], [data-lexical-editor='true'], .ContentEditable__root)
  :where(code, pre, kbd, samp) {
  font-family: var(--font-mono), monospace !important;
}

@media (max-width: 1180px) {
  .portfolio-admin-article-command__updated {
    display: none;
  }
}

@media (max-width: 980px) {
  .portfolio-admin-article-command__reading {
    display: none;
  }
}

@media (max-width: 1024px) {
  .collection-edit:has(.portfolio-admin-article-readiness) .tabs-field__tabs {
    position: static;
  }
}

/* Portfolio Admin Experience - blog post editor:end */

/* Portfolio Admin Experience - moderation and inbox lists:start */

/* Shared page spacing */
.portfolio-admin-moderation-overview,
.portfolio-admin-inbox-overview {
  width: 100%;
  box-sizing: border-box;
  padding: 30px clamp(28px, 4vw, 64px) 18px;
}

.portfolio-admin-moderation-overview__header,
.portfolio-admin-inbox-overview__header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 20px;
}

.portfolio-admin-moderation-overview__eyebrow,
.portfolio-admin-inbox-overview__eyebrow {
  display: block;
  margin-bottom: 8px;
  font-family: var(--font-mono), monospace;
  font-size: 11px;
  font-weight: 650;
  letter-spacing: 0.16em;
  line-height: 1;
  text-transform: uppercase;
}

.portfolio-admin-moderation-overview__eyebrow {
  color: var(--portfolio-admin-green);
}

.portfolio-admin-inbox-overview__eyebrow {
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-moderation-overview__header h2,
.portfolio-admin-inbox-overview__header h2 {
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(24px, 2.3vw, 34px);
  font-weight: 600;
  letter-spacing: -0.035em;
  line-height: 1.05;
}

.portfolio-admin-moderation-overview__header p,
.portfolio-admin-inbox-overview__header p {
  max-width: 760px;
  margin: 10px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 14px;
  line-height: 1.55;
}

.portfolio-admin-moderation-overview__metrics,
.portfolio-admin-inbox-overview__metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
}

/* Metrics */
.portfolio-admin-moderation-stat,
.portfolio-admin-inbox-stat {
  --metric-color: var(--portfolio-admin-cyan);
  position: relative;
  min-width: 0;
  min-height: 122px;
  overflow: hidden;
  padding: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(
      135deg,
      color-mix(in srgb, var(--metric-color) 7%, transparent),
      transparent 60%
    ),
    var(--portfolio-admin-surface);
  box-shadow: inset 0 2px 0 color-mix(in srgb, var(--metric-color) 72%, transparent);
}

.portfolio-admin-moderation-stat.is-purple,
.portfolio-admin-inbox-stat.is-purple {
  --metric-color: var(--portfolio-admin-purple);
}

.portfolio-admin-moderation-stat.is-green,
.portfolio-admin-inbox-stat.is-green {
  --metric-color: var(--portfolio-admin-green);
}

.portfolio-admin-moderation-stat.is-red {
  --metric-color: #ff6b75;
}

.portfolio-admin-inbox-stat.is-slate {
  --metric-color: #71808c;
}

.portfolio-admin-moderation-stat > div,
.portfolio-admin-inbox-stat > div {
  position: relative;
  z-index: 1;
  display: flex;
  justify-content: space-between;
  gap: 12px;
}

.portfolio-admin-moderation-stat > div span,
.portfolio-admin-inbox-stat > div span {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.11em;
  text-transform: uppercase;
}

.portfolio-admin-moderation-stat > div code,
.portfolio-admin-inbox-stat > div code {
  color: color-mix(in srgb, var(--metric-color) 78%, white);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-moderation-stat > strong,
.portfolio-admin-inbox-stat > strong {
  position: relative;
  z-index: 1;
  display: block;
  margin-top: 15px;
  color: var(--metric-color);
  font-family: var(--font-mono), monospace;
  font-size: 29px;
  letter-spacing: -0.05em;
  line-height: 1;
}

.portfolio-admin-moderation-stat > p,
.portfolio-admin-inbox-stat > p {
  position: relative;
  z-index: 1;
  max-width: 82%;
  margin: 8px 0 0;
  color: var(--portfolio-admin-text-dim);
  font-size: 11px;
  line-height: 1.4;
}

.portfolio-admin-moderation-stat > i,
.portfolio-admin-inbox-stat > i {
  position: absolute;
  right: -34px;
  bottom: -52px;
  width: 128px;
  height: 128px;
  border: 1px solid color-mix(in srgb, var(--metric-color) 18%, transparent);
  border-radius: 50%;
}

/* Hide native duplicate create action on both tailored list pages. */
.template-default__wrap:has(.portfolio-admin-moderation-overview) .list-header__title-actions,
.template-default__wrap:has(.portfolio-admin-moderation-overview)
  .list-header
  a[href*='/collections/testimonials/create'],
.template-default__wrap:has(.portfolio-admin-inbox-overview) .list-header__title-actions,
.template-default__wrap:has(.portfolio-admin-inbox-overview)
  .list-header
  a[href*='/collections/contact-messages/create'] {
  display: none !important;
}

/* Testimonial identity */
.portfolio-admin-testimonial-cell {
  display: flex;
  min-width: 290px;
  align-items: center;
  gap: 12px;
  color: inherit;
  text-decoration: none;
}

.portfolio-admin-testimonial-cell__avatar {
  flex: 0 0 42px;
  display: grid;
  width: 42px;
  height: 42px;
  place-items: center;
  border: 1px solid rgba(69, 214, 139, 0.22);
  border-radius: 10px;
  background: linear-gradient(135deg, rgba(69, 214, 139, 0.12), rgba(50, 215, 231, 0.08)), #0d1924;
  color: var(--portfolio-admin-green);
  background-position: center;
  background-size: cover;
  font-family: var(--font-mono), monospace;
  font-size: 10px;
  font-weight: 700;
}

.portfolio-admin-testimonial-cell__avatar.has-image {
  color: transparent;
}

.portfolio-admin-testimonial-cell__copy {
  display: flex;
  min-width: 0;
  flex: 1;
  flex-direction: column;
  gap: 3px;
}

.portfolio-admin-testimonial-cell__copy strong {
  overflow: hidden;
  color: var(--portfolio-admin-text);
  font-size: 13px;
  font-weight: 600;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-testimonial-cell__copy small {
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
}

.portfolio-admin-testimonial-cell__copy q {
  overflow: hidden;
  max-width: 420px;
  color: var(--portfolio-admin-text-dim);
  font-size: 10px;
  line-height: 1.35;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-testimonial-cell__open,
.portfolio-admin-message-cell__open {
  flex: 0 0 27px;
  display: grid;
  width: 27px;
  height: 27px;
  margin-left: auto;
  place-items: center;
  border: 1px solid transparent;
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  opacity: 0;
  transform: translateX(-4px);
  transition: 130ms ease;
}

.portfolio-admin-testimonial-cell:hover .portfolio-admin-testimonial-cell__copy strong,
.portfolio-admin-testimonial-cell:focus-visible .portfolio-admin-testimonial-cell__copy strong {
  color: var(--portfolio-admin-green);
}

.portfolio-admin-testimonial-cell:hover .portfolio-admin-testimonial-cell__open,
.portfolio-admin-testimonial-cell:focus-visible .portfolio-admin-testimonial-cell__open,
.portfolio-admin-message-cell:hover .portfolio-admin-message-cell__open,
.portfolio-admin-message-cell:focus-visible .portfolio-admin-message-cell__open {
  border-color: rgba(50, 215, 231, 0.22);
  background: rgba(50, 215, 231, 0.05);
  color: var(--portfolio-admin-cyan);
  opacity: 1;
  transform: none;
}

/* Shared status/source/date */
.portfolio-admin-moderation-status,
.portfolio-admin-message-status {
  --status-color: var(--portfolio-admin-purple);
  display: inline-flex;
  min-height: 26px;
  align-items: center;
  gap: 7px;
  padding: 0 10px;
  border: 1px solid color-mix(in srgb, var(--status-color) 28%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--status-color) 9%, transparent);
  color: color-mix(in srgb, var(--status-color) 82%, white);
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.055em;
  text-transform: uppercase;
}

.portfolio-admin-moderation-status i,
.portfolio-admin-message-status i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--status-color);
}

.portfolio-admin-moderation-status.is-approved,
.portfolio-admin-message-status.is-read {
  --status-color: var(--portfolio-admin-green);
}

.portfolio-admin-moderation-status.is-rejected {
  --status-color: #ff6b75;
}

.portfolio-admin-message-status.is-archived {
  --status-color: #71808c;
}

.portfolio-admin-source-badge {
  display: inline-flex;
  width: max-content;
  min-height: 26px;
  align-items: center;
  padding: 0 10px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  text-transform: capitalize;
}

.portfolio-admin-testimonial-rating {
  display: inline-flex;
  align-items: center;
  gap: 9px;
}

.portfolio-admin-testimonial-rating > span {
  display: flex;
  gap: 2px;
}

.portfolio-admin-testimonial-rating i {
  color: rgba(245, 179, 66, 0.2);
  font-size: 12px;
  font-style: normal;
}

.portfolio-admin-testimonial-rating i.is-active {
  color: var(--portfolio-admin-amber);
}

.portfolio-admin-testimonial-rating code {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

.portfolio-admin-testimonial-rating.is-empty {
  color: var(--portfolio-admin-text-dim);
  font-size: 10px;
}

.portfolio-admin-moderation-date {
  display: inline-flex;
  min-width: 130px;
  align-items: center;
  gap: 9px;
}

.portfolio-admin-moderation-date > i {
  flex: 0 0 7px;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--portfolio-admin-purple);
}

.portfolio-admin-moderation-date.is-approved > i {
  background: var(--portfolio-admin-green);
}

.portfolio-admin-moderation-date.is-rejected > i {
  background: #ff6b75;
}

.portfolio-admin-moderation-date > span {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.portfolio-admin-moderation-date strong {
  color: var(--portfolio-admin-text);
  font-size: 11px;
}

.portfolio-admin-moderation-date small {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

/* Inbox identity */
.portfolio-admin-message-cell {
  display: flex;
  min-width: 360px;
  align-items: center;
  gap: 12px;
  color: inherit;
  text-decoration: none;
}

.portfolio-admin-message-cell__avatar {
  position: relative;
  flex: 0 0 42px;
  display: grid;
  width: 42px;
  height: 42px;
  place-items: center;
  border: 1px solid rgba(168, 103, 255, 0.22);
  border-radius: 50%;
  background: rgba(168, 103, 255, 0.08);
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
  font-weight: 700;
}

.portfolio-admin-message-cell__avatar i {
  position: absolute;
  top: -2px;
  right: -2px;
  width: 8px;
  height: 8px;
  border: 2px solid #08121b;
  border-radius: 50%;
  background: var(--portfolio-admin-purple);
  box-shadow: 0 0 8px rgba(168, 103, 255, 0.45);
}

.portfolio-admin-message-cell__copy {
  display: flex;
  min-width: 0;
  flex: 1;
  flex-direction: column;
  gap: 5px;
}

.portfolio-admin-message-cell__copy > span {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 3px;
}

.portfolio-admin-message-cell__copy strong {
  overflow: hidden;
  color: var(--portfolio-admin-text);
  font-size: 13px;
  font-weight: 600;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-message-cell.is-new .portfolio-admin-message-cell__copy strong {
  color: #efe8ff;
}

.portfolio-admin-message-cell__copy small {
  overflow: hidden;
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-message-cell__copy p {
  overflow: hidden;
  max-width: 520px;
  margin: 0;
  color: var(--portfolio-admin-text-dim);
  font-size: 10px;
  line-height: 1.35;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-message-cell:hover .portfolio-admin-message-cell__copy strong,
.portfolio-admin-message-cell:focus-visible .portfolio-admin-message-cell__copy strong {
  color: var(--portfolio-admin-purple);
}

/* Tailored table surfaces */
.template-default__wrap:has(.portfolio-admin-moderation-overview) .collection-list .table,
.template-default__wrap:has(.portfolio-admin-inbox-overview) .collection-list .table {
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background: var(--portfolio-admin-surface);
}

.template-default__wrap:has(.portfolio-admin-moderation-overview)
  .collection-list
  .table
  tbody
  tr:hover {
  background: rgba(69, 214, 139, 0.022);
  box-shadow: inset 2px 0 0 rgba(69, 214, 139, 0.42);
}

.template-default__wrap:has(.portfolio-admin-inbox-overview)
  .collection-list
  .table
  tbody
  tr:hover {
  background: rgba(168, 103, 255, 0.024);
  box-shadow: inset 2px 0 0 rgba(168, 103, 255, 0.44);
}

@media (max-width: 1120px) {
  .portfolio-admin-moderation-overview__metrics,
  .portfolio-admin-inbox-overview__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 768px) {
  .portfolio-admin-moderation-overview,
  .portfolio-admin-inbox-overview {
    padding: 22px 18px 14px;
  }

  .portfolio-admin-moderation-overview__header,
  .portfolio-admin-inbox-overview__header {
    align-items: stretch;
    flex-direction: column;
  }

  .portfolio-admin-moderation-overview__metrics,
  .portfolio-admin-inbox-overview__metrics {
    grid-template-columns: 1fr;
  }
}

/* Portfolio Admin Experience - moderation and inbox lists:end */

/* Portfolio Admin Experience - moderation and inbox editors:start */

/* Keep long source values compact in both list views. */
.portfolio-admin-source-badge {
  max-width: 180px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Testimonial moderation workspace */
.portfolio-admin-testimonial-moderation {
  --moderation-color: var(--portfolio-admin-purple);
  overflow: hidden;
  margin: 0 0 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(
      145deg,
      color-mix(in srgb, var(--moderation-color) 7%, transparent),
      transparent 55%
    ),
    var(--portfolio-admin-surface);
}

.portfolio-admin-testimonial-moderation.is-approved {
  --moderation-color: var(--portfolio-admin-green);
}

.portfolio-admin-testimonial-moderation.is-rejected {
  --moderation-color: #ff6b75;
}

.portfolio-admin-testimonial-moderation__header,
.portfolio-admin-message-operations > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: 17px;
}

.portfolio-admin-testimonial-moderation__header > div,
.portfolio-admin-message-operations > header > div {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.portfolio-admin-testimonial-moderation__header span,
.portfolio-admin-message-operations > header span {
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-testimonial-moderation__header strong,
.portfolio-admin-message-operations > header strong {
  color: var(--moderation-color);
  font-family: var(--font-mono), monospace;
  font-size: 18px;
  letter-spacing: -0.025em;
  text-transform: capitalize;
}

.portfolio-admin-testimonial-moderation__header > i,
.portfolio-admin-message-operations > header > i {
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: var(--moderation-color);
  box-shadow: 0 0 14px color-mix(in srgb, var(--moderation-color) 52%, transparent);
}

.portfolio-admin-testimonial-moderation__meta {
  display: grid;
  grid-template-columns: 1fr 1fr;
  border-top: 1px solid var(--portfolio-admin-border);
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-testimonial-moderation__meta > span {
  display: flex;
  min-width: 0;
  min-height: 54px;
  flex-direction: column;
  justify-content: center;
  gap: 4px;
  padding: 0 16px;
}

.portfolio-admin-testimonial-moderation__meta > span + span {
  border-left: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-testimonial-moderation__meta small {
  color: var(--portfolio-admin-text-dim);
  font-size: 8px;
  letter-spacing: 0.09em;
  text-transform: uppercase;
}

.portfolio-admin-testimonial-moderation__meta strong {
  overflow: hidden;
  color: var(--portfolio-admin-text);
  font-size: 10px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-testimonial-moderation__actions,
.portfolio-admin-message-operations__actions {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 7px;
  padding: 14px;
}

.portfolio-admin-testimonial-moderation__actions button,
.portfolio-admin-message-operations__actions button {
  min-height: 34px;
  padding: 0 8px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  font-weight: 650;
  cursor: pointer;
}

.portfolio-admin-testimonial-moderation__actions button:hover,
.portfolio-admin-message-operations__actions button:hover {
  border-color: rgba(168, 103, 255, 0.32);
  color: var(--portfolio-admin-text);
}

.portfolio-admin-testimonial-moderation__actions button.is-active {
  border-color: rgba(168, 103, 255, 0.42);
  background: rgba(168, 103, 255, 0.09);
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-testimonial-moderation__actions button.is-active.is-approve {
  border-color: rgba(69, 214, 139, 0.42);
  background: rgba(69, 214, 139, 0.08);
  color: var(--portfolio-admin-green);
}

.portfolio-admin-testimonial-moderation__actions button.is-active.is-reject {
  border-color: rgba(255, 107, 117, 0.42);
  background: rgba(255, 107, 117, 0.08);
  color: #ff7d86;
}

.portfolio-admin-testimonial-moderation > p,
.portfolio-admin-message-operations > p {
  margin: 0;
  padding: 0 15px 15px;
  color: var(--portfolio-admin-text-dim);
  font-size: 9px;
  line-height: 1.45;
}

.portfolio-admin-testimonial-command,
.portfolio-admin-message-command {
  display: flex;
  min-width: 0;
  align-items: center;
  gap: 8px;
  margin-right: 6px;
}

.portfolio-admin-testimonial-command__status,
.portfolio-admin-message-command__status {
  --command-color: var(--portfolio-admin-purple);
  display: inline-flex;
  min-height: 32px;
  align-items: center;
  gap: 7px;
  padding: 0 10px;
  border: 1px solid color-mix(in srgb, var(--command-color) 30%, transparent);
  border-radius: 8px;
  background: color-mix(in srgb, var(--command-color) 8%, transparent);
  color: color-mix(in srgb, var(--command-color) 82%, white);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  white-space: nowrap;
}

.portfolio-admin-testimonial-command__status i,
.portfolio-admin-message-command__status i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--command-color);
}

.portfolio-admin-testimonial-command__status.is-approved,
.portfolio-admin-message-command__status.is-read {
  --command-color: var(--portfolio-admin-green);
}

.portfolio-admin-testimonial-command__status.is-rejected {
  --command-color: #ff6b75;
}

.portfolio-admin-message-command__status.is-archived {
  --command-color: #71808c;
}

.portfolio-admin-testimonial-command__rating {
  display: inline-flex;
  min-height: 32px;
  align-items: baseline;
  gap: 3px;
  padding: 0 10px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
}

.portfolio-admin-testimonial-command__rating strong {
  color: var(--portfolio-admin-amber);
  font-family: var(--font-mono), monospace;
  font-size: 11px;
}

.portfolio-admin-testimonial-command__rating small {
  color: var(--portfolio-admin-text-dim);
  font-size: 8px;
}

.portfolio-admin-testimonial-command__source,
.portfolio-admin-message-command__source {
  max-width: 150px;
  overflow: hidden;
  color: var(--portfolio-admin-text-dim);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Testimonial tabs */
.collection-edit:has(.portfolio-admin-testimonial-moderation) .document-fields__sidebar,
.collection-edit:has(.portfolio-admin-message-operations) .document-fields__sidebar {
  display: flex;
  flex-direction: column;
  border-left-color: var(--portfolio-admin-border);
  background:
    linear-gradient(180deg, rgba(69, 214, 139, 0.018), transparent 220px), rgba(7, 16, 25, 0.32);
}

.collection-edit:has(.portfolio-admin-testimonial-moderation)
  .document-fields__sidebar
  .portfolio-admin-testimonial-moderation,
.collection-edit:has(.portfolio-admin-message-operations)
  .document-fields__sidebar
  .portfolio-admin-message-operations {
  order: -100;
}

.collection-edit:has(.portfolio-admin-testimonial-moderation) .tabs-field__tabs {
  display: flex;
  gap: 4px;
  margin-bottom: 24px;
  padding: 5px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background: rgba(7, 16, 25, 0.92);
}

.collection-edit:has(.portfolio-admin-testimonial-moderation) .tabs-field__tab-button {
  min-height: 36px;
  margin: 0;
  padding: 0 13px;
  border: 1px solid transparent;
  border-radius: 7px;
  color: var(--portfolio-admin-text-dim);
  font-size: 11px;
  font-weight: 600;
  box-shadow: none !important;
}

.collection-edit:has(.portfolio-admin-testimonial-moderation) .tabs-field__tab-button::before,
.collection-edit:has(.portfolio-admin-testimonial-moderation) .tabs-field__tab-button::after {
  display: none !important;
}

.collection-edit:has(.portfolio-admin-testimonial-moderation) .tabs-field__tab-button--active {
  border-color: rgba(69, 214, 139, 0.28);
  background: rgba(69, 214, 139, 0.065);
  color: var(--portfolio-admin-green);
}

/* Contact Message Viewer */
.portfolio-admin-message-viewer {
  --message-color: var(--portfolio-admin-purple);
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 14px;
  background:
    linear-gradient(
      145deg,
      color-mix(in srgb, var(--message-color) 5%, transparent),
      transparent 48%
    ),
    var(--portfolio-admin-surface);
}

.portfolio-admin-message-viewer.is-read {
  --message-color: var(--portfolio-admin-green);
}

.portfolio-admin-message-viewer.is-archived {
  --message-color: #71808c;
}

.portfolio-admin-message-viewer__sender {
  display: flex;
  align-items: center;
  gap: 13px;
  padding: 18px 20px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-message-viewer__avatar {
  position: relative;
  flex: 0 0 44px;
  display: grid;
  width: 44px;
  height: 44px;
  place-items: center;
  border: 1px solid color-mix(in srgb, var(--message-color) 28%, transparent);
  border-radius: 50%;
  background: color-mix(in srgb, var(--message-color) 9%, transparent);
  color: var(--message-color);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
  font-weight: 700;
}

.portfolio-admin-message-viewer__avatar i {
  position: absolute;
  top: -2px;
  right: -2px;
  width: 8px;
  height: 8px;
  border: 2px solid #08121b;
  border-radius: 50%;
  background: var(--message-color);
  box-shadow: 0 0 9px color-mix(in srgb, var(--message-color) 48%, transparent);
}

.portfolio-admin-message-viewer__sender > div {
  display: flex;
  min-width: 0;
  flex: 1;
  flex-direction: column;
  gap: 3px;
}

.portfolio-admin-message-viewer__sender > div > span,
.portfolio-admin-message-viewer__content > span {
  color: var(--portfolio-admin-text-dim);
  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.11em;
  text-transform: uppercase;
}

.portfolio-admin-message-viewer__sender strong {
  color: var(--portfolio-admin-text);
  font-size: 13px;
}

.portfolio-admin-message-viewer__sender a {
  width: max-content;
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  text-decoration: none;
}

.portfolio-admin-message-viewer__sender a:hover {
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-message-viewer__source {
  max-width: 180px;
  overflow: hidden;
  padding: 7px 10px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  text-overflow: ellipsis;
  text-transform: capitalize;
  white-space: nowrap;
}

.portfolio-admin-message-viewer__content {
  padding: clamp(24px, 4vw, 44px);
}

.portfolio-admin-message-viewer__content h2 {
  margin: 8px 0 22px;
  color: var(--portfolio-admin-text);
  font-size: clamp(22px, 2.4vw, 32px);
  font-weight: 600;
  letter-spacing: -0.035em;
  line-height: 1.15;
}

.portfolio-admin-message-viewer__body {
  max-width: 760px;
  color: #c9d3dc;
  font-size: 15px;
  line-height: 1.75;
  white-space: pre-wrap;
}

.portfolio-admin-message-viewer__footer {
  display: flex;
  min-height: 58px;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 0 20px;
  border-top: 1px solid var(--portfolio-admin-border);
  background: rgba(255, 255, 255, 0.012);
}

.portfolio-admin-message-viewer__reply,
.portfolio-admin-message-command__reply {
  display: inline-flex;
  min-height: 32px;
  align-items: center;
  gap: 7px;
  padding: 0 11px;
  border: 1px solid rgba(168, 103, 255, 0.25);
  border-radius: 8px;
  background: rgba(168, 103, 255, 0.045);
  color: #d4b6ff;
  font-size: 10px;
  font-weight: 650;
  text-decoration: none;
}

.portfolio-admin-message-viewer__reply:hover,
.portfolio-admin-message-command__reply:hover {
  border-color: rgba(168, 103, 255, 0.48);
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-message-viewer__reply.is-disabled {
  border-color: var(--portfolio-admin-border);
  color: var(--portfolio-admin-text-dim);
}

.portfolio-admin-message-viewer__footer small {
  color: var(--portfolio-admin-text-dim);
  font-size: 9px;
}

/* Inbox operations */
.portfolio-admin-message-operations {
  --moderation-color: var(--portfolio-admin-purple);
  overflow: hidden;
  margin: 0 0 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(
      145deg,
      color-mix(in srgb, var(--moderation-color) 7%, transparent),
      transparent 55%
    ),
    var(--portfolio-admin-surface);
}

.portfolio-admin-message-operations.is-read {
  --moderation-color: var(--portfolio-admin-green);
}

.portfolio-admin-message-operations.is-archived {
  --moderation-color: #71808c;
}

.portfolio-admin-message-operations__source {
  display: flex;
  min-height: 50px;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 0 16px;
  border-top: 1px solid var(--portfolio-admin-border);
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-message-operations__source span {
  color: var(--portfolio-admin-text-dim);
  font-size: 8px;
  letter-spacing: 0.09em;
  text-transform: uppercase;
}

.portfolio-admin-message-operations__source strong {
  max-width: 150px;
  overflow: hidden;
  color: var(--portfolio-admin-text);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  text-overflow: ellipsis;
  text-transform: capitalize;
  white-space: nowrap;
}

.portfolio-admin-message-operations__actions button.is-active {
  border-color: rgba(168, 103, 255, 0.42);
  background: rgba(168, 103, 255, 0.09);
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-message-operations__actions button.is-active.is-read {
  border-color: rgba(69, 214, 139, 0.42);
  background: rgba(69, 214, 139, 0.08);
  color: var(--portfolio-admin-green);
}

.portfolio-admin-message-operations__actions button.is-active.is-archive {
  border-color: rgba(113, 128, 140, 0.45);
  background: rgba(113, 128, 140, 0.09);
  color: #a9b4bd;
}

/* Hide duplicated native message fields now represented by the viewer. */
.collection-edit:has(.portfolio-admin-message-viewer)
  :is(#field-name, #field-email, #field-message) {
  display: none !important;
}

@media (max-width: 980px) {
  .portfolio-admin-testimonial-command__source,
  .portfolio-admin-message-command__source {
    display: none;
  }
}

@media (max-width: 768px) {
  .portfolio-admin-message-viewer__sender,
  .portfolio-admin-message-viewer__footer {
    align-items: flex-start;
    flex-direction: column;
  }

  .portfolio-admin-message-viewer__source {
    max-width: 100%;
  }
}

/* Portfolio Admin Experience - moderation and inbox editors:end */

/* Portfolio Admin Experience - custom sidebar status fields:start */

.portfolio-admin-custom-status-field {
  --custom-status-color: var(--portfolio-admin-purple);
  margin-bottom: 18px;
}

.portfolio-admin-custom-status-field.is-approved,
.portfolio-admin-custom-status-field.is-read {
  --custom-status-color: var(--portfolio-admin-green);
}

.portfolio-admin-custom-status-field.is-rejected {
  --custom-status-color: #ff6b75;
}

.portfolio-admin-custom-status-field.is-archived {
  --custom-status-color: #71808c;
}

.portfolio-admin-custom-status-field > label {
  display: flex;
  align-items: center;
  gap: 5px;
  margin-bottom: 9px;
  color: var(--portfolio-admin-text);
  font-size: 12px;
}

.portfolio-admin-custom-status-field > label > span {
  color: #ff6b75;
}

.portfolio-admin-custom-status-field__control {
  display: grid;
  grid-template-columns: 16px minmax(0, 1fr) 18px;
  min-width: 0;
  min-height: 42px;
  align-items: center;
  gap: 8px;
  overflow: hidden;
  padding: 0 12px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 9px;
  background:
    linear-gradient(
      90deg,
      color-mix(in srgb, var(--custom-status-color) 6%, transparent),
      transparent 46%
    ),
    #0d1924;
}

.portfolio-admin-custom-status-field__control:focus-within {
  border-color: color-mix(in srgb, var(--custom-status-color) 58%, transparent);
  box-shadow: 0 0 0 2px color-mix(in srgb, var(--custom-status-color) 9%, transparent);
}

.portfolio-admin-custom-status-field__control > i {
  width: 7px;
  height: 7px;
  justify-self: center;
  border-radius: 50%;
  background: var(--custom-status-color);
  box-shadow: 0 0 10px color-mix(in srgb, var(--custom-status-color) 48%, transparent);
}

.portfolio-admin-custom-status-field__control > select {
  min-width: 0;
  width: 100%;
  height: 40px;
  margin: 0;
  padding: 0;
  appearance: none;
  border: 0;
  outline: 0;
  background: transparent;
  color: var(--portfolio-admin-text);
  font: inherit;
  font-size: 12px;
  line-height: 1.2;
  cursor: pointer;
  box-shadow: none;
}

.portfolio-admin-custom-status-field__control > select:disabled {
  cursor: not-allowed;
  opacity: 0.58;
}

.portfolio-admin-custom-status-field__control > select > option {
  background: #0d1924;
  color: var(--portfolio-admin-text);
}

.portfolio-admin-custom-status-field__control > svg {
  width: 16px;
  height: 16px;
  justify-self: end;
  color: var(--portfolio-admin-text-muted);
  pointer-events: none;
}

/* Portfolio Admin Experience - custom sidebar status fields:end */

/* Portfolio Admin Experience - media library list:start */

.portfolio-admin-media-overview {
  width: 100%;
  box-sizing: border-box;
  padding: 30px clamp(28px, 4vw, 64px) 18px;
}

.portfolio-admin-media-overview__header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 20px;
}

.portfolio-admin-media-overview__eyebrow {
  display: block;
  margin-bottom: 8px;
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), monospace;
  font-size: 11px;
  font-weight: 650;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.portfolio-admin-media-overview__header h2 {
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(24px, 2.3vw, 34px);
  font-weight: 600;
  letter-spacing: -0.035em;
}

.portfolio-admin-media-overview__header p {
  max-width: 760px;
  margin: 10px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 14px;
  line-height: 1.55;
}

.portfolio-admin-media-overview__metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
}

.portfolio-admin-media-stat {
  --media-stat-color: var(--portfolio-admin-cyan);
  position: relative;
  min-height: 122px;
  overflow: hidden;
  padding: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(
      135deg,
      color-mix(in srgb, var(--media-stat-color) 7%, transparent),
      transparent 60%
    ),
    var(--portfolio-admin-surface);
  box-shadow: inset 0 2px 0 color-mix(in srgb, var(--media-stat-color) 72%, transparent);
}

.portfolio-admin-media-stat.is-green {
  --media-stat-color: var(--portfolio-admin-green);
}

.portfolio-admin-media-stat.is-purple {
  --media-stat-color: var(--portfolio-admin-purple);
}

.portfolio-admin-media-stat.is-amber {
  --media-stat-color: var(--portfolio-admin-amber);
}

.portfolio-admin-media-stat > div {
  position: relative;
  z-index: 1;
  display: flex;
  justify-content: space-between;
  gap: 12px;
}

.portfolio-admin-media-stat > div span {
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.11em;
  text-transform: uppercase;
}

.portfolio-admin-media-stat > div code {
  color: color-mix(in srgb, var(--media-stat-color) 78%, white);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-media-stat > strong {
  position: relative;
  z-index: 1;
  display: block;
  margin-top: 15px;
  color: var(--media-stat-color);
  font-family: var(--font-mono), monospace;
  font-size: 29px;
  letter-spacing: -0.05em;
}

.portfolio-admin-media-stat > p {
  position: relative;
  z-index: 1;
  max-width: 82%;
  margin: 8px 0 0;
  color: var(--portfolio-admin-text-dim);
  font-size: 11px;
  line-height: 1.4;
}

.portfolio-admin-media-stat > i {
  position: absolute;
  right: -34px;
  bottom: -52px;
  width: 128px;
  height: 128px;
  border: 1px solid color-mix(in srgb, var(--media-stat-color) 18%, transparent);
  border-radius: 50%;
}

.template-default__wrap:has(.portfolio-admin-media-overview) .list-header__title-actions,
.template-default__wrap:has(.portfolio-admin-media-overview)
  .list-header
  a[href*='/collections/media/create'] {
  display: none !important;
}

.template-default__wrap:has(.portfolio-admin-media-overview) .collection-list .table {
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background: var(--portfolio-admin-surface);
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  .table
  tbody
  tr:hover {
  background: rgba(50, 215, 231, 0.022);
  box-shadow: inset 2px 0 0 rgba(50, 215, 231, 0.44);
}

.portfolio-admin-media-asset-cell {
  display: flex;
  min-width: 260px;
  align-items: center;
  gap: 12px;
  color: inherit;
  text-decoration: none;
}

.portfolio-admin-media-asset-cell__preview {
  flex: 0 0 54px;
  display: grid;
  width: 54px;
  height: 42px;
  place-items: center;
  border: 1px solid rgba(50, 215, 231, 0.2);
  border-radius: 9px;
  background: linear-gradient(135deg, rgba(50, 215, 231, 0.1), rgba(168, 103, 255, 0.07)), #0c1822;
  color: var(--portfolio-admin-cyan);
  background-position: center;
  background-size: cover;
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-weight: 700;
}

.portfolio-admin-media-asset-cell__preview.has-image {
  color: transparent;
}

.portfolio-admin-media-asset-cell__copy,
.portfolio-admin-media-alt-cell,
.portfolio-admin-media-meta-cell {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 4px;
}

.portfolio-admin-media-asset-cell__copy {
  flex: 1;
}

.portfolio-admin-media-asset-cell__copy strong,
.portfolio-admin-media-alt-cell strong,
.portfolio-admin-media-meta-cell strong {
  overflow: hidden;
  color: var(--portfolio-admin-text);
  font-size: 11px;
  font-weight: 600;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-media-asset-cell__copy small,
.portfolio-admin-media-alt-cell small,
.portfolio-admin-media-meta-cell small {
  overflow: hidden;
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-media-asset-cell__open {
  display: grid;
  width: 27px;
  height: 27px;
  margin-left: auto;
  place-items: center;
  border: 1px solid transparent;
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  opacity: 0;
  transform: translateX(-4px);
  transition: 130ms ease;
}

.portfolio-admin-media-asset-cell:hover .portfolio-admin-media-asset-cell__open {
  border-color: rgba(50, 215, 231, 0.22);
  color: var(--portfolio-admin-cyan);
  opacity: 1;
  transform: none;
}

.portfolio-admin-media-alt-cell {
  min-width: 220px;
}

.portfolio-admin-media-folder,
.portfolio-admin-media-visibility {
  display: inline-flex;
  width: max-content;
  max-width: 170px;
  min-height: 26px;
  align-items: center;
  gap: 7px;
  overflow: hidden;
  padding: 0 10px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-media-folder > i {
  width: 9px;
  height: 7px;
  border: 1px solid currentColor;
  border-radius: 2px;
}

.portfolio-admin-media-folder.is-unsorted {
  border-color: rgba(245, 179, 66, 0.25);
  color: var(--portfolio-admin-amber);
}

.portfolio-admin-media-visibility {
  --visibility-color: #71808c;
  border-color: color-mix(in srgb, var(--visibility-color) 28%, transparent);
  color: color-mix(in srgb, var(--visibility-color) 82%, white);
  background: color-mix(in srgb, var(--visibility-color) 8%, transparent);
  text-transform: uppercase;
}

.portfolio-admin-media-visibility.is-public {
  --visibility-color: var(--portfolio-admin-green);
}

.portfolio-admin-media-visibility > i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--visibility-color);
}

.portfolio-admin-media-meta-cell {
  min-width: 130px;
}

.portfolio-admin-media-meta-cell strong {
  font-family: var(--font-mono), monospace;
}

@media (max-width: 1120px) {
  .portfolio-admin-media-overview__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 768px) {
  .portfolio-admin-media-overview {
    padding: 22px 18px 14px;
  }

  .portfolio-admin-media-overview__header {
    align-items: stretch;
    flex-direction: column;
  }

  .portfolio-admin-media-overview__metrics {
    grid-template-columns: 1fr;
  }
}

/* Portfolio Admin Experience - media library list:end */

/* Portfolio Admin Experience - media library layout polish:start */

/* Keep the custom registry heading as the only page title. */
.template-default__wrap:has(.portfolio-admin-media-overview) .collection-list .list-header__title,
.template-default__wrap:has(.portfolio-admin-media-overview) .collection-list .list-header > h1 {
  display: none !important;
}

.template-default__wrap:has(.portfolio-admin-media-overview) .collection-list .list-header {
  margin-bottom: 12px;
  padding-top: 0;
}

/* Restore spacing between the icon and Upload asset label. */
.portfolio-admin-media-overview__header .portfolio-admin-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  white-space: nowrap;
}

/*
 * `.table` is Payload's outer table viewport, not always the semantic table.
 * It must scroll instead of clipping columns pushed outside by long metadata.
 */
.template-default__wrap:has(.portfolio-admin-media-overview) .collection-list .table {
  width: 100%;
  max-width: 100%;
  overflow-x: hidden !important;
  overflow-y: hidden;
  -webkit-overflow-scrolling: touch;
}

/* Apply fixed geometry to the actual semantic table. */
.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table) {
  width: 100%;
  min-width: 0;
  max-width: 100%;
  table-layout: fixed;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td) {
  min-width: 0;
  overflow: hidden;
  box-sizing: border-box;
}

/*
 * Active Media columns:
 * 1 select, 2 Asset, 3 Alt, 4 Folder, 5 Is Public, 6 Asset Meta.
 * nth-child is an intentional fallback because Payload may put `cell-*`
 * classes on an inner element instead of the th/td itself.
 */
.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td):nth-child(1) {
  width: 4%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td):nth-child(2) {
  width: 29%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td):nth-child(3) {
  width: 27%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td):nth-child(4) {
  width: 14%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td):nth-child(5) {
  width: 11%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td):nth-child(6) {
  width: 15%;
}

/* Prefer semantic cell classes whenever Payload exposes them on th/td. */
.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td).cell-_select {
  width: 4%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td).cell-asset {
  width: 29%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td).cell-alt {
  width: 27%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td).cell-folder {
  width: 14%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td).cell-isPublic {
  width: 11%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  :is(th, td).cell-assetMeta {
  width: 15%;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  tbody
  tr {
  height: 68px;
}

.template-default__wrap:has(.portfolio-admin-media-overview)
  .collection-list
  :is(.table:is(table), .table > table, .table table)
  tbody
  td {
  padding-top: 10px;
  padding-bottom: 10px;
  vertical-align: middle;
}

.portfolio-admin-media-asset-cell {
  width: 100%;
  min-width: 0;
}

.portfolio-admin-media-asset-cell__preview {
  flex-basis: 62px;
  width: 62px;
  height: 46px;
}

.portfolio-admin-media-asset-cell__copy,
.portfolio-admin-media-alt-cell,
.portfolio-admin-media-meta-cell {
  min-width: 0 !important;
  max-width: 100%;
  overflow: hidden;
}

.portfolio-admin-media-alt-cell,
.portfolio-admin-media-meta-cell {
  width: 100%;
}

.portfolio-admin-media-alt-cell strong,
.portfolio-admin-media-alt-cell small,
.portfolio-admin-media-asset-cell__copy strong,
.portfolio-admin-media-asset-cell__copy small,
.portfolio-admin-media-meta-cell strong,
.portfolio-admin-media-meta-cell small {
  display: block;
  width: 100%;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-media-folder,
.portfolio-admin-media-visibility {
  max-width: 100%;
}

/* The table fits desktop widths; narrower workspaces scroll horizontally. */
@media (max-width: 980px) {
  .template-default__wrap:has(.portfolio-admin-media-overview) .collection-list .table {
    overflow-x: auto !important;
    overflow-y: hidden;
    -webkit-overflow-scrolling: touch;
  }

  .template-default__wrap:has(.portfolio-admin-media-overview)
    .collection-list
    :is(.table:is(table), .table > table, .table table) {
    width: 1080px;
    min-width: 1080px;
    max-width: none;
  }
}

/* Portfolio Admin Experience - media library layout polish:end */

/* Portfolio Admin Experience - media editor:start */

.collection-edit:has(.portfolio-admin-media-editor) .document-fields__edit {
  padding-top: 30px;
}

.portfolio-admin-media-editor {
  display: grid;
  grid-template-columns: minmax(280px, 0.88fr) minmax(360px, 1.12fr);
  gap: 24px;
  margin-bottom: 24px;
  padding: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 14px;
  background:
    linear-gradient(135deg, rgba(50, 215, 231, 0.04), transparent 45%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-media-editor__preview {
  min-width: 0;
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background: #08121b;
}

.portfolio-admin-media-editor__canvas {
  position: relative;
  display: grid;
  min-height: 300px;
  place-items: center;
  overflow: hidden;
  background:
    linear-gradient(
      45deg,
      rgba(255, 255, 255, 0.022) 25%,
      transparent 25%,
      transparent 75%,
      rgba(255, 255, 255, 0.022) 75%
    ),
    linear-gradient(
      45deg,
      rgba(255, 255, 255, 0.022) 25%,
      transparent 25%,
      transparent 75%,
      rgba(255, 255, 255, 0.022) 75%
    ),
    #09141e;
  background-position:
    0 0,
    12px 12px;
  background-size: 24px 24px;
}

.portfolio-admin-media-editor__canvas.has-image {
  background-color: #071019;
  background-position: center;
  background-repeat: no-repeat;
  background-size: contain;
}

.portfolio-admin-media-editor__empty {
  display: grid;
  max-width: 250px;
  justify-items: center;
  padding: 28px;
  color: var(--portfolio-admin-text-muted);
  text-align: center;
}

.portfolio-admin-media-editor__empty svg {
  width: 52px;
  height: 52px;
  margin-bottom: 14px;
  color: var(--portfolio-admin-cyan);
  opacity: 0.68;
}

.portfolio-admin-media-editor__empty strong {
  color: var(--portfolio-admin-text);
  font-size: 13px;
}

.portfolio-admin-media-editor__empty span {
  margin-top: 7px;
  font-size: 10px;
  line-height: 1.5;
}

.portfolio-admin-media-editor__preview-footer {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  padding: 12px;
  border-top: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-media-editor__badge {
  --media-badge-color: #71808c;
  display: inline-flex;
  min-height: 25px;
  align-items: center;
  gap: 7px;
  padding: 0 9px;
  border: 1px solid color-mix(in srgb, var(--media-badge-color) 28%, transparent);
  border-radius: 7px;
  background: color-mix(in srgb, var(--media-badge-color) 7%, transparent);
  color: color-mix(in srgb, var(--media-badge-color) 78%, white);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

.portfolio-admin-media-editor__badge.is-cyan {
  --media-badge-color: var(--portfolio-admin-cyan);
}

.portfolio-admin-media-editor__badge.is-green {
  --media-badge-color: var(--portfolio-admin-green);
}

.portfolio-admin-media-editor__badge.is-purple {
  --media-badge-color: var(--portfolio-admin-purple);
}

.portfolio-admin-media-editor__badge > i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--media-badge-color);
}

.portfolio-admin-media-editor__content {
  display: flex;
  min-width: 0;
  flex-direction: column;
  justify-content: center;
  padding: 10px 8px;
}

.portfolio-admin-media-editor__eyebrow,
.portfolio-admin-media-external header span,
.portfolio-admin-media-external.is-local > div:last-child > span {
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 650;
  letter-spacing: 0.14em;
  text-transform: uppercase;
}

.portfolio-admin-media-editor__title-row {
  display: flex;
  min-width: 0;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
  margin-top: 10px;
}

.portfolio-admin-media-editor__title-row > div {
  min-width: 0;
}

.portfolio-admin-media-editor__title-row h2 {
  overflow: hidden;
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(22px, 2.2vw, 34px);
  font-weight: 600;
  letter-spacing: -0.035em;
  line-height: 1.1;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-media-editor__title-row p {
  max-width: 620px;
  margin: 10px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 12px;
  line-height: 1.55;
}

.portfolio-admin-media-editor__source-link,
.portfolio-admin-media-external__link {
  display: inline-flex;
  min-height: 34px;
  flex: 0 0 auto;
  align-items: center;
  gap: 8px;
  padding: 0 11px;
  border: 1px solid rgba(168, 103, 255, 0.28);
  border-radius: 8px;
  background: rgba(168, 103, 255, 0.055);
  color: #c9a8ff;
  font-size: 10px;
  font-weight: 600;
  text-decoration: none;
}

.portfolio-admin-media-editor__source-link:hover,
.portfolio-admin-media-external__link:hover {
  border-color: rgba(168, 103, 255, 0.54);
  background: rgba(168, 103, 255, 0.1);
}

.portfolio-admin-media-editor__meta-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1px;
  overflow: hidden;
  margin-top: 24px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 10px;
  background: var(--portfolio-admin-border);
}

.portfolio-admin-media-editor__meta-item {
  min-width: 0;
  padding: 14px;
  background: #0b1721;
}

.portfolio-admin-media-editor__meta-item span {
  display: block;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
  letter-spacing: 0.09em;
  text-transform: uppercase;
}

.portfolio-admin-media-editor__meta-item strong {
  display: block;
  overflow: hidden;
  margin-top: 7px;
  color: var(--portfolio-admin-text);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
  font-weight: 550;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-media-readiness {
  --media-readiness-color: var(--portfolio-admin-cyan);
  overflow: hidden;
  margin-bottom: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(145deg, rgba(50, 215, 231, 0.05), transparent 48%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-media-readiness > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 18px;
}

.portfolio-admin-media-readiness > header span {
  display: block;
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-media-readiness > header strong {
  display: block;
  margin-top: 8px;
  color: var(--media-readiness-color);
  font-family: var(--font-mono), monospace;
  font-size: 27px;
  letter-spacing: -0.05em;
}

.portfolio-admin-media-readiness__ring {
  --media-readiness: 0deg;
  position: relative;
  display: grid;
  width: 48px;
  height: 48px;
  flex: 0 0 48px;
  place-items: center;
  border-radius: 50%;
  background: conic-gradient(
    var(--media-readiness-color) var(--media-readiness),
    rgba(120, 145, 160, 0.15) 0
  );
}

.portfolio-admin-media-readiness__ring::before {
  position: absolute;
  width: 37px;
  height: 37px;
  border-radius: 50%;
  background: #0b1721;
  content: '';
}

.portfolio-admin-media-readiness__ring > i {
  position: relative;
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: var(--media-readiness-color);
  box-shadow: 0 0 10px var(--media-readiness-color);
}

.portfolio-admin-media-readiness > p {
  margin: 0;
  padding: 0 18px 16px;
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  line-height: 1.45;
}

.portfolio-admin-media-readiness > ul {
  margin: 0;
  padding: 0;
  border-top: 1px solid var(--portfolio-admin-border);
  list-style: none;
}

.portfolio-admin-media-readiness > ul li {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 16px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-media-readiness > ul li:last-child {
  border-bottom: 0;
}

.portfolio-admin-media-readiness > ul li > span {
  display: grid;
  width: 24px;
  height: 24px;
  flex: 0 0 24px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-media-readiness > ul li.is-complete > span {
  border-color: rgba(68, 217, 144, 0.28);
  background: rgba(68, 217, 144, 0.07);
  color: var(--portfolio-admin-green);
}

.portfolio-admin-media-readiness > ul li div {
  min-width: 0;
}

.portfolio-admin-media-readiness > ul li strong,
.portfolio-admin-media-readiness > ul li small {
  display: block;
}

.portfolio-admin-media-readiness > ul li strong {
  color: var(--portfolio-admin-text);
  font-size: 10px;
  font-weight: 600;
}

.portfolio-admin-media-readiness > ul li small {
  margin-top: 3px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
}

.portfolio-admin-media-external {
  padding: 20px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(135deg, rgba(168, 103, 255, 0.055), transparent 52%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-media-external > header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
}

.portfolio-admin-media-external h3 {
  margin: 8px 0 0;
  color: var(--portfolio-admin-text);
  font-size: 20px;
  font-weight: 600;
  text-transform: capitalize;
}

.portfolio-admin-media-external p {
  max-width: 700px;
  margin: 8px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 11px;
  line-height: 1.55;
}

.portfolio-admin-media-external__grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1px;
  overflow: hidden;
  margin-top: 20px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 10px;
  background: var(--portfolio-admin-border);
}

.portfolio-admin-media-external__item {
  min-width: 0;
  padding: 14px;
  background: #0a1620;
}

.portfolio-admin-media-external__item span {
  display: block;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
  letter-spacing: 0.09em;
  text-transform: uppercase;
}

.portfolio-admin-media-external__item strong {
  display: block;
  overflow: hidden;
  margin-top: 7px;
  color: var(--portfolio-admin-text);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 500;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-media-external__link {
  width: max-content;
  margin-top: 16px;
}

.portfolio-admin-media-external.is-local {
  display: flex;
  align-items: center;
  gap: 18px;
}

.portfolio-admin-media-external__icon {
  display: grid;
  width: 58px;
  height: 58px;
  flex: 0 0 58px;
  place-items: center;
  border: 1px solid rgba(50, 215, 231, 0.22);
  border-radius: 13px;
  background: rgba(50, 215, 231, 0.055);
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-media-external__icon svg {
  width: 32px;
  height: 32px;
}

@media (max-width: 980px) {
  .portfolio-admin-media-editor {
    grid-template-columns: 1fr;
  }

  .portfolio-admin-media-editor__canvas {
    min-height: 260px;
  }
}

@media (max-width: 640px) {
  .portfolio-admin-media-editor__title-row,
  .portfolio-admin-media-external > header {
    align-items: stretch;
    flex-direction: column;
  }

  .portfolio-admin-media-editor__meta-grid,
  .portfolio-admin-media-external__grid {
    grid-template-columns: 1fr;
  }

  .portfolio-admin-media-external.is-local {
    align-items: flex-start;
    flex-direction: column;
  }
}

/* Portfolio Admin Experience - media editor:end */

/* Portfolio Admin Experience - media editor polish:start */

.collection-edit:has(.portfolio-admin-media-editor) .file-field {
  overflow: hidden;
  margin-bottom: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background: linear-gradient(90deg, rgba(50, 215, 231, 0.035), transparent 46%), #0a1620;
  box-shadow: none;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-field:not(:disabled):hover,
.collection-edit:has(.portfolio-admin-media-editor) .file-details:not(:disabled):hover {
  box-shadow: none;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-field__upload,
.collection-edit:has(.portfolio-admin-media-editor) .file-field__file-selected,
.collection-edit:has(.portfolio-admin-media-editor) .file-details > header {
  width: 100%;
  min-width: 0;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-details {
  border: 0;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-details > header {
  min-height: 82px;
  align-items: stretch;
  flex-wrap: nowrap;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-field__thumbnail-wrap,
.collection-edit:has(.portfolio-admin-media-editor) .file-details .thumbnail {
  width: 124px;
  min-width: 124px;
  height: 82px;
  flex: 0 0 124px;
  overflow: hidden;
  border-radius: 0;
  background: #071019;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-details .thumbnail :is(img, svg),
.collection-edit:has(.portfolio-admin-media-editor) .file-field__thumbnail-wrap :is(img, svg) {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-details__main-detail,
.collection-edit:has(.portfolio-admin-media-editor) .file-field__file-adjustments {
  min-width: 0;
  flex: 1;
  justify-content: center;
  gap: 5px;
  padding: 11px 54px 11px 15px;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-meta__url {
  min-width: 0;
  align-items: center;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-meta__url a {
  display: block;
  max-width: min(54vw, 720px);
  overflow: hidden;
  color: var(--portfolio-admin-text);
  font-size: 11px;
  font-weight: 600;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-meta__size-type {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-details__file-mutation,
.collection-edit:has(.portfolio-admin-media-editor) .file-field__upload-actions {
  gap: 7px;
  margin-top: 4px;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-details__edit {
  min-height: 28px;
  padding: 0 9px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  background: rgba(255, 255, 255, 0.025);
  color: var(--portfolio-admin-text);
  font-size: 9px;
}

.collection-edit:has(.portfolio-admin-media-editor) .file-details__edit:hover {
  border-color: rgba(50, 215, 231, 0.28);
  background: rgba(50, 215, 231, 0.055);
  color: var(--portfolio-admin-cyan);
}

.collection-edit:has(.portfolio-admin-media-editor) .file-details__remove,
.collection-edit:has(.portfolio-admin-media-editor) .file-field__remove {
  top: 50%;
  right: 12px;
  margin: 0;
  transform: translateY(-50%);
}

.portfolio-admin-media-document-title {
  display: block;
  width: min(100%, 980px);
  max-width: min(100%, 980px);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-media-caption {
  margin-bottom: 22px;
}

.portfolio-admin-media-caption__header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 18px;
  margin-bottom: 9px;
}

.portfolio-admin-media-caption__header > div {
  min-width: 0;
}

.portfolio-admin-media-caption__header label {
  display: block;
  color: var(--portfolio-admin-text);
  font-size: 12px;
}

.portfolio-admin-media-caption__header > div > span {
  display: block;
  margin-top: 6px;
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  line-height: 1.45;
}

.portfolio-admin-media-caption__expand {
  display: inline-flex;
  min-height: 32px;
  flex: 0 0 auto;
  align-items: center;
  gap: 7px;
  padding: 0 10px;
  border: 1px solid rgba(168, 103, 255, 0.25);
  border-radius: 8px;
  background: rgba(168, 103, 255, 0.045);
  color: #c6a5ff;
  font: inherit;
  font-size: 9px;
  font-weight: 600;
  cursor: pointer;
}

.portfolio-admin-media-caption__expand:hover,
.portfolio-admin-media-caption__expand:focus-visible {
  border-color: rgba(168, 103, 255, 0.52);
  background: rgba(168, 103, 255, 0.09);
}

.portfolio-admin-media-caption__expand svg {
  width: 15px;
  height: 15px;
}

.portfolio-admin-media-caption textarea {
  display: block;
  width: 100%;
  min-height: 236px;
  max-height: 70vh;
  box-sizing: border-box;
  resize: vertical;
  padding: 15px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 10px 10px 0 0;
  outline: none;
  background: #0d1924;
  color: var(--portfolio-admin-text);
  font: inherit;
  font-size: 12px;
  line-height: 1.58;
  transition:
    border-color 140ms ease,
    box-shadow 140ms ease,
    min-height 180ms ease;
}

.portfolio-admin-media-caption.is-expanded textarea {
  min-height: min(560px, 66vh);
}

.portfolio-admin-media-caption textarea:focus {
  border-color: rgba(168, 103, 255, 0.58);
  box-shadow: 0 0 0 2px rgba(168, 103, 255, 0.08);
}

.portfolio-admin-media-caption textarea:disabled {
  cursor: not-allowed;
  opacity: 0.62;
}

.portfolio-admin-media-caption__footer {
  display: flex;
  min-height: 34px;
  align-items: center;
  justify-content: flex-end;
  gap: 16px;
  padding: 0 12px;
  border: 1px solid var(--portfolio-admin-border);
  border-top: 0;
  border-radius: 0 0 10px 10px;
  background: #0a1620;
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
}

.portfolio-admin-media-caption__footer strong {
  color: var(--portfolio-admin-text);
  font-weight: 600;
}

.portfolio-admin-media-caption__error {
  display: block;
  margin-top: 7px;
  color: #ff6b75;
  font-size: 9px;
}

.portfolio-admin-media-caption.has-error textarea {
  border-color: rgba(255, 107, 117, 0.58);
}

@media (max-width: 768px) {
  .collection-edit:has(.portfolio-admin-media-editor) .file-details > header {
    min-height: 0;
    flex-wrap: wrap;
  }

  .collection-edit:has(.portfolio-admin-media-editor) .file-field__thumbnail-wrap,
  .collection-edit:has(.portfolio-admin-media-editor) .file-details .thumbnail {
    width: 100%;
    min-width: 100%;
    height: 170px;
    flex-basis: 100%;
  }

  .portfolio-admin-media-caption__header {
    align-items: stretch;
    flex-direction: column;
  }

  .portfolio-admin-media-caption__expand {
    width: max-content;
  }
}

@media (prefers-reduced-motion: reduce) {
  .portfolio-admin-media-caption textarea {
    transition: none;
  }
}

/* Portfolio Admin Experience - media editor polish:end */

/* Portfolio Admin Experience - media readiness spacing:start */

.collection-edit:has(.portfolio-admin-media-editor) .portfolio-admin-media-readiness {
  margin-top: 24px;
}

/* Portfolio Admin Experience - media readiness spacing:end */

/* Portfolio Admin Experience - taxonomy workspace lists:start */

.template-default__wrap:has(.portfolio-admin-taxonomy-overview)
  .collection-list
  .list-header__title,
.template-default__wrap:has(.portfolio-admin-taxonomy-overview) .collection-list .list-header > h1 {
  display: none !important;
}

.template-default__wrap:has(.portfolio-admin-taxonomy-overview) .collection-list .list-header {
  margin-bottom: 12px;
  padding-top: 0;
}

.portfolio-admin-taxonomy-overview {
  margin-bottom: 22px;
}

.portfolio-admin-taxonomy-overview__header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 20px;
}

.portfolio-admin-taxonomy-overview__eyebrow {
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.portfolio-admin-taxonomy-overview.is-tech-stack .portfolio-admin-taxonomy-overview__eyebrow {
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-taxonomy-overview h1 {
  margin: 9px 0 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(28px, 3vw, 38px);
  font-weight: 600;
  letter-spacing: -0.045em;
  line-height: 1.05;
}

.portfolio-admin-taxonomy-overview__header p {
  max-width: 760px;
  margin: 9px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 13px;
  line-height: 1.5;
}

.portfolio-admin-taxonomy-overview__header .portfolio-admin-button {
  gap: 8px;
  white-space: nowrap;
}

.portfolio-admin-taxonomy-overview__metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
}

.portfolio-admin-taxonomy-metric {
  --taxonomy-tone: var(--portfolio-admin-cyan);
  position: relative;
  min-height: 116px;
  overflow: hidden;
  padding: 16px 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-top: 2px solid var(--taxonomy-tone);
  border-radius: 12px;
  background:
    linear-gradient(
      145deg,
      color-mix(in srgb, var(--taxonomy-tone) 7%, transparent),
      transparent 58%
    ),
    var(--portfolio-admin-surface);
}

.portfolio-admin-taxonomy-metric.is-purple {
  --taxonomy-tone: var(--portfolio-admin-purple);
}

.portfolio-admin-taxonomy-metric.is-green {
  --taxonomy-tone: var(--portfolio-admin-green);
}

.portfolio-admin-taxonomy-metric.is-amber {
  --taxonomy-tone: var(--portfolio-admin-amber);
}

.portfolio-admin-taxonomy-metric > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.portfolio-admin-taxonomy-metric > header span {
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-taxonomy-metric > header code {
  color: var(--taxonomy-tone);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
}

.portfolio-admin-taxonomy-metric > strong {
  display: block;
  margin-top: 12px;
  color: var(--taxonomy-tone);
  font-family: var(--font-mono), monospace;
  font-size: 28px;
  letter-spacing: -0.05em;
}

.portfolio-admin-taxonomy-metric > small {
  display: block;
  margin-top: 7px;
  color: var(--portfolio-admin-text-soft);
  font-size: 10px;
}

.portfolio-admin-taxonomy-metric > i {
  position: absolute;
  right: -25px;
  bottom: -40px;
  width: 122px;
  height: 84px;
  border: 1px solid var(--taxonomy-tone);
  border-radius: 50%;
  opacity: 0.14;
}

.portfolio-admin-category-cell,
.portfolio-admin-technology-cell {
  display: grid;
  min-width: 0;
  align-items: center;
  gap: 12px;
  color: var(--portfolio-admin-text);
  text-decoration: none;
}

.portfolio-admin-category-cell {
  grid-template-columns: 42px minmax(0, 1fr) auto;
}

.portfolio-admin-technology-cell {
  grid-template-columns: 44px minmax(0, 1fr) auto;
}

.portfolio-admin-category-cell__mark,
.portfolio-admin-technology-cell__mark {
  display: grid;
  place-items: center;
  font-family: var(--font-mono), monospace;
  font-size: 10px;
  font-weight: 700;
}

.portfolio-admin-category-cell__mark {
  width: 40px;
  height: 36px;
  border: 1px solid rgba(50, 215, 231, 0.26);
  border-radius: 9px;
  background: rgba(50, 215, 231, 0.07);
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-technology-cell__mark {
  --technology-color: var(--portfolio-admin-cyan);
  width: 42px;
  height: 38px;
  border: 1px solid color-mix(in srgb, var(--technology-color) 38%, transparent);
  border-radius: 9px;
  background: color-mix(in srgb, var(--technology-color) 10%, transparent);
  color: var(--technology-color);
}

.portfolio-admin-category-cell__copy,
.portfolio-admin-technology-cell__copy {
  min-width: 0;
}

.portfolio-admin-category-cell__copy strong,
.portfolio-admin-category-cell__copy small,
.portfolio-admin-technology-cell__copy strong,
.portfolio-admin-technology-cell__copy small {
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-category-cell__copy strong,
.portfolio-admin-technology-cell__copy strong {
  font-size: 11px;
  font-weight: 650;
}

.portfolio-admin-category-cell__copy small,
.portfolio-admin-technology-cell__copy small {
  margin-top: 4px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
}

.portfolio-admin-category-cell__copy code,
.portfolio-admin-technology-cell__copy code {
  color: var(--portfolio-admin-text-soft);
  font-family: var(--font-mono), monospace;
}

.portfolio-admin-category-cell__arrow,
.portfolio-admin-technology-cell__arrow {
  color: var(--portfolio-admin-text-muted);
  font-size: 11px;
  opacity: 0;
  transform: translateX(-4px);
  transition:
    opacity 130ms ease,
    transform 130ms ease;
}

.portfolio-admin-category-cell:hover .portfolio-admin-category-cell__arrow,
.portfolio-admin-technology-cell:hover .portfolio-admin-technology-cell__arrow {
  opacity: 1;
  transform: translateX(0);
}

.portfolio-admin-taxonomy-badge,
.portfolio-admin-boolean-badge {
  --badge-tone: var(--portfolio-admin-cyan);
  display: inline-flex;
  min-height: 25px;
  align-items: center;
  gap: 7px;
  padding: 0 9px;
  border: 1px solid color-mix(in srgb, var(--badge-tone) 30%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--badge-tone) 7%, transparent);
  color: var(--badge-tone);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

.portfolio-admin-taxonomy-badge > i,
.portfolio-admin-boolean-badge > i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: currentColor;
}

.portfolio-admin-taxonomy-badge.is-project,
.portfolio-admin-boolean-badge.is-purple {
  --badge-tone: var(--portfolio-admin-purple);
}

.portfolio-admin-taxonomy-badge.is-blog,
.portfolio-admin-boolean-badge.is-green {
  --badge-tone: var(--portfolio-admin-green);
}

.portfolio-admin-taxonomy-badge.is-shared {
  --badge-tone: var(--portfolio-admin-amber);
}

.portfolio-admin-boolean-badge:not(.is-active) {
  --badge-tone: var(--portfolio-admin-text-muted);
  opacity: 0.72;
}

.portfolio-admin-taxonomy-parent,
.portfolio-admin-technology-category {
  display: inline-flex;
  min-width: 0;
  max-width: 100%;
  align-items: center;
  gap: 7px;
  overflow: hidden;
  color: var(--portfolio-admin-text-soft);
  font-size: 9px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-taxonomy-parent > i {
  width: 8px;
  height: 8px;
  border-right: 1px solid var(--portfolio-admin-cyan);
  border-bottom: 1px solid var(--portfolio-admin-cyan);
  opacity: 0.7;
}

.portfolio-admin-taxonomy-parent.is-root,
.portfolio-admin-technology-category.is-missing {
  color: var(--portfolio-admin-text-muted);
}

.portfolio-admin-taxonomy-parent.is-root > i {
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 50%;
}

.portfolio-admin-technology-category {
  min-height: 25px;
  padding: 0 9px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  background: rgba(255, 255, 255, 0.018);
}

.portfolio-admin-technology-links {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.portfolio-admin-technology-links :is(a, span) {
  display: inline-flex;
  min-height: 24px;
  align-items: center;
  padding: 0 8px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
  text-decoration: none;
}

.portfolio-admin-technology-links a {
  border-color: rgba(50, 215, 231, 0.22);
  color: #9fe8ef;
}

.portfolio-admin-technology-links a:hover {
  border-color: rgba(50, 215, 231, 0.48);
  background: rgba(50, 215, 231, 0.055);
}

.portfolio-admin-taxonomy-order {
  display: inline-grid;
  min-width: 28px;
  height: 27px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-soft);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

@media (max-width: 1100px) {
  .portfolio-admin-taxonomy-overview__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 680px) {
  .portfolio-admin-taxonomy-overview__header {
    align-items: stretch;
    flex-direction: column;
  }

  .portfolio-admin-taxonomy-overview__header .portfolio-admin-button {
    width: max-content;
  }

  .portfolio-admin-taxonomy-overview__metrics {
    grid-template-columns: 1fr;
  }
}

/* Portfolio Admin Experience - taxonomy workspace lists:end */

/* Portfolio Admin Experience - taxonomy layout inset:start */

/*
 * Match the page gutter used by Projects, Blog Posts, Media,
 * Testimonials, and Contact Messages.
 */
.portfolio-admin-taxonomy-overview {
  width: 100%;
  box-sizing: border-box;
  margin-bottom: 0;
  padding: 30px clamp(28px, 4vw, 64px) 18px;
}

/*
 * The custom taxonomy header already owns the primary create action.
 * Hide only Payload's duplicated list-header action, while preserving
 * the real empty-state create button.
 */
.template-default__wrap:has(.portfolio-admin-taxonomy-overview) .list-header__title-actions,
.template-default__wrap:has(.portfolio-admin-taxonomy-overview)
  .list-header
  a[href*='/collections/categories/create'],
.template-default__wrap:has(.portfolio-admin-taxonomy-overview)
  .list-header
  a[href*='/collections/tech-stack/create'] {
  display: none !important;
}

@media (max-width: 760px) {
  .portfolio-admin-taxonomy-overview {
    padding: 22px 14px 16px;
  }
}

/* Portfolio Admin Experience - taxonomy layout inset:end */

/* Portfolio Admin Experience - category editor:start */

.collection-edit:has(.portfolio-admin-category-workspace) .document-fields {
  padding-top: 18px;
}

.collection-edit:has(.portfolio-admin-category-workspace) .tabs-field {
  overflow: hidden;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
}

.collection-edit:has(.portfolio-admin-category-workspace) .tabs-field > .tabs-field__tabs {
  border-bottom: 1px solid var(--portfolio-admin-border);
  background: rgba(6, 16, 24, 0.58);
}

.collection-edit:has(.portfolio-admin-category-workspace) .tabs-field > .tabs-field__tab-panel {
  padding: 28px 30px 32px;
}

.portfolio-admin-category-workspace {
  display: grid;
  grid-template-columns: minmax(0, 1.18fr) minmax(300px, 0.82fr);
  gap: 22px;
  margin-bottom: 22px;
  padding: 24px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 13px;
  background:
    radial-gradient(circle at 86% 14%, rgba(50, 215, 231, 0.08), transparent 30%),
    linear-gradient(145deg, rgba(168, 103, 255, 0.035), transparent 52%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-category-workspace__eyebrow {
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.portfolio-admin-category-workspace__title-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 12px;
  margin-top: 10px;
}

.portfolio-admin-category-workspace h2 {
  min-width: 0;
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(25px, 3vw, 36px);
  font-weight: 600;
  letter-spacing: -0.045em;
  line-height: 1.06;
}

.portfolio-admin-category-workspace__copy > code {
  display: block;
  width: max-content;
  max-width: 100%;
  overflow: hidden;
  margin-top: 9px;
  color: var(--portfolio-admin-text-soft);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-category-workspace__copy > p {
  max-width: 680px;
  margin: 18px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 12px;
  line-height: 1.58;
}

.portfolio-admin-category-workspace dl {
  display: flex;
  flex-wrap: wrap;
  gap: 9px;
  margin: 22px 0 0;
}

.portfolio-admin-category-workspace dl > div {
  min-width: 104px;
  padding: 10px 12px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 9px;
  background: rgba(255, 255, 255, 0.018);
}

.portfolio-admin-category-workspace dt {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 7px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-category-workspace dd {
  overflow: hidden;
  margin: 6px 0 0;
  color: var(--portfolio-admin-text);
  font-size: 10px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-category-status {
  --category-status-tone: var(--portfolio-admin-cyan);
  display: inline-flex;
  min-height: 26px;
  align-items: center;
  gap: 7px;
  padding: 0 9px;
  border: 1px solid color-mix(in srgb, var(--category-status-tone) 34%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--category-status-tone) 7%, transparent);
  color: var(--category-status-tone);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.portfolio-admin-category-status.is-purple,
.portfolio-admin-category-node.is-purple {
  --category-status-tone: var(--portfolio-admin-purple);
}

.portfolio-admin-category-status.is-green,
.portfolio-admin-category-node.is-green {
  --category-status-tone: var(--portfolio-admin-green);
}

.portfolio-admin-category-status.is-amber,
.portfolio-admin-category-node.is-amber {
  --category-status-tone: var(--portfolio-admin-amber);
}

.portfolio-admin-category-status.is-cyan,
.portfolio-admin-category-node.is-cyan {
  --category-status-tone: var(--portfolio-admin-cyan);
}

.portfolio-admin-category-status > i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: currentColor;
}

.portfolio-admin-category-node {
  --category-status-tone: var(--portfolio-admin-cyan);
  position: relative;
  display: grid;
  min-height: 228px;
  place-items: center;
  overflow: hidden;
  padding: 38px 24px;
  border: 1px solid
    color-mix(in srgb, var(--category-status-tone) 24%, var(--portfolio-admin-border));
  border-radius: 12px;
  background:
    linear-gradient(
      145deg,
      color-mix(in srgb, var(--category-status-tone) 8%, transparent),
      transparent 58%
    ),
    #08141e;
}

.portfolio-admin-category-node::before,
.portfolio-admin-category-node::after {
  position: absolute;
  border: 1px solid color-mix(in srgb, var(--category-status-tone) 28%, transparent);
  border-radius: 50%;
  content: '';
}

.portfolio-admin-category-node::before {
  width: 210px;
  height: 210px;
}

.portfolio-admin-category-node::after {
  width: 142px;
  height: 142px;
}

.portfolio-admin-category-node > i {
  position: absolute;
  width: 1px;
  height: 100%;
  background: linear-gradient(transparent, var(--category-status-tone), transparent);
  opacity: 0.28;
}

.portfolio-admin-category-node > div {
  position: relative;
  z-index: 2;
  display: grid;
  width: min(100%, 240px);
  min-height: 112px;
  place-items: center;
  padding: 18px;
  border: 1px solid color-mix(in srgb, var(--category-status-tone) 46%, transparent);
  border-radius: 11px;
  background: rgba(5, 14, 22, 0.9);
  text-align: center;
}

.portfolio-admin-category-node > div small {
  color: var(--category-status-tone);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.14em;
}

.portfolio-admin-category-node > div strong {
  display: block;
  max-width: 100%;
  overflow: hidden;
  margin-top: 8px;
  color: var(--portfolio-admin-text);
  font-size: 15px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-category-node > div code {
  display: block;
  max-width: 100%;
  overflow: hidden;
  margin-top: 7px;
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-category-node__parent,
.portfolio-admin-category-node__scope {
  position: absolute;
  z-index: 3;
  max-width: calc(100% - 34px);
  overflow: hidden;
  font-family: var(--font-mono), monospace;
  font-size: 7px;
  letter-spacing: 0.08em;
  text-overflow: ellipsis;
  text-transform: uppercase;
  white-space: nowrap;
}

.portfolio-admin-category-node__parent {
  top: 15px;
  left: 17px;
  color: var(--portfolio-admin-text-muted);
}

.portfolio-admin-category-node__scope {
  right: 17px;
  bottom: 15px;
  color: var(--category-status-tone);
}

.portfolio-admin-category-hierarchy {
  margin-top: 8px;
  padding: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background:
    linear-gradient(90deg, rgba(50, 215, 231, 0.035), transparent 58%), rgba(7, 17, 25, 0.56);
}

.portfolio-admin-category-hierarchy > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.portfolio-admin-category-hierarchy > header span,
.portfolio-admin-category-hierarchy > header strong {
  display: block;
}

.portfolio-admin-category-hierarchy > header span {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-category-hierarchy > header strong {
  margin-top: 5px;
  color: var(--portfolio-admin-text);
  font-size: 12px;
}

.portfolio-admin-category-hierarchy__path {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 17px;
  padding: 11px 12px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-soft);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

.portfolio-admin-category-hierarchy__path .is-root {
  color: var(--portfolio-admin-cyan);
}

.portfolio-admin-category-hierarchy__path i {
  color: var(--portfolio-admin-text-muted);
  font-style: normal;
}

.portfolio-admin-category-hierarchy__path strong {
  color: var(--portfolio-admin-text);
  font-weight: 650;
}

.portfolio-admin-category-hierarchy > p {
  margin: 13px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  line-height: 1.5;
}

.portfolio-admin-category-readiness {
  --category-readiness: 0%;
  overflow: hidden;
  margin-top: 24px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(145deg, rgba(50, 215, 231, 0.045), transparent 54%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-category-readiness > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 18px;
}

.portfolio-admin-category-readiness > header span,
.portfolio-admin-category-readiness > header strong {
  display: block;
}

.portfolio-admin-category-readiness > header span {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-category-readiness > header strong {
  margin-top: 7px;
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), monospace;
  font-size: 26px;
}

.portfolio-admin-category-readiness > header > i {
  position: relative;
  display: grid;
  width: 52px;
  height: 52px;
  place-items: center;
  border-radius: 50%;
  background: conic-gradient(
    var(--portfolio-admin-cyan) var(--category-readiness),
    rgba(255, 255, 255, 0.07) 0
  );
}

.portfolio-admin-category-readiness > header > i::after {
  position: absolute;
  width: 42px;
  height: 42px;
  border-radius: 50%;
  background: #0b1822;
  content: '';
}

.portfolio-admin-category-readiness > header > i > b {
  position: relative;
  z-index: 1;
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: var(--portfolio-admin-cyan);
  box-shadow: 0 0 12px var(--portfolio-admin-cyan);
}

.portfolio-admin-category-readiness > p {
  margin: 0;
  padding: 0 18px 17px;
  color: var(--portfolio-admin-text-soft);
  font-size: 10px;
  line-height: 1.5;
}

.portfolio-admin-category-readiness ul {
  margin: 0;
  padding: 0;
  border-top: 1px solid var(--portfolio-admin-border);
  list-style: none;
}

.portfolio-admin-category-readiness li {
  display: grid;
  grid-template-columns: 26px minmax(0, 1fr);
  gap: 10px;
  padding: 13px 16px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-category-readiness li:last-child {
  border-bottom: 0;
}

.portfolio-admin-category-readiness li > span {
  display: grid;
  width: 24px;
  height: 24px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-category-readiness li.is-complete > span {
  border-color: rgba(67, 221, 145, 0.3);
  background: rgba(67, 221, 145, 0.07);
  color: var(--portfolio-admin-green);
}

.portfolio-admin-category-readiness li strong,
.portfolio-admin-category-readiness li small {
  display: block;
}

.portfolio-admin-category-readiness li strong {
  color: var(--portfolio-admin-text);
  font-size: 10px;
}

.portfolio-admin-category-readiness li small {
  margin-top: 4px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
  line-height: 1.4;
}

@media (max-width: 980px) {
  .portfolio-admin-category-workspace {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 760px) {
  .collection-edit:has(.portfolio-admin-category-workspace) .tabs-field > .tabs-field__tab-panel {
    padding: 20px 14px 24px;
  }

  .portfolio-admin-category-workspace {
    padding: 18px;
  }

  .portfolio-admin-category-workspace__title-row {
    align-items: flex-start;
    flex-direction: column;
  }
}

/* Portfolio Admin Experience - category editor:end */

/* Portfolio Admin Experience - category tabs exact-dom:start */

/*
 * Confirmed Payload DOM:
 *
 * <div class="tabs-field__tabs">
 *   <button class="tabs-field__tab-button tabs-field__tab-button--active">
 *   <button class="tabs-field__tab-button">
 * </div>
 */

.collection-edit:has(.portfolio-admin-category-workspace) .tabs-field {
  width: 100% !important;
  overflow: visible !important;
  border: 0 !important;
  border-radius: 0 !important;
  background: transparent !important;
}

.collection-edit:has(.portfolio-admin-category-workspace) .tabs-field__tabs {
  position: sticky;
  top: calc(var(--portfolio-admin-topbar-height) + 8px);
  z-index: 12;
  display: flex !important;
  width: 100% !important;
  min-height: 52px;
  box-sizing: border-box !important;
  align-items: center;
  gap: 6px;
  overflow-x: auto;
  margin: 0 0 24px !important;
  padding: 6px 8px !important;
  border: 1px solid var(--portfolio-admin-border) !important;
  border-radius: 11px !important;
  background: rgba(7, 16, 25, 0.94) !important;
  box-shadow: 0 12px 26px rgba(0, 0, 0, 0.08);
  backdrop-filter: blur(16px);
  scrollbar-width: none;
}

.collection-edit:has(.portfolio-admin-category-workspace) .tabs-field__tabs::-webkit-scrollbar {
  display: none;
}

.collection-edit:has(.portfolio-admin-category-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button {
  display: inline-flex !important;
  min-width: 0;
  min-height: 38px;
  box-sizing: border-box;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
  margin: 0 !important;
  padding: 0 15px !important;
  border: 1px solid transparent !important;
  border-radius: 8px !important;
  appearance: none;
  background: transparent !important;
  color: var(--portfolio-admin-text-dim) !important;
  font: inherit;
  font-size: 11px !important;
  font-weight: 600 !important;
  line-height: 1 !important;
  white-space: nowrap;
  box-shadow: none !important;
  cursor: pointer;
  transition:
    border-color 140ms ease,
    background-color 140ms ease,
    color 140ms ease,
    box-shadow 140ms ease;
}

.collection-edit:has(.portfolio-admin-category-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button:hover {
  border-color: rgba(50, 215, 231, 0.12) !important;
  background: rgba(255, 255, 255, 0.025) !important;
  color: var(--portfolio-admin-text) !important;
}

.collection-edit:has(.portfolio-admin-category-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button.tabs-field__tab-button--active {
  border-color: rgba(50, 215, 231, 0.42) !important;
  background: linear-gradient(
    180deg,
    rgba(50, 215, 231, 0.12),
    rgba(50, 215, 231, 0.065)
  ) !important;
  color: var(--portfolio-admin-cyan) !important;
  box-shadow:
    inset 0 0 0 1px rgba(50, 215, 231, 0.045),
    0 0 0 1px rgba(50, 215, 231, 0.025) !important;
}

.collection-edit:has(.portfolio-admin-category-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button::before,
.collection-edit:has(.portfolio-admin-category-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button::after {
  display: none !important;
  width: 0 !important;
  height: 0 !important;
  border: 0 !important;
  background: transparent !important;
  box-shadow: none !important;
  content: none !important;
}

.collection-edit:has(.portfolio-admin-category-workspace) .tabs-field > .tabs-field__tab-panel {
  display: block !important;
  width: 100% !important;
  min-width: 0 !important;
  box-sizing: border-box !important;
  padding: 0 !important;
  border: 0 !important;
  background: transparent !important;
}

@media (max-width: 760px) {
  .collection-edit:has(.portfolio-admin-category-workspace) .tabs-field__tabs {
    top: calc(var(--portfolio-admin-topbar-height) + 5px);
    min-height: 48px;
    margin-bottom: 20px !important;
    padding: 5px !important;
  }

  .collection-edit:has(.portfolio-admin-category-workspace)
    .tabs-field__tabs
    > button.tabs-field__tab-button {
    min-height: 36px;
    padding: 0 12px !important;
  }
}

@media (prefers-reduced-motion: reduce) {
  .collection-edit:has(.portfolio-admin-category-workspace)
    .tabs-field__tabs
    > button.tabs-field__tab-button {
    transition: none;
  }
}

/* Portfolio Admin Experience - category tabs exact-dom:end */

/* Portfolio Admin Experience - tech stack editor:start */

.collection-edit:has(.portfolio-admin-technology-workspace) .document-fields {
  padding-top: 18px;
}

.portfolio-admin-technology-workspace {
  --technology-brand: var(--portfolio-admin-cyan);
  display: grid;
  grid-template-columns: minmax(0, 1.12fr) minmax(310px, 0.88fr);
  gap: 22px;
  margin-bottom: 22px;
  padding: 24px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 13px;
  background:
    radial-gradient(
      circle at 86% 18%,
      color-mix(in srgb, var(--technology-brand) 13%, transparent),
      transparent 31%
    ),
    linear-gradient(145deg, rgba(168, 103, 255, 0.04), transparent 54%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-technology-workspace__eyebrow {
  color: var(--technology-brand);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.portfolio-admin-technology-workspace__title-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 14px;
  margin-top: 10px;
}

.portfolio-admin-technology-workspace h2 {
  min-width: 0;
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(25px, 3vw, 36px);
  font-weight: 600;
  letter-spacing: -0.045em;
  line-height: 1.06;
}

.portfolio-admin-technology-workspace__statuses {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
}

.portfolio-admin-technology-status {
  --technology-status-tone: var(--portfolio-admin-cyan);
  display: inline-flex;
  min-height: 26px;
  align-items: center;
  gap: 7px;
  padding: 0 9px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

.portfolio-admin-technology-status.is-green {
  --technology-status-tone: var(--portfolio-admin-green);
}

.portfolio-admin-technology-status.is-purple {
  --technology-status-tone: var(--portfolio-admin-purple);
}

.portfolio-admin-technology-status.is-cyan {
  --technology-status-tone: var(--portfolio-admin-cyan);
}

.portfolio-admin-technology-status.is-active {
  border-color: color-mix(in srgb, var(--technology-status-tone) 34%, transparent);
  background: color-mix(in srgb, var(--technology-status-tone) 7%, transparent);
  color: var(--technology-status-tone);
}

.portfolio-admin-technology-status > i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: currentColor;
}

.portfolio-admin-technology-workspace__copy > code {
  display: block;
  width: max-content;
  max-width: 100%;
  overflow: hidden;
  margin-top: 10px;
  color: var(--portfolio-admin-text-soft);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-technology-workspace__copy > p {
  max-width: 680px;
  margin: 18px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 12px;
  line-height: 1.58;
}

.portfolio-admin-technology-workspace dl {
  display: flex;
  flex-wrap: wrap;
  gap: 9px;
  margin: 22px 0 0;
}

.portfolio-admin-technology-workspace dl > div {
  min-width: 112px;
  padding: 10px 12px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 9px;
  background: rgba(255, 255, 255, 0.018);
}

.portfolio-admin-technology-workspace dt {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 7px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-technology-workspace dd {
  overflow: hidden;
  margin: 6px 0 0;
  color: var(--portfolio-admin-text);
  font-size: 10px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-technology-workspace__links {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 18px;
}

.portfolio-admin-technology-workspace__links :is(a, span),
.portfolio-admin-technology-resources article :is(a, span.is-missing) {
  display: inline-flex;
  min-height: 30px;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 0 10px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  text-decoration: none;
}

.portfolio-admin-technology-workspace__links a,
.portfolio-admin-technology-resources article a {
  border-color: rgba(50, 215, 231, 0.26);
  color: var(--portfolio-admin-text);
}

.portfolio-admin-technology-workspace__links a:hover,
.portfolio-admin-technology-resources article a:hover {
  background: rgba(50, 215, 231, 0.055);
}

.portfolio-admin-technology-orbit {
  position: relative;
  display: grid;
  min-height: 254px;
  place-items: center;
  overflow: hidden;
  padding: 36px 24px;
  border: 1px solid color-mix(in srgb, var(--technology-brand) 32%, var(--portfolio-admin-border));
  border-radius: 12px;
  background:
    linear-gradient(
      145deg,
      color-mix(in srgb, var(--technology-brand) 8%, transparent),
      transparent 58%
    ),
    #07131d;
}

.portfolio-admin-technology-orbit > i {
  position: absolute;
  border: 1px solid color-mix(in srgb, var(--technology-brand) 38%, transparent);
  border-radius: 50%;
}

.portfolio-admin-technology-orbit > i.is-outer {
  width: 224px;
  height: 224px;
}

.portfolio-admin-technology-orbit > i.is-inner {
  width: 154px;
  height: 154px;
}

.portfolio-admin-technology-orbit::before,
.portfolio-admin-technology-orbit::after {
  position: absolute;
  content: '';
}

.portfolio-admin-technology-orbit::before {
  width: 1px;
  height: 100%;
  background: linear-gradient(
    transparent,
    color-mix(in srgb, var(--technology-brand) 50%, transparent),
    transparent
  );
}

.portfolio-admin-technology-orbit::after {
  width: 100%;
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent,
    color-mix(in srgb, var(--technology-brand) 50%, transparent),
    transparent
  );
}

.portfolio-admin-technology-orbit__core {
  position: relative;
  z-index: 2;
  display: grid;
  width: min(100%, 238px);
  min-height: 128px;
  place-items: center;
  padding: 18px;
  border: 1px solid color-mix(in srgb, var(--technology-brand) 52%, transparent);
  border-radius: 12px;
  background: rgba(5, 14, 22, 0.92);
  box-shadow:
    0 0 0 7px color-mix(in srgb, var(--technology-brand) 4%, transparent),
    0 22px 60px rgba(0, 0, 0, 0.3);
  text-align: center;
}

.portfolio-admin-technology-orbit__core > span {
  display: grid;
  width: 46px;
  height: 46px;
  place-items: center;
  border: 1px solid color-mix(in srgb, var(--technology-brand) 52%, transparent);
  border-radius: 12px;
  background: color-mix(in srgb, var(--technology-brand) 10%, transparent);
  color: var(--technology-brand);
  font-family: var(--font-mono), monospace;
  font-size: 13px;
  font-weight: 700;
}

.portfolio-admin-technology-orbit__core strong {
  display: block;
  max-width: 100%;
  overflow: hidden;
  margin-top: 11px;
  color: var(--portfolio-admin-text);
  font-size: 15px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-technology-orbit__core code {
  display: block;
  max-width: 100%;
  overflow: hidden;
  margin-top: 7px;
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-technology-orbit__label,
.portfolio-admin-technology-orbit__category {
  position: absolute;
  z-index: 3;
  max-width: calc(100% - 34px);
  overflow: hidden;
  font-family: var(--font-mono), monospace;
  font-size: 7px;
  letter-spacing: 0.09em;
  text-overflow: ellipsis;
  text-transform: uppercase;
  white-space: nowrap;
}

.portfolio-admin-technology-orbit__label {
  top: 16px;
  left: 17px;
  color: var(--technology-brand);
}

.portfolio-admin-technology-orbit__category {
  right: 17px;
  bottom: 16px;
  color: var(--portfolio-admin-text-muted);
}

.collection-edit:has(.portfolio-admin-technology-workspace) .tabs-field {
  width: 100%;
  overflow: visible;
  border: 0;
  border-radius: 0;
  background: transparent;
}

.collection-edit:has(.portfolio-admin-technology-workspace) .tabs-field__tabs {
  position: sticky;
  top: calc(var(--portfolio-admin-topbar-height) + 8px);
  z-index: 12;
  display: flex;
  width: 100%;
  min-height: 52px;
  box-sizing: border-box;
  align-items: center;
  gap: 6px;
  overflow-x: auto;
  margin: 0 0 24px;
  padding: 6px 8px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background: rgba(7, 16, 25, 0.94);
  box-shadow: 0 12px 26px rgba(0, 0, 0, 0.08);
  backdrop-filter: blur(16px);
  scrollbar-width: none;
}

.collection-edit:has(.portfolio-admin-technology-workspace) .tabs-field__tabs::-webkit-scrollbar {
  display: none;
}

.collection-edit:has(.portfolio-admin-technology-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button {
  display: inline-flex;
  min-height: 38px;
  box-sizing: border-box;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
  margin: 0;
  padding: 0 15px;
  border: 1px solid transparent;
  border-radius: 8px;
  appearance: none;
  background: transparent;
  color: var(--portfolio-admin-text-dim);
  font: inherit;
  font-size: 11px;
  font-weight: 600;
  line-height: 1;
  white-space: nowrap;
  box-shadow: none;
  cursor: pointer;
  transition:
    border-color 140ms ease,
    background-color 140ms ease,
    color 140ms ease;
}

.collection-edit:has(.portfolio-admin-technology-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button:hover {
  border-color: rgba(168, 103, 255, 0.14);
  background: rgba(255, 255, 255, 0.025);
  color: var(--portfolio-admin-text);
}

.collection-edit:has(.portfolio-admin-technology-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button.tabs-field__tab-button--active {
  border-color: rgba(168, 103, 255, 0.42);
  background: rgba(168, 103, 255, 0.08);
  color: var(--portfolio-admin-purple);
}

.collection-edit:has(.portfolio-admin-technology-workspace) .tabs-field__tab-panel {
  display: block;
  width: 100%;
  min-width: 0;
  box-sizing: border-box;
  padding: 0;
  border: 0;
  background: transparent;
}

.portfolio-admin-technology-classification,
.portfolio-admin-technology-resources {
  margin-top: 8px;
  padding: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background:
    linear-gradient(90deg, rgba(168, 103, 255, 0.035), transparent 58%), rgba(7, 17, 25, 0.56);
}

.portfolio-admin-technology-classification > header,
.portfolio-admin-technology-resources > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.portfolio-admin-technology-classification > header span,
.portfolio-admin-technology-classification > header strong,
.portfolio-admin-technology-resources > header span,
.portfolio-admin-technology-resources > header strong {
  display: block;
}

.portfolio-admin-technology-classification > header span,
.portfolio-admin-technology-resources > header span {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-technology-classification > header strong,
.portfolio-admin-technology-resources > header strong {
  margin-top: 5px;
  color: var(--portfolio-admin-text);
  font-size: 12px;
}

.portfolio-admin-technology-classification > header code,
.portfolio-admin-technology-resources > header code {
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

.portfolio-admin-technology-classification__path {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 17px;
  padding: 11px 12px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-soft);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

.portfolio-admin-technology-classification__path > span {
  color: var(--portfolio-admin-purple);
}

.portfolio-admin-technology-classification__path > i {
  color: var(--portfolio-admin-text-muted);
  font-style: normal;
}

.portfolio-admin-technology-classification__path > b {
  color: var(--portfolio-admin-text);
  font-weight: 650;
}

.portfolio-admin-technology-classification__states {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 14px;
}

.portfolio-admin-technology-resources__grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
  margin-top: 17px;
}

.portfolio-admin-technology-resources article {
  padding: 14px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 9px;
  background: rgba(255, 255, 255, 0.018);
}

.portfolio-admin-technology-resources article.is-ready {
  border-color: rgba(67, 221, 145, 0.24);
  background: rgba(67, 221, 145, 0.035);
}

.portfolio-admin-technology-resources article > span,
.portfolio-admin-technology-resources article > strong {
  display: block;
}

.portfolio-admin-technology-resources article > span {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.portfolio-admin-technology-resources article > strong {
  margin: 7px 0 12px;
  color: var(--portfolio-admin-text);
  font-size: 10px;
  line-height: 1.4;
}

.portfolio-admin-technology-readiness {
  --technology-readiness: 0%;
  overflow: hidden;
  margin-top: 24px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(145deg, rgba(168, 103, 255, 0.05), transparent 54%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-technology-readiness > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 18px;
}

.portfolio-admin-technology-readiness > header span,
.portfolio-admin-technology-readiness > header strong {
  display: block;
}

.portfolio-admin-technology-readiness > header span {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-technology-readiness > header strong {
  margin-top: 7px;
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 26px;
  letter-spacing: -0.05em;
}

.portfolio-admin-technology-readiness > header > i {
  position: relative;
  display: grid;
  width: 52px;
  height: 52px;
  place-items: center;
  border-radius: 50%;
  background: conic-gradient(
    var(--portfolio-admin-purple) var(--technology-readiness),
    rgba(255, 255, 255, 0.07) 0
  );
}

.portfolio-admin-technology-readiness > header > i::after {
  position: absolute;
  width: 42px;
  height: 42px;
  border-radius: 50%;
  background: #0b1822;
  content: '';
}

.portfolio-admin-technology-readiness > header > i > b {
  position: relative;
  z-index: 1;
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: var(--portfolio-admin-purple);
  box-shadow: 0 0 12px var(--portfolio-admin-purple);
}

.portfolio-admin-technology-readiness > p {
  margin: 0;
  padding: 0 18px 17px;
  color: var(--portfolio-admin-text-soft);
  font-size: 10px;
  line-height: 1.5;
}

.portfolio-admin-technology-readiness ul {
  margin: 0;
  padding: 0;
  border-top: 1px solid var(--portfolio-admin-border);
  list-style: none;
}

.portfolio-admin-technology-readiness li {
  display: grid;
  grid-template-columns: 26px minmax(0, 1fr);
  gap: 10px;
  padding: 13px 16px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-technology-readiness li:last-child {
  border-bottom: 0;
}

.portfolio-admin-technology-readiness li > span {
  display: grid;
  width: 24px;
  height: 24px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-technology-readiness li.is-complete > span {
  border-color: rgba(67, 221, 145, 0.3);
  background: rgba(67, 221, 145, 0.07);
  color: var(--portfolio-admin-green);
}

.portfolio-admin-technology-readiness li strong,
.portfolio-admin-technology-readiness li small {
  display: block;
}

.portfolio-admin-technology-readiness li strong {
  color: var(--portfolio-admin-text);
  font-size: 10px;
}

.portfolio-admin-technology-readiness li small {
  margin-top: 4px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
  line-height: 1.4;
}

@media (max-width: 980px) {
  .portfolio-admin-technology-workspace {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 760px) {
  .portfolio-admin-technology-workspace {
    padding: 18px;
  }

  .portfolio-admin-technology-workspace__title-row {
    align-items: flex-start;
    flex-direction: column;
  }

  .collection-edit:has(.portfolio-admin-technology-workspace) .tabs-field__tabs {
    top: calc(var(--portfolio-admin-topbar-height) + 5px);
    min-height: 48px;
    margin-bottom: 20px;
    padding: 5px;
  }

  .collection-edit:has(.portfolio-admin-technology-workspace)
    .tabs-field__tabs
    > button.tabs-field__tab-button {
    min-height: 36px;
    padding: 0 12px;
  }

  .portfolio-admin-technology-resources__grid {
    grid-template-columns: 1fr;
  }
}

@media (prefers-reduced-motion: reduce) {
  .collection-edit:has(.portfolio-admin-technology-workspace)
    .tabs-field__tabs
    > button.tabs-field__tab-button {
    transition: none;
  }
}

/* Portfolio Admin Experience - tech stack editor:end */

/* Portfolio Admin Experience - tech stack polish:start */

/*
 * Remove Payload's native active underline.
 * The active state is already represented by the purple button treatment.
 */
.collection-edit:has(.portfolio-admin-technology-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button::before,
.collection-edit:has(.portfolio-admin-technology-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button::after {
  display: none !important;
  width: 0 !important;
  height: 0 !important;
  border: 0 !important;
  background: transparent !important;
  box-shadow: none !important;
  content: none !important;
}

/*
 * Working resource links should read as interactive controls,
 * while missing destinations remain deliberately muted.
 */
.portfolio-admin-technology-workspace__links > a,
.portfolio-admin-technology-resources article > a {
  border-color: rgba(50, 215, 231, 0.34) !important;
  background: rgba(50, 215, 231, 0.035) !important;
  color: var(--portfolio-admin-text) !important;
  opacity: 1 !important;
}

.portfolio-admin-technology-workspace__links > a > span,
.portfolio-admin-technology-workspace__links > a > i,
.portfolio-admin-technology-resources article > a > span,
.portfolio-admin-technology-resources article > a > i {
  color: inherit !important;
  opacity: 1 !important;
}

.portfolio-admin-technology-workspace__links > a:hover,
.portfolio-admin-technology-resources article > a:hover {
  border-color: rgba(50, 215, 231, 0.52) !important;
  background: rgba(50, 215, 231, 0.075) !important;
  color: var(--portfolio-admin-cyan) !important;
}

.portfolio-admin-technology-workspace__links > span.is-missing,
.portfolio-admin-technology-resources article > span.is-missing {
  border-color: var(--portfolio-admin-border) !important;
  background: rgba(255, 255, 255, 0.012) !important;
  color: var(--portfolio-admin-text-muted) !important;
  opacity: 0.62;
}

/* Portfolio Admin Experience - tech stack polish:end */

/* Portfolio Admin Experience - project versions list:start */

.template-default__wrap:has(.portfolio-admin-release-overview) .collection-list .list-header__title,
.template-default__wrap:has(.portfolio-admin-release-overview) .collection-list .list-header > h1 {
  display: none !important;
}

.template-default__wrap:has(.portfolio-admin-release-overview) .collection-list .list-header {
  margin-bottom: 12px;
  padding-top: 0;
}

.portfolio-admin-release-overview {
  margin-bottom: 22px;
}

.portfolio-admin-release-overview__header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 20px;
}

.portfolio-admin-release-overview__eyebrow {
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.portfolio-admin-release-overview h1 {
  margin: 9px 0 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(28px, 3vw, 38px);
  font-weight: 600;
  letter-spacing: -0.045em;
  line-height: 1.05;
}

.portfolio-admin-release-overview__header p {
  max-width: 760px;
  margin: 9px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 13px;
  line-height: 1.5;
}

.portfolio-admin-release-overview__metrics {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
}

.portfolio-admin-release-metric {
  --release-tone: var(--portfolio-admin-cyan);
  position: relative;
  min-height: 116px;
  overflow: hidden;
  padding: 16px 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-top: 2px solid var(--release-tone);
  border-radius: 12px;
  background:
    linear-gradient(
      145deg,
      color-mix(in srgb, var(--release-tone) 7%, transparent),
      transparent 58%
    ),
    var(--portfolio-admin-surface);
}

.portfolio-admin-release-metric.is-purple {
  --release-tone: var(--portfolio-admin-purple);
}

.portfolio-admin-release-metric.is-green {
  --release-tone: var(--portfolio-admin-green);
}

.portfolio-admin-release-metric.is-amber {
  --release-tone: var(--portfolio-admin-amber);
}

.portfolio-admin-release-metric > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.portfolio-admin-release-metric > header span {
  color: var(--portfolio-admin-text-muted);
  font-size: 9px;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-release-metric > header code {
  color: var(--release-tone);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
}

.portfolio-admin-release-metric > strong {
  display: block;
  margin-top: 12px;
  color: var(--release-tone);
  font-family: var(--font-mono), monospace;
  font-size: 28px;
  letter-spacing: -0.05em;
}

.portfolio-admin-release-metric > small {
  display: block;
  margin-top: 7px;
  color: var(--portfolio-admin-text-soft);
  font-size: 10px;
}

.portfolio-admin-release-metric > i {
  position: absolute;
  right: -25px;
  bottom: -40px;
  width: 122px;
  height: 84px;
  border: 1px solid var(--release-tone);
  border-radius: 50%;
  opacity: 0.14;
}

.portfolio-admin-release-cell {
  display: grid;
  min-width: 0;
  grid-template-columns: 48px minmax(0, 1fr) auto;
  align-items: center;
  gap: 12px;
  color: var(--portfolio-admin-text);
  text-decoration: none;
}

.portfolio-admin-release-cell__mark {
  display: grid;
  width: 46px;
  height: 38px;
  place-items: center;
  overflow: hidden;
  border: 1px solid rgba(168, 103, 255, 0.34);
  border-radius: 9px;
  background: rgba(168, 103, 255, 0.08);
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
}

.portfolio-admin-release-cell__copy {
  min-width: 0;
}

.portfolio-admin-release-cell__copy strong,
.portfolio-admin-release-cell__copy small {
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-release-cell__copy strong {
  font-size: 11px;
  font-weight: 650;
}

.portfolio-admin-release-cell__copy strong code {
  margin-right: 8px;
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

.portfolio-admin-release-cell__copy small {
  margin-top: 4px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
}

.portfolio-admin-release-cell__arrow {
  color: var(--portfolio-admin-text-muted);
  opacity: 0;
  transform: translateX(-4px);
  transition:
    opacity 130ms ease,
    transform 130ms ease;
}

.portfolio-admin-release-cell:hover .portfolio-admin-release-cell__arrow {
  opacity: 1;
  transform: translateX(0);
}

.portfolio-admin-release-project,
.portfolio-admin-release-date,
.portfolio-admin-release-breaking {
  display: inline-flex;
  min-width: 0;
  max-width: 100%;
  min-height: 26px;
  align-items: center;
  gap: 7px;
  overflow: hidden;
  padding: 0 9px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-soft);
  font-size: 8px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-release-project > i {
  width: 8px;
  height: 8px;
  border: 1px solid rgba(50, 215, 231, 0.36);
  border-radius: 2px;
  transform: rotate(45deg);
}

.portfolio-admin-release-date > i {
  width: 7px;
  height: 7px;
  border: 1px solid rgba(50, 215, 231, 0.4);
  border-radius: 50%;
}

.portfolio-admin-release-project.is-missing,
.portfolio-admin-release-date.is-missing {
  color: var(--portfolio-admin-text-muted);
  opacity: 0.72;
}

.portfolio-admin-release-badge {
  --release-badge-tone: var(--portfolio-admin-text-muted);
  display: inline-flex;
  min-height: 25px;
  align-items: center;
  gap: 7px;
  padding: 0 9px;
  border: 1px solid color-mix(in srgb, var(--release-badge-tone) 28%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--release-badge-tone) 6%, transparent);
  color: var(--release-badge-tone);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  opacity: 0.72;
}

.portfolio-admin-release-badge.is-purple.is-active {
  --release-badge-tone: var(--portfolio-admin-purple);
  opacity: 1;
}

.portfolio-admin-release-badge.is-green.is-active {
  --release-badge-tone: var(--portfolio-admin-green);
  opacity: 1;
}

.portfolio-admin-release-badge > i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: currentColor;
}

.portfolio-admin-release-breaking > i {
  display: grid;
  width: 17px;
  height: 17px;
  place-items: center;
  border: 1px solid rgba(67, 221, 145, 0.26);
  border-radius: 5px;
  color: var(--portfolio-admin-green);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-style: normal;
}

.portfolio-admin-release-breaking.has-breaking {
  border-color: rgba(245, 183, 66, 0.28);
  background: rgba(245, 183, 66, 0.04);
  color: var(--portfolio-admin-amber);
}

.portfolio-admin-release-breaking.has-breaking > i {
  border-color: rgba(245, 183, 66, 0.34);
  color: var(--portfolio-admin-amber);
}

@media (max-width: 1100px) {
  .portfolio-admin-release-overview__metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 680px) {
  .portfolio-admin-release-overview__header {
    align-items: stretch;
    flex-direction: column;
  }

  .portfolio-admin-release-overview__metrics {
    grid-template-columns: 1fr;
  }
}

/* Portfolio Admin Experience - project versions list:end */

/* Portfolio Admin Experience - project versions header geometry v2:start */

/*
 * Keep the Release Registry aligned with the collection list horizontally
 * and give it breathing room below the admin topbar.
 */
.template-default__wrap:has(.portfolio-admin-release-overview) .portfolio-admin-release-overview {
  width: auto !important;
  max-width: none !important;
  box-sizing: border-box;
  margin-top: 28px !important;
  margin-right: var(--gutter-h) !important;
  margin-left: var(--gutter-h) !important;
}

@media (max-width: 760px) {
  .template-default__wrap:has(.portfolio-admin-release-overview) .portfolio-admin-release-overview {
    margin-top: 20px !important;
    margin-right: var(--gutter-h) !important;
    margin-left: var(--gutter-h) !important;
  }
}

/* Portfolio Admin Experience - project versions header geometry v2:end */

/* Portfolio Admin Experience - project version editor:start */

/* The custom Release Registry header already owns the primary create action. */
.template-default__wrap:has(.portfolio-admin-release-overview) .collection-list .list-header .btn {
  display: none !important;
}

.collection-edit:has(.portfolio-admin-version-workspace) .document-fields {
  padding-top: 18px;
}

.portfolio-admin-version-workspace {
  display: grid;
  grid-template-columns: minmax(0, 1.12fr) minmax(320px, 0.88fr);
  gap: 22px;
  margin-bottom: 22px;
  padding: 24px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 13px;
  background:
    radial-gradient(circle at 88% 18%, rgba(168, 103, 255, 0.1), transparent 30%),
    linear-gradient(145deg, rgba(50, 215, 231, 0.035), transparent 52%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-version-workspace__eyebrow {
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.portfolio-admin-version-workspace__title-row {
  display: flex;
  align-items: baseline;
  flex-wrap: wrap;
  gap: 12px;
  margin-top: 10px;
}

.portfolio-admin-version-workspace__title-row > code {
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 14px;
  font-weight: 700;
}

.portfolio-admin-version-workspace h2 {
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(25px, 3vw, 36px);
  font-weight: 600;
  letter-spacing: -0.045em;
  line-height: 1.06;
}

.portfolio-admin-version-workspace__statuses {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  margin-top: 16px;
}

.portfolio-admin-version-status {
  --version-status-tone: var(--portfolio-admin-text-muted);
  display: inline-flex;
  min-height: 26px;
  align-items: center;
  gap: 7px;
  padding: 0 9px;
  border: 1px solid color-mix(in srgb, var(--version-status-tone) 26%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--version-status-tone) 5%, transparent);
  color: var(--version-status-tone);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  opacity: 0.7;
}

.portfolio-admin-version-status.is-purple.is-active {
  --version-status-tone: var(--portfolio-admin-purple);
  opacity: 1;
}

.portfolio-admin-version-status.is-green.is-active {
  --version-status-tone: var(--portfolio-admin-green);
  opacity: 1;
}

.portfolio-admin-version-status.is-amber.is-active {
  --version-status-tone: var(--portfolio-admin-amber);
  opacity: 1;
}

.portfolio-admin-version-status > i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: currentColor;
}

.portfolio-admin-version-workspace__copy > p {
  max-width: 700px;
  margin: 18px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 12px;
  line-height: 1.6;
}

.portfolio-admin-version-workspace dl {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 9px;
  margin: 22px 0 0;
}

.portfolio-admin-version-workspace dl > div {
  min-width: 0;
  padding: 11px 12px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 9px;
  background: rgba(255, 255, 255, 0.018);
}

.portfolio-admin-version-workspace dt {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 7px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-version-workspace dd {
  overflow: hidden;
  margin: 6px 0 0;
  color: var(--portfolio-admin-text);
  font-size: 10px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-version-orbit {
  position: relative;
  display: grid;
  min-height: 270px;
  place-items: center;
  overflow: hidden;
  padding: 42px 24px 54px;
  border: 1px solid rgba(168, 103, 255, 0.34);
  border-radius: 12px;
  background: linear-gradient(145deg, rgba(168, 103, 255, 0.075), transparent 58%), #07131d;
}

.portfolio-admin-version-orbit > i {
  position: absolute;
  border: 1px solid rgba(168, 103, 255, 0.28);
  border-radius: 50%;
}

.portfolio-admin-version-orbit > i.is-outer {
  width: 232px;
  height: 232px;
}

.portfolio-admin-version-orbit > i.is-inner {
  width: 160px;
  height: 160px;
}

.portfolio-admin-version-orbit::before,
.portfolio-admin-version-orbit::after {
  position: absolute;
  content: '';
}

.portfolio-admin-version-orbit::before {
  width: 1px;
  height: 100%;
  background: linear-gradient(transparent, rgba(168, 103, 255, 0.34), transparent);
}

.portfolio-admin-version-orbit::after {
  width: 100%;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(168, 103, 255, 0.34), transparent);
}

.portfolio-admin-version-orbit__label {
  position: absolute;
  top: 16px;
  left: 17px;
  z-index: 3;
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 7px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-version-orbit__core {
  position: relative;
  z-index: 2;
  display: grid;
  width: min(100%, 248px);
  min-height: 132px;
  place-items: center;
  padding: 18px;
  border: 1px solid rgba(168, 103, 255, 0.48);
  border-radius: 12px;
  background: rgba(5, 14, 22, 0.94);
  box-shadow:
    0 0 0 7px rgba(168, 103, 255, 0.035),
    0 22px 60px rgba(0, 0, 0, 0.3);
  text-align: center;
}

.portfolio-admin-version-orbit__core > span {
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 17px;
  font-weight: 700;
}

.portfolio-admin-version-orbit__core > strong,
.portfolio-admin-version-orbit__core > small {
  display: block;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-version-orbit__core > strong {
  margin-top: 10px;
  color: var(--portfolio-admin-text);
  font-size: 15px;
}

.portfolio-admin-version-orbit__core > small {
  margin-top: 7px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
}

.portfolio-admin-version-orbit__timeline {
  position: absolute;
  right: 18px;
  bottom: 17px;
  left: 18px;
  z-index: 3;
  display: grid;
  grid-template-columns: auto 1fr auto 1fr auto;
  align-items: center;
  gap: 7px;
}

.portfolio-admin-version-orbit__timeline > span {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 7px;
  text-transform: uppercase;
}

.portfolio-admin-version-orbit__timeline > span.is-complete {
  color: var(--portfolio-admin-green);
}

.portfolio-admin-version-orbit__timeline > span.has-risk {
  color: var(--portfolio-admin-amber);
}

.portfolio-admin-version-orbit__timeline > i {
  height: 1px;
  background: var(--portfolio-admin-border-strong);
}

.collection-edit:has(.portfolio-admin-version-workspace) .tabs-field {
  width: 100%;
  overflow: visible;
  border: 0;
  background: transparent;
}

.collection-edit:has(.portfolio-admin-version-workspace) .tabs-field__tabs {
  position: sticky;
  top: calc(var(--portfolio-admin-topbar-height) + 8px);
  z-index: 12;
  display: flex;
  width: 100%;
  min-height: 52px;
  box-sizing: border-box;
  align-items: center;
  gap: 6px;
  overflow-x: auto;
  margin: 0 0 24px;
  padding: 6px 8px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background: rgba(7, 16, 25, 0.94);
  backdrop-filter: blur(16px);
  scrollbar-width: none;
}

.collection-edit:has(.portfolio-admin-version-workspace) .tabs-field__tabs::-webkit-scrollbar {
  display: none;
}

.collection-edit:has(.portfolio-admin-version-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button {
  display: inline-flex;
  min-height: 38px;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
  margin: 0;
  padding: 0 15px;
  border: 1px solid transparent;
  border-radius: 8px;
  appearance: none;
  background: transparent;
  color: var(--portfolio-admin-text-dim);
  font: inherit;
  font-size: 11px;
  font-weight: 600;
  box-shadow: none;
  cursor: pointer;
}

.collection-edit:has(.portfolio-admin-version-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button.tabs-field__tab-button--active {
  border-color: rgba(168, 103, 255, 0.42);
  background: rgba(168, 103, 255, 0.08);
  color: var(--portfolio-admin-purple);
}

.collection-edit:has(.portfolio-admin-version-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button::before,
.collection-edit:has(.portfolio-admin-version-workspace)
  .tabs-field__tabs
  > button.tabs-field__tab-button::after {
  display: none !important;
  width: 0 !important;
  height: 0 !important;
  content: none !important;
}

.collection-edit:has(.portfolio-admin-version-workspace) .tabs-field__tab-panel {
  display: block;
  width: 100%;
  min-width: 0;
  padding: 0;
  border: 0;
  background: transparent;
}

.portfolio-admin-version-summary {
  margin-top: 18px;
  padding: 18px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 11px;
  background:
    linear-gradient(90deg, rgba(50, 215, 231, 0.035), transparent 58%), rgba(7, 17, 25, 0.56);
}

.portfolio-admin-version-summary.is-risk.has-risk {
  border-color: rgba(245, 183, 66, 0.26);
  background:
    linear-gradient(90deg, rgba(245, 183, 66, 0.045), transparent 58%), rgba(7, 17, 25, 0.56);
}

.portfolio-admin-version-summary > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.portfolio-admin-version-summary > header span,
.portfolio-admin-version-summary > header strong {
  display: block;
}

.portfolio-admin-version-summary > header span {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.portfolio-admin-version-summary > header strong {
  margin-top: 5px;
  color: var(--portfolio-admin-text);
  font-size: 12px;
}

.portfolio-admin-version-summary > header code {
  color: var(--portfolio-admin-cyan);
  font-family: var(--font-mono), monospace;
  font-size: 9px;
}

.portfolio-admin-version-summary.is-risk.has-risk > header code {
  color: var(--portfolio-admin-amber);
}

.portfolio-admin-version-summary > p {
  margin: 14px 0 0;
  color: var(--portfolio-admin-text-muted);
  font-size: 10px;
  line-height: 1.55;
}

.portfolio-admin-version-readiness {
  --version-readiness: 0%;
  overflow: hidden;
  margin-top: 24px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 12px;
  background:
    linear-gradient(145deg, rgba(168, 103, 255, 0.05), transparent 54%),
    var(--portfolio-admin-surface);
}

.portfolio-admin-version-readiness > header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 18px;
}

.portfolio-admin-version-readiness > header span,
.portfolio-admin-version-readiness > header strong {
  display: block;
}

.portfolio-admin-version-readiness > header span {
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.portfolio-admin-version-readiness > header strong {
  margin-top: 7px;
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 26px;
}

.portfolio-admin-version-readiness > header > i {
  position: relative;
  display: grid;
  width: 52px;
  height: 52px;
  place-items: center;
  border-radius: 50%;
  background: conic-gradient(
    var(--portfolio-admin-purple) var(--version-readiness),
    rgba(255, 255, 255, 0.07) 0
  );
}

.portfolio-admin-version-readiness > header > i::after {
  position: absolute;
  width: 42px;
  height: 42px;
  border-radius: 50%;
  background: #0b1822;
  content: '';
}

.portfolio-admin-version-readiness > header > i > b {
  position: relative;
  z-index: 1;
  width: 4px;
  height: 4px;
  border-radius: 50%;
  background: var(--portfolio-admin-purple);
}

.portfolio-admin-version-readiness > p {
  margin: 0;
  padding: 0 18px 17px;
  color: var(--portfolio-admin-text-soft);
  font-size: 10px;
}

.portfolio-admin-version-readiness ul {
  margin: 0;
  padding: 0;
  border-top: 1px solid var(--portfolio-admin-border);
  list-style: none;
}

.portfolio-admin-version-readiness li {
  display: grid;
  grid-template-columns: 26px minmax(0, 1fr);
  gap: 10px;
  padding: 13px 16px;
  border-bottom: 1px solid var(--portfolio-admin-border);
}

.portfolio-admin-version-readiness li:last-child {
  border-bottom: 0;
}

.portfolio-admin-version-readiness li > span {
  display: grid;
  width: 24px;
  height: 24px;
  place-items: center;
  border: 1px solid var(--portfolio-admin-border-strong);
  border-radius: 7px;
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 10px;
}

.portfolio-admin-version-readiness li.is-complete > span {
  border-color: rgba(67, 221, 145, 0.3);
  background: rgba(67, 221, 145, 0.07);
  color: var(--portfolio-admin-green);
}

.portfolio-admin-version-readiness li strong,
.portfolio-admin-version-readiness li small {
  display: block;
}

.portfolio-admin-version-readiness li strong {
  color: var(--portfolio-admin-text);
  font-size: 10px;
}

.portfolio-admin-version-readiness li small {
  margin-top: 4px;
  color: var(--portfolio-admin-text-muted);
  font-size: 8px;
}

@media (max-width: 980px) {
  .portfolio-admin-version-workspace {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 760px) {
  .portfolio-admin-version-workspace {
    padding: 18px;
  }

  .portfolio-admin-version-workspace dl {
    grid-template-columns: 1fr;
  }

  .collection-edit:has(.portfolio-admin-version-workspace) .tabs-field__tabs {
    top: calc(var(--portfolio-admin-topbar-height) + 5px);
    min-height: 48px;
    margin-bottom: 20px;
    padding: 5px;
  }
}

/* Portfolio Admin Experience - project version editor:end */

/* Portfolio Admin Experience - project version header:start */

.portfolio-admin-version-header-marker {
  display: none !important;
}

.portfolio-admin-version-header-host {
  min-width: 0;
  flex: 1 1 auto;
}

.portfolio-admin-version-header {
  display: flex;
  width: 100%;
  min-width: 0;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
  padding: 3px 0 5px;
}

.portfolio-admin-version-header__identity {
  min-width: 0;
}

.portfolio-admin-version-header__eyebrow {
  display: block;
  color: var(--portfolio-admin-purple);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-weight: 700;
  letter-spacing: 0.15em;
  text-transform: uppercase;
}

.portfolio-admin-version-header__title-row {
  display: flex;
  min-width: 0;
  align-items: center;
  flex-wrap: wrap;
  gap: 11px;
  margin-top: 6px;
}

.portfolio-admin-version-header h1 {
  max-width: min(720px, 62vw);
  overflow: hidden;
  margin: 0;
  color: var(--portfolio-admin-text);
  font-size: clamp(22px, 2.6vw, 32px);
  font-weight: 600;
  letter-spacing: -0.042em;
  line-height: 1.08;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-version-header__project {
  display: inline-flex;
  min-height: 25px;
  max-width: 220px;
  align-items: center;
  overflow: hidden;
  padding: 0 9px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 7px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.portfolio-admin-version-header__signals {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  flex-wrap: wrap;
  gap: 7px;
  flex: 0 0 auto;
}

.portfolio-admin-version-header__badge {
  --version-header-tone: var(--portfolio-admin-text-muted);
  display: inline-flex;
  min-height: 28px;
  align-items: center;
  gap: 7px;
  padding: 0 9px;
  border: 1px solid color-mix(in srgb, var(--version-header-tone) 24%, transparent);
  border-radius: 8px;
  background: color-mix(in srgb, var(--version-header-tone) 5%, transparent);
  color: var(--version-header-tone);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.045em;
  text-transform: uppercase;
  opacity: 0.68;
}

.portfolio-admin-version-header__badge.is-purple.is-active {
  --version-header-tone: var(--portfolio-admin-purple);
  opacity: 1;
}

.portfolio-admin-version-header__badge.is-green.is-active {
  --version-header-tone: var(--portfolio-admin-green);
  opacity: 1;
}

.portfolio-admin-version-header__badge.is-amber.is-active {
  --version-header-tone: var(--portfolio-admin-amber);
  opacity: 1;
}

.portfolio-admin-version-header__badge > i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: currentColor;
}

.portfolio-admin-version-header__signals > time {
  display: inline-flex;
  min-height: 28px;
  align-items: center;
  padding: 0 9px;
  border: 1px solid var(--portfolio-admin-border);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.018);
  color: var(--portfolio-admin-text-muted);
  font-family: var(--font-mono), monospace;
  font-size: 8px;
  white-space: nowrap;
}

.collection-edit:has(.portfolio-admin-version-header-marker) .portfolio-admin-version-command {
  display: none !important;
}

@media (max-width: 1180px) {
  .portfolio-admin-version-header {
    align-items: flex-start;
    flex-direction: column;
    gap: 12px;
  }

  .portfolio-admin-version-header__signals {
    justify-content: flex-start;
  }

  .portfolio-admin-version-header h1 {
    max-width: 76vw;
  }
}

@media (max-width: 760px) {
  .portfolio-admin-version-header h1 {
    max-width: 82vw;
    font-size: 22px;
  }

  .portfolio-admin-version-header__project,
  .portfolio-admin-version-header__signals > time {
    display: none;
  }

  .portfolio-admin-version-header__signals {
    gap: 5px;
  }

  .portfolio-admin-version-header__badge {
    min-height: 26px;
    padding: 0 8px;
    font-size: 7px;
  }
}

/* Portfolio Admin Experience - project version header:end */

/* Portfolio Admin Experience - project version layout repair:start */

/*
 * Final editor geometry matching the approved mockup:
 * command header -> tabs shell -> full-width field panel + readiness sidebar.
 */
.collection-edit:has(.portfolio-admin-version-header-marker) .portfolio-admin-version-workspace {
  display: none !important;
}

.collection-edit:has(.portfolio-admin-version-header-marker) .document-fields {
  padding-top: 28px !important;
}

/*
 * Payload places the tab field inside an additional content gutter.
 * Neutralize that inner gutter so the tab shell uses the complete main column.
 */
.collection-edit:has(.portfolio-admin-version-header-marker) .document-fields > .tabs-field,
.collection-edit:has(.portfolio-admin-version-header-marker)
  .document-fields
  .field-type.tabs-field {
  width: 100% !important;
  max-width: none !important;
  box-sizing: border-box;
  margin: 0 !important;
  padding: 0 !important;
}

/*
 * Tabs are a single framed editor shell, not a detached narrow toolbar.
 */
.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tabs {
  position: static !important;
  top: auto !important;
  width: 100% !important;
  min-height: 64px;
  box-sizing: border-box;
  align-items: stretch;
  gap: 8px;
  margin: 0 !important;
  padding: 0 18px !important;
  overflow-x: auto;
  border: 1px solid var(--portfolio-admin-border);
  border-bottom-color: var(--portfolio-admin-border-strong);
  border-radius: 12px 12px 0 0;
  background: rgba(7, 16, 25, 0.78);
  box-shadow: none;
  backdrop-filter: none;
}

.collection-edit:has(.portfolio-admin-version-header-marker)
  .tabs-field__tabs
  > button.tabs-field__tab-button {
  position: relative;
  min-height: 63px;
  padding: 0 18px;
  border: 0 !important;
  border-radius: 0;
  background: transparent !important;
}

.collection-edit:has(.portfolio-admin-version-header-marker)
  .tabs-field__tabs
  > button.tabs-field__tab-button.tabs-field__tab-button--active {
  color: var(--portfolio-admin-purple);
}

.collection-edit:has(.portfolio-admin-version-header-marker)
  .tabs-field__tabs
  > button.tabs-field__tab-button.tabs-field__tab-button--active::after {
  position: absolute !important;
  right: 0 !important;
  bottom: 0 !important;
  left: 0 !important;
  display: block !important;
  width: auto !important;
  height: 2px !important;
  border: 0 !important;
  background: var(--portfolio-admin-purple) !important;
  box-shadow: 0 0 12px rgba(168, 103, 255, 0.32) !important;
  content: '' !important;
}

/*
 * Match the form body to the same left and right edges as the tab frame.
 * This removes the oversized right gutter visible in the broken layout.
 */
.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel {
  width: 100% !important;
  max-width: none !important;
  min-width: 0;
  box-sizing: border-box;
  margin: 0 !important;
  padding: 28px 22px 30px !important;
  border: 1px solid var(--portfolio-admin-border);
  border-top: 0;
  border-radius: 0 0 12px 12px;
  background:
    linear-gradient(145deg, rgba(168, 103, 255, 0.025), transparent 55%), rgba(12, 25, 36, 0.72);
}

.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel > *,
.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel .field-type,
.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel .render-fields {
  width: 100%;
  max-width: none !important;
  box-sizing: border-box;
}

/*
 * Keep Payload row widths, but remove inherited max-width and right spacing
 * that made Version/Title and Summary stop too early.
 */
.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel .field-type.row,
.collection-edit:has(.portfolio-admin-version-header-marker)
  .tabs-field__tab-panel
  .render-fields
  > .row {
  width: 100% !important;
  max-width: none !important;
  margin-right: 0 !important;
}

.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel input,
.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel textarea,
.collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel .react-select {
  max-width: none !important;
}

/*
 * The main editor column must consume all available space before the sidebar.
 */
.collection-edit:has(.portfolio-admin-version-header-marker) .document-fields__edit,
.collection-edit:has(.portfolio-admin-version-header-marker) .document-fields__main {
  min-width: 0;
  width: 100%;
  max-width: none !important;
}

/* The readiness card begins at the same vertical level as the tabs shell. */
.collection-edit:has(.portfolio-admin-version-header-marker) .document-fields__sidebar {
  padding-top: 28px !important;
}

.collection-edit:has(.portfolio-admin-version-header-marker) .portfolio-admin-version-readiness {
  margin-top: 0 !important;
}

@media (max-width: 980px) {
  .collection-edit:has(.portfolio-admin-version-header-marker) .document-fields {
    padding-top: 20px !important;
  }

  .collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tabs {
    min-height: 56px;
    padding: 0 10px !important;
  }

  .collection-edit:has(.portfolio-admin-version-header-marker)
    .tabs-field__tabs
    > button.tabs-field__tab-button {
    min-height: 55px;
    padding: 0 13px;
  }

  .collection-edit:has(.portfolio-admin-version-header-marker) .tabs-field__tab-panel {
    padding: 22px 16px 24px !important;
  }

  .collection-edit:has(.portfolio-admin-version-header-marker) .document-fields__sidebar {
    padding-top: 20px !important;
  }
}

/* Portfolio Admin Experience - project version layout repair:end */

====================================================================================================
FILE: src/collections/BlogPosts.ts
====================================================================================================
import type { CollectionConfig } from 'payload'
import { authenticatedAccess, publicAccess } from '@/access'

import { seoField, slugField } from '@/fields'
import { formatSlug } from '@/utils/formatSlug'

export const BlogPosts: CollectionConfig = {
  slug: 'blog-posts',
  // portfolio-admin-blog-posts-list-v1
  admin: {
    useAsTitle: 'title',
    defaultColumns: ['title', 'status', 'category', 'publishedAt'],
    listSearchableFields: ['title', 'slug', 'excerpt'],
    components: {
      beforeList: ['./components/admin/blog-posts/BlogPostsListHeader'],
      edit: {
        beforeDocumentControls: ['./components/admin/blog-posts/ArticleDocumentControls'],
      },
    },
  },
  access: {
    read: publicAccess,
    create: authenticatedAccess,
    update: authenticatedAccess,
    delete: authenticatedAccess,
  },
  fields: [
    // portfolio-admin-blog-post-editor-v1
    {
      type: 'tabs',
      tabs: [
        {
          label: 'Content',
          description: 'Article identity, summary, and long-form editorial body.',
          fields: [
            {
              name: 'title',
              type: 'text',
              required: true,
              admin: {
                components: {
                  Cell: './components/admin/blog-posts/BlogPostCells#BlogPostTitleCell',
                },
              },
            },
            slugField({ sourceField: 'title' }),
            {
              name: 'excerpt',
              type: 'textarea',
              required: true,
              admin: {
                description: 'Short summary for blog cards, previews, and SEO snippets.',
              },
            },
            {
              name: 'content',
              type: 'richText',
              required: true,
            },
          ],
        },
        {
          label: 'Media',
          description: 'Primary editorial imagery.',
          fields: [
            {
              name: 'coverImage',
              type: 'upload',
              relationTo: 'media',
            },
          ],
        },
        {
          label: 'Taxonomy',
          description: 'Category and article tags.',
          fields: [
            {
              name: 'category',
              type: 'relationship',
              relationTo: 'categories',
              hasMany: false,
              admin: {
                description: 'Use categories with type "Blog" or "Shared".',
                components: {
                  Cell: './components/admin/blog-posts/BlogPostCells#BlogPostCategoryCell',
                },
              },
            },
            {
              name: 'tags',
              type: 'array',
              fields: [
                {
                  name: 'label',
                  type: 'text',
                  required: true,
                },
                {
                  name: 'slug',
                  type: 'text',
                  required: true,
                  admin: {
                    description:
                      'URL-friendly tag identifier. Auto-generated from label when empty.',
                  },
                  hooks: {
                    beforeValidate: [
                      ({ value, siblingData }) => {
                        if (typeof value === 'string' && value.length > 0) {
                          return formatSlug(value)
                        }

                        if (typeof siblingData?.label === 'string') {
                          return formatSlug(siblingData.label)
                        }

                        return value
                      },
                    ],
                  },
                },
              ],
            },
          ],
        },
        {
          label: 'Relationships',
          description: 'Project context connected to this article.',
          fields: [
            {
              name: 'relatedProject',
              type: 'relationship',
              relationTo: 'projects',
              hasMany: false,
              admin: {
                description: 'Optional project connected to this article or dev note.',
              },
            },
          ],
        },
        {
          label: 'SEO',
          description: 'Search and social-sharing metadata.',
          fields: [seoField()],
        },
      ],
    },
    {
      name: 'articleReadiness',
      type: 'ui',
      admin: {
        position: 'sidebar',
        components: {
          Field: './components/admin/blog-posts/ArticleReadinessPanel',
        },
      },
    },
    {
      name: 'status',
      type: 'select',
      required: true,
      defaultValue: 'draft',
      options: [
        {
          label: 'Draft',
          value: 'draft',
        },
        {
          label: 'Published',
          value: 'published',
        },
        {
          label: 'Archived',
          value: 'archived',
        },
      ],
      admin: {
        position: 'sidebar',
        components: {
          Cell: './components/admin/blog-posts/BlogPostCells#BlogPostStatusCell',
        },
      },
    },
    {
      name: 'publishedAt',
      type: 'date',
      admin: {
        position: 'sidebar',
        date: {
          pickerAppearance: 'dayAndTime',
        },
        components: {
          Cell: './components/admin/blog-posts/BlogPostCells#BlogPostPublishedCell',
        },
      },
    },
    {
      name: 'author',
      type: 'relationship',
      relationTo: 'users',
      hasMany: false,
      admin: {
        position: 'sidebar',
      },
    },
  ],
}

====================================================================================================
FILE: src/collections/Categories.ts
====================================================================================================
import type { CollectionConfig } from 'payload'

import { authenticatedAccess, publicAccess } from '@/access'
import { slugField } from '@/fields'

export const Categories: CollectionConfig = {
  slug: 'categories',
  admin: {
    useAsTitle: 'title',
    defaultColumns: ['title', 'type', 'parent', 'sortOrder'],
    listSearchableFields: ['title', 'slug', 'description', 'type'],
    components: {
      beforeList: ['./components/admin/taxonomy/CategoriesListHeader'],
    },
  },
  access: {
    read: publicAccess,
    create: authenticatedAccess,
    update: authenticatedAccess,
    delete: authenticatedAccess,
  },
  fields: [
    {
      name: 'categoryWorkspace',
      type: 'ui',
      admin: {
        components: {
          Field: './components/admin/taxonomy/CategoryEditor#CategoryWorkspace',
        },
      },
    },
    {
      type: 'tabs',
      tabs: [
        {
          label: 'Identity',
          fields: [
            {
              name: 'title',
              type: 'text',
              required: true,
              admin: {
                components: {
                  Cell: './components/admin/taxonomy/CategoryCells#CategoryTitleCell',
                },
              },
            },
            slugField({ sourceField: 'title' }),
            {
              name: 'description',
              type: 'textarea',
              admin: {
                description: 'Explain what belongs in this category and where it is used.',
              },
            },
          ],
        },
        {
          label: 'Classification',
          fields: [
            {
              type: 'row',
              fields: [
                {
                  name: 'type',
                  type: 'select',
                  required: true,
                  defaultValue: 'shared',
                  options: [
                    { label: 'Project', value: 'project' },
                    { label: 'Blog', value: 'blog' },
                    { label: 'Tech Stack', value: 'tech-stack' },
                    { label: 'Shared', value: 'shared' },
                  ],
                  admin: {
                    width: '50%',
                    components: {
                      Cell: './components/admin/taxonomy/CategoryCells#CategoryTypeCell',
                    },
                  },
                },
                {
                  name: 'parent',
                  type: 'relationship',
                  relationTo: 'categories',
                  hasMany: false,
                  admin: {
                    width: '50%',
                    description: 'Optional parent category. Leave empty for a root node.',
                    components: {
                      Cell: './components/admin/taxonomy/CategoryCells#CategoryParentCell',
                    },
                  },
                },
              ],
            },
            {
              name: 'categoryHierarchy',
              type: 'ui',
              admin: {
                components: {
                  Field: './components/admin/taxonomy/CategoryEditor#CategoryHierarchy',
                },
              },
            },
          ],
        },
      ],
    },
    {
      name: 'sortOrder',
      type: 'number',
      defaultValue: 0,
      admin: {
        position: 'sidebar',
        description: 'Lower values appear earlier in manually ordered category lists.',
        components: {
          Cell: './components/admin/taxonomy/CategoryCells#CategorySortCell',
        },
      },
    },
    {
      name: 'categoryReadiness',
      type: 'ui',
      admin: {
        position: 'sidebar',
        components: {
          Field: './components/admin/taxonomy/CategoryEditor#CategoryReadiness',
        },
      },
    },
  ],
}

====================================================================================================
FILE: src/collections/ProjectVersions.ts
====================================================================================================
import type { CollectionConfig } from 'payload'

import { authenticatedAccess, publicAccess } from '@/access'

export const ProjectVersions: CollectionConfig = {
  slug: 'project-versions',
  admin: {
    useAsTitle: 'version',
    defaultColumns: [
      'version',
      'project',
      'releaseDate',
      'isCurrent',
      'isStable',
      'breakingChanges',
    ],
    listSearchableFields: ['version', 'title', 'summary'],
    components: {
      beforeList: ['./components/admin/project-versions/ProjectVersionsListHeader'],
    },
  },
  access: {
    read: publicAccess,
    create: authenticatedAccess,
    update: authenticatedAccess,
    delete: authenticatedAccess,
  },
  fields: [
    {
      name: 'releaseHeader',
      type: 'ui',
      admin: {
        components: {
          Field: './components/admin/project-versions/ProjectVersionHeader#ProjectVersionHeader',
        },
      },
    },
    {
      name: 'releaseWorkspace',
      type: 'ui',
      admin: {
        components: {
          Field: './components/admin/project-versions/ProjectVersionEditor#ReleaseWorkspace',
        },
      },
    },
    {
      type: 'tabs',
      tabs: [
        {
          label: 'Release',
          fields: [
            {
              name: 'project',
              type: 'relationship',
              relationTo: 'projects',
              required: true,
              hasMany: false,
              admin: {
                components: {
                  Cell: './components/admin/project-versions/ProjectVersionCells#ProjectVersionProjectCell',
                },
              },
            },
            {
              type: 'row',
              fields: [
                {
                  name: 'version',
                  type: 'text',
                  required: true,
                  admin: {
                    width: '34%',
                    description: 'Version number or label, e.g. 1.0.0, Phase 2.2, MVP.',
                    components: {
                      Cell: './components/admin/project-versions/ProjectVersionCells#ProjectVersionIdentityCell',
                    },
                  },
                },
                {
                  name: 'title',
                  type: 'text',
                  required: true,
                  admin: {
                    width: '66%',
                  },
                },
              ],
            },
            {
              name: 'releaseDate',
              type: 'date',
              admin: {
                date: {
                  pickerAppearance: 'dayOnly',
                },
                description: 'Public or internal release date for this version.',
                components: {
                  Cell: './components/admin/project-versions/ProjectVersionCells#ProjectVersionDateCell',
                },
              },
            },
            {
              name: 'summary',
              type: 'textarea',
              required: true,
              admin: {
                description: 'Concise explanation used in version history and previews.',
              },
            },
          ],
        },
        {
          label: 'Highlights',
          fields: [
            {
              name: 'highlights',
              type: 'array',
              labels: {
                singular: 'Highlight',
                plural: 'Highlights',
              },
              fields: [
                {
                  name: 'title',
                  type: 'text',
                  required: true,
                },
                {
                  name: 'description',
                  type: 'textarea',
                },
              ],
            },
            {
              name: 'releaseHighlightsSummary',
              type: 'ui',
              admin: {
                components: {
                  Field:
                    './components/admin/project-versions/ProjectVersionEditor#ReleaseHighlightsSummary',
                },
              },
            },
          ],
        },
        {
          label: 'Breaking changes',
          fields: [
            {
              name: 'breakingChanges',
              type: 'array',
              labels: {
                singular: 'Breaking change',
                plural: 'Breaking changes',
              },
              admin: {
                components: {
                  Cell: './components/admin/project-versions/ProjectVersionCells#ProjectVersionBreakingCell',
                },
              },
              fields: [
                {
                  name: 'title',
                  type: 'text',
                  required: true,
                },
                {
                  name: 'description',
                  type: 'textarea',
                },
              ],
            },
            {
              name: 'releaseRiskSummary',
              type: 'ui',
              admin: {
                components: {
                  Field:
                    './components/admin/project-versions/ProjectVersionEditor#ReleaseRiskSummary',
                },
              },
            },
          ],
        },
      ],
    },
    {
      name: 'isStable',
      type: 'checkbox',
      defaultValue: true,
      admin: {
        position: 'sidebar',
        description: 'Suitable for production use.',
        components: {
          Cell: './components/admin/project-versions/ProjectVersionCells#ProjectVersionStableCell',
        },
      },
    },
    {
      name: 'isCurrent',
      type: 'checkbox',
      defaultValue: false,
      admin: {
        position: 'sidebar',
        description: 'Current visible project version.',
        components: {
          Cell: './components/admin/project-versions/ProjectVersionCells#ProjectVersionCurrentCell',
        },
      },
    },
    {
      name: 'releaseReadiness',
      type: 'ui',
      admin: {
        position: 'sidebar',
        components: {
          Field: './components/admin/project-versions/ProjectVersionEditor#ReleaseReadiness',
        },
      },
    },
  ],
}

====================================================================================================
FILE: src/collections/Projects.ts
====================================================================================================
import type { CollectionConfig } from 'payload'
import { authenticatedAccess, publicAccess } from '@/access'

import { seoField, slugField } from '@/fields'

export const Projects: CollectionConfig = {
  slug: 'projects',
  // portfolio-admin-projects-list-components-v2
  admin: {
    useAsTitle: 'title',
    defaultColumns: ['title', 'stage', 'progress', 'isFeatured', 'publishedAt'],
    listSearchableFields: ['title', 'slug', 'excerpt'],
    components: {
      beforeList: ['./components/admin/projects/ProjectsListHeader'],
      edit: {
        beforeDocumentControls: ['./components/admin/projects/ProjectDocumentControls'],
      },
    },
  },
  access: {
    read: publicAccess,
    create: authenticatedAccess,
    update: authenticatedAccess,
    delete: authenticatedAccess,
  },
  // portfolio-admin-project-editor-operational-polish-v1
  fields: [
    // portfolio-admin-project-editor-tabs-v1
    {
      type: 'tabs',
      tabs: [
        {
          label: 'Overview',
          description: 'Core identity, positioning, classification, and technology.',
          fields: [
            {
              name: 'title',
              type: 'text',
              required: true,
              admin: {
                components: {
                  Cell: './components/admin/projects/ProjectCells#ProjectTitleCell',
                },
              },
            },
            slugField({ sourceField: 'title' }),
            {
              name: 'excerpt',
              type: 'textarea',
              required: true,
              admin: {
                description: 'Short project summary for cards, lists, and SEO previews.',
              },
            },
            {
              name: 'cardTagline',
              type: 'textarea',
              admin: {
                description:
                  'Short marketing line used in featured project previews. Keep it concise and outcome-focused.',
              },
            },
            {
              name: 'category',
              type: 'relationship',
              relationTo: 'categories',
              hasMany: false,
              admin: {
                description: 'Use categories with type "Project" or "Shared".',
              },
            },
            {
              name: 'techStack',
              type: 'relationship',
              relationTo: 'tech-stack',
              hasMany: true,
              admin: {
                description: 'Technologies used in this project.',
              },
            },
          ],
        },
        {
          label: 'Case Study',
          description: 'Long-form project story and measurable highlights.',
          fields: [
            {
              name: 'description',
              type: 'richText',
              required: true,
            },
            {
              name: 'highlights',
              type: 'array',
              fields: [
                {
                  name: 'title',
                  type: 'text',
                  required: true,
                },
                {
                  name: 'description',
                  type: 'textarea',
                  required: true,
                },
                {
                  name: 'icon',
                  type: 'text',
                  admin: {
                    description: 'Icon key for frontend icon mapping.',
                  },
                },
              ],
            },
          ],
        },
        {
          label: 'Media',
          description: 'Primary visuals and the project gallery.',
          fields: [
            {
              name: 'coverImage',
              type: 'upload',
              relationTo: 'media',
              admin: {
                description: 'Primary image for project cards and list pages.',
              },
            },
            {
              name: 'featuredImage',
              type: 'upload',
              relationTo: 'media',
              admin: {
                description: 'Large visual for featured homepage/project sections.',
              },
            },
            {
              name: 'gallery',
              type: 'array',
              fields: [
                {
                  name: 'image',
                  type: 'upload',
                  relationTo: 'media',
                  required: true,
                },
                {
                  name: 'caption',
                  type: 'text',
                },
                {
                  name: 'alt',
                  type: 'text',
                  required: true,
                },
                {
                  name: 'sortOrder',
                  type: 'number',
                  defaultValue: 0,
                },
                {
                  name: 'isFeatured',
                  type: 'checkbox',
                  defaultValue: false,
                },
                {
                  name: 'deviceFrame',
                  type: 'select',
                  required: true,
                  defaultValue: 'none',
                  options: [
                    {
                      label: 'None',
                      value: 'none',
                    },
                    {
                      label: 'Desktop',
                      value: 'desktop',
                    },
                    {
                      label: 'Laptop',
                      value: 'laptop',
                    },
                    {
                      label: 'Tablet',
                      value: 'tablet',
                    },
                    {
                      label: 'Mobile',
                      value: 'mobile',
                    },
                  ],
                },
              ],
            },
          ],
        },
        {
          label: 'Links & Repository',
          description: 'Repository metadata and public project destinations.',
          fields: [
            {
              name: 'github',
              type: 'group',
              fields: [
                {
                  name: 'url',
                  type: 'text',
                  admin: {
                    description: 'Public GitHub repository URL.',
                  },
                },
                {
                  name: 'owner',
                  type: 'text',
                  admin: {
                    description: 'GitHub owner or organization name.',
                  },
                },
                {
                  name: 'repo',
                  type: 'text',
                  admin: {
                    description: 'GitHub repository name.',
                  },
                },
                {
                  name: 'showStats',
                  type: 'checkbox',
                  defaultValue: true,
                  admin: {
                    description: 'Allow frontend to fetch and show live GitHub stats.',
                  },
                },
              ],
            },
            {
              name: 'links',
              type: 'array',
              fields: [
                {
                  name: 'label',
                  type: 'text',
                  required: true,
                },
                {
                  name: 'url',
                  type: 'text',
                  admin: {
                    description: 'If empty, frontend should show disabled CTA with "Невдовзі".',
                  },
                },
                {
                  name: 'type',
                  type: 'select',
                  required: true,
                  defaultValue: 'other',
                  options: [
                    {
                      label: 'GitHub',
                      value: 'github',
                    },
                    {
                      label: 'Live',
                      value: 'live',
                    },
                    {
                      label: 'Documentation',
                      value: 'documentation',
                    },
                    {
                      label: 'Case Study',
                      value: 'case-study',
                    },
                    {
                      label: 'Figma',
                      value: 'figma',
                    },
                    {
                      label: 'Other',
                      value: 'other',
                    },
                  ],
                },
                {
                  name: 'isEnabled',
                  type: 'checkbox',
                  defaultValue: true,
                },
              ],
            },
          ],
        },
        {
          label: 'Relationships',
          description: 'Editorial content associated with this project.',
          fields: [
            {
              name: 'relatedBlogPosts',
              type: 'relationship',
              relationTo: 'blog-posts',
              hasMany: true,
              admin: {
                description: 'Articles related to this project.',
              },
            },
          ],
        },
        {
          label: 'SEO',
          description: 'Search and social-sharing metadata.',
          fields: [seoField()],
        },
      ],
    },
    {
      name: 'projectHealth',
      type: 'ui',
      admin: {
        position: 'sidebar',
        components: {
          Field: './components/admin/projects/ProjectHealthPanel',
        },
      },
    },
    {
      name: 'stage',
      type: 'select',
      required: true,
      defaultValue: 'development',
      options: [
        {
          label: 'Idea',
          value: 'idea',
        },
        {
          label: 'Planning',
          value: 'planning',
        },
        {
          label: 'Development',
          value: 'development',
        },
        {
          label: 'Testing',
          value: 'testing',
        },
        {
          label: 'Released',
          value: 'released',
        },
        {
          label: 'Maintenance',
          value: 'maintenance',
        },
        {
          label: 'Archived',
          value: 'archived',
        },
      ],
      admin: {
        position: 'sidebar',
        components: {
          Cell: './components/admin/projects/ProjectCells#ProjectStageCell',
        },
      },
    },
    {
      name: 'progress',
      type: 'number',
      min: 0,
      max: 100,
      defaultValue: 0,
      admin: {
        position: 'sidebar',
        description: 'Project completion percentage from 0 to 100.',
        components: {
          Cell: './components/admin/projects/ProjectCells#ProjectProgressCell',
          Field: './components/admin/projects/ProjectProgressField',
        },
      },
    },
    {
      name: 'currentVersion',
      type: 'text',
      admin: {
        position: 'sidebar',
        description: 'Current public or internal version, e.g. 1.0.0.',
      },
    },
    {
      name: 'isFeatured',
      type: 'checkbox',
      defaultValue: false,
      admin: {
        position: 'sidebar',
        description: 'Show this project in featured sections.',
        components: {
          Cell: './components/admin/projects/ProjectCells#ProjectFeaturedCell',
        },
      },
    },
    {
      name: 'publishedAt',
      type: 'date',
      admin: {
        position: 'sidebar',
        date: {
          pickerAppearance: 'dayAndTime',
        },
        components: {
          Cell: './components/admin/projects/ProjectCells#ProjectPublishedCell',
        },
      },
    },
    {
      name: 'startedAt',
      type: 'date',
      admin: {
        position: 'sidebar',
        date: {
          pickerAppearance: 'dayOnly',
        },
      },
    },
    {
      name: 'releasedAt',
      type: 'date',
      admin: {
        position: 'sidebar',
        date: {
          pickerAppearance: 'dayOnly',
        },
      },
    },
  ],
}

====================================================================================================
FILE: src/collections/TechStack.ts
====================================================================================================
import type { CollectionConfig } from 'payload'

import { authenticatedAccess, publicAccess } from '@/access'
import { slugField } from '@/fields'

export const TechStack: CollectionConfig = {
  slug: 'tech-stack',
  admin: {
    useAsTitle: 'name',
    defaultColumns: ['name', 'category', 'featured', 'visible', 'officialUrl', 'sortOrder'],
    listSearchableFields: ['name', 'slug', 'description'],
    components: {
      beforeList: ['./components/admin/taxonomy/TechStackListHeader'],
    },
  },
  access: {
    read: publicAccess,
    create: authenticatedAccess,
    update: authenticatedAccess,
    delete: authenticatedAccess,
  },
  fields: [
    {
      name: 'technologyWorkspace',
      type: 'ui',
      admin: {
        components: {
          Field: './components/admin/taxonomy/TechStackEditor#TechnologyWorkspace',
        },
      },
    },
    {
      type: 'tabs',
      tabs: [
        {
          label: 'Identity',
          fields: [
            {
              type: 'row',
              fields: [
                {
                  name: 'name',
                  type: 'text',
                  required: true,
                  admin: {
                    width: '70%',
                    components: {
                      Cell: './components/admin/taxonomy/TechStackCells#TechnologyNameCell',
                    },
                  },
                },
                {
                  name: 'color',
                  type: 'text',
                  admin: {
                    width: '30%',
                    description: 'Optional brand color in hexadecimal format, e.g. #3178C6.',
                  },
                },
              ],
            },
            slugField({
              sourceField: 'name',
              description: 'Used as the icon key for AppIcon, e.g. nextjs, react, typescript.',
            }),
            {
              name: 'description',
              type: 'textarea',
              admin: {
                description: 'Short tooltip-friendly description.',
              },
            },
          ],
        },
        {
          label: 'Classification',
          fields: [
            {
              name: 'category',
              type: 'relationship',
              relationTo: 'categories',
              hasMany: false,
              admin: {
                description: 'Use categories with type "Tech Stack" or "Shared".',
                components: {
                  Cell: './components/admin/taxonomy/TechStackCells#TechnologyCategoryCell',
                },
              },
            },
            {
              name: 'technologyClassification',
              type: 'ui',
              admin: {
                components: {
                  Field: './components/admin/taxonomy/TechStackEditor#TechnologyClassification',
                },
              },
            },
          ],
        },
        {
          label: 'Resources',
          fields: [
            {
              name: 'officialUrl',
              type: 'text',
              admin: {
                description: 'Official website URL.',
                components: {
                  Cell: './components/admin/taxonomy/TechStackCells#TechnologyLinksCell',
                },
              },
            },
            {
              name: 'documentationUrl',
              type: 'text',
              admin: {
                description: 'Official documentation URL.',
              },
            },
            {
              name: 'technologyResources',
              type: 'ui',
              admin: {
                components: {
                  Field: './components/admin/taxonomy/TechStackEditor#TechnologyResources',
                },
              },
            },
          ],
        },
      ],
    },
    {
      name: 'featured',
      type: 'checkbox',
      defaultValue: false,
      admin: {
        position: 'sidebar',
        description: 'Show in highlighted homepage sections.',
        components: {
          Cell: './components/admin/taxonomy/TechStackCells#TechnologyFeaturedCell',
        },
      },
    },
    {
      name: 'visible',
      type: 'checkbox',
      defaultValue: true,
      admin: {
        position: 'sidebar',
        description: 'Allow this technology to appear on public pages.',
        components: {
          Cell: './components/admin/taxonomy/TechStackCells#TechnologyVisibleCell',
        },
      },
    },
    {
      name: 'sortOrder',
      type: 'number',
      defaultValue: 0,
      admin: {
        position: 'sidebar',
        description: 'Lower values appear earlier in manually ordered technology lists.',
        components: {
          Cell: './components/admin/taxonomy/TechStackCells#TechnologySortCell',
        },
      },
    },
    {
      name: 'technologyReadiness',
      type: 'ui',
      admin: {
        position: 'sidebar',
        components: {
          Field: './components/admin/taxonomy/TechStackEditor#TechnologyReadiness',
        },
      },
    },
  ],
}

====================================================================================================
FILE: src/collections/Testimonials.ts
====================================================================================================
import type { CollectionConfig } from 'payload'
import { authenticatedAccess, publicAccess } from '@/access'

export const Testimonials: CollectionConfig = {
  slug: 'testimonials',
  // portfolio-admin-testimonials-moderation-list-v1
  admin: {
    useAsTitle: 'name',
    defaultColumns: ['name', 'status', 'rating', 'source', 'approvedAt'],
    listSearchableFields: ['name', 'role', 'company', 'message', 'source'],
    components: {
      beforeList: ['./components/admin/testimonials/TestimonialsListHeader'],
      edit: {
        beforeDocumentControls: ['./components/admin/testimonials/TestimonialDocumentControls'],
      },
    },
  },
  access: {
    read: publicAccess,
    create: publicAccess,
    update: authenticatedAccess,
    delete: authenticatedAccess,
  },
  fields: [
    // portfolio-admin-testimonial-editor-v1
    {
      type: 'tabs',
      tabs: [
        {
          label: 'Feedback',
          description: 'Client identity and testimonial content.',
          fields: [
            {
              type: 'row',
              fields: [
                {
                  name: 'name',
                  type: 'text',
                  required: true,
                  admin: {
                    components: {
                      Cell: './components/admin/testimonials/TestimonialCells#TestimonialIdentityCell',
                    },
                  },
                },
                {
                  name: 'role',
                  type: 'text',
                },
                {
                  name: 'company',
                  type: 'text',
                },
              ],
            },
            {
              name: 'message',
              type: 'textarea',
              required: true,
            },
          ],
        },
        {
          label: 'Media',
          description: 'Optional client avatar or organization image.',
          fields: [
            {
              name: 'avatar',
              type: 'upload',
              relationTo: 'media',
            },
          ],
        },
      ],
    },
    {
      name: 'testimonialModeration',
      type: 'ui',
      admin: {
        position: 'sidebar',
        components: {
          Field: './components/admin/testimonials/TestimonialModerationPanel',
        },
      },
    },
    {
      name: 'status',
      type: 'select',
      required: true,
      defaultValue: 'pending',
      options: [
        {
          label: 'Pending',
          value: 'pending',
        },
        {
          label: 'Approved',
          value: 'approved',
        },
        {
          label: 'Rejected',
          value: 'rejected',
        },
      ],
      admin: {
        position: 'sidebar',
        components: {
          Cell: './components/admin/testimonials/TestimonialCells#TestimonialStatusCell',
          Field: './components/admin/testimonials/TestimonialStatusField',
        },
      },
    },
    {
      name: 'rating',
      type: 'number',
      min: 1,
      max: 5,
      admin: {
        position: 'sidebar',
        description: 'Optional rating from 1 to 5.',
        components: {
          Cell: './components/admin/testimonials/TestimonialCells#TestimonialRatingCell',
        },
      },
    },
    {
      name: 'source',
      type: 'text',
      admin: {
        position: 'sidebar',
        description: 'Where this testimonial came from, e.g. website, LinkedIn, direct message.',
        components: {
          Cell: './components/admin/testimonials/TestimonialCells#TestimonialSourceCell',
        },
      },
    },
    {
      name: 'approvedAt',
      type: 'date',
      admin: {
        position: 'sidebar',
        date: {
          pickerAppearance: 'dayAndTime',
        },
        components: {
          Cell: './components/admin/testimonials/TestimonialCells#TestimonialApprovedCell',
        },
      },
    },
  ],
}

====================================================================================================
FILE: src/components/admin/blog-posts/ArticleDocumentControls.tsx
====================================================================================================
// Portfolio Admin Experience - Article Document Controls v1
'use client'

import Link from 'next/link'
import { useDocumentInfo, useFormFields } from '@payloadcms/ui'

type FieldState = {
  value?: unknown
}

const statusLabels: Record<string, string> = {
  archived: 'Archived',
  draft: 'Draft',
  published: 'Published',
}

function readText(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function collectText(value: unknown, result: string[]): void {
  if (typeof value === 'string') {
    result.push(value)
    return
  }

  if (Array.isArray(value)) {
    value.forEach((item) => collectText(item, result))
    return
  }

  if (!value || typeof value !== 'object') {
    return
  }

  const record = value as Record<string, unknown>

  if (typeof record.text === 'string') {
    result.push(record.text)
  }

  Object.entries(record).forEach(([key, nested]) => {
    if (key !== 'text' && key !== 'type' && key !== 'version') {
      collectText(nested, result)
    }
  })
}

function readingTime(value: unknown): number {
  const fragments: string[] = []
  collectText(value, fragments)

  const words = fragments.join(' ').trim().split(/\s+/u).filter(Boolean).length

  return words > 0 ? Math.max(1, Math.ceil(words / 220)) : 0
}

function formatUpdated(value: unknown): string | null {
  if (typeof value !== 'string' && typeof value !== 'number' && !(value instanceof Date)) {
    return null
  }

  const date = value instanceof Date ? value : new Date(value)

  if (Number.isNaN(date.getTime())) {
    return null
  }

  return new Intl.DateTimeFormat('en-GB', {
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
    month: 'short',
  }).format(date)
}

export default function ArticleDocumentControls() {
  const { isEditing, lastUpdateTime } = useDocumentInfo()
  const state = useFormFields(([fields]) => ({
    content: (fields.content as FieldState | undefined)?.value,
    slug: (fields.slug as FieldState | undefined)?.value,
    status: (fields.status as FieldState | undefined)?.value,
  }))

  const status = readText(state.status, 'draft').toLowerCase()
  const slug = readText(state.slug)
  const minutes = readingTime(state.content)
  const updated = formatUpdated(lastUpdateTime)

  return (
    <div className="portfolio-admin-article-command">
      <span className={`portfolio-admin-article-command__status is-${status}`}>
        <i aria-hidden="true" />
        {statusLabels[status] || status}
      </span>

      <span className="portfolio-admin-article-command__reading">
        <strong>{minutes}</strong>
        <small>min read</small>
      </span>

      {isEditing && updated ? (
        <span className="portfolio-admin-article-command__updated">
          Updated <time>{updated}</time>
        </span>
      ) : null}

      {slug ? (
        <Link
          className="portfolio-admin-article-command__preview"
          href={`/articles/${encodeURIComponent(slug)}`}
          target="_blank"
        >
          Preview
          <span aria-hidden="true">↗</span>
        </Link>
      ) : (
        <span className="portfolio-admin-article-command__preview is-disabled">Preview</span>
      )}
    </div>
  )
}

====================================================================================================
FILE: src/components/admin/blog-posts/ArticleReadinessPanel.tsx
====================================================================================================
// Portfolio Admin Experience - Article Readiness Panel v1
'use client'

import type { CSSProperties } from 'react'
import Link from 'next/link'
import { useFormFields } from '@payloadcms/ui'

type FieldState = {
  rows?: unknown[]
  value?: unknown
}

type ReadinessCheck = {
  description: string
  label: string
  ready: boolean
}

function asField(value: unknown): FieldState | undefined {
  return value && typeof value === 'object' ? (value as FieldState) : undefined
}

function hasText(value: unknown): boolean {
  return typeof value === 'string' && value.trim().length > 0
}

function hasValue(value: unknown): boolean {
  if (value === null || value === undefined || value === '') {
    return false
  }

  if (Array.isArray(value)) {
    return value.length > 0
  }

  return true
}

function hasRows(field: FieldState | undefined): boolean {
  return Array.isArray(field?.rows) && field.rows.length > 0
}

function collectText(value: unknown, result: string[]): void {
  if (typeof value === 'string') {
    result.push(value)
    return
  }

  if (Array.isArray(value)) {
    value.forEach((item) => collectText(item, result))
    return
  }

  if (!value || typeof value !== 'object') {
    return
  }

  const record = value as Record<string, unknown>

  if (typeof record.text === 'string') {
    result.push(record.text)
  }

  Object.entries(record).forEach(([key, nested]) => {
    if (key !== 'text' && key !== 'type' && key !== 'version') {
      collectText(nested, result)
    }
  })
}

function countWords(value: unknown): number {
  const fragments: string[] = []
  collectText(value, fragments)

  return fragments.join(' ').trim().split(/\s+/u).filter(Boolean).length
}

function getReadingTime(words: number): number {
  return words > 0 ? Math.max(1, Math.ceil(words / 220)) : 0
}

export default function ArticleReadinessPanel() {
  const fields = useFormFields(([formFields]) => formFields)
  const read = (path: string): FieldState | undefined => asField(fields[path])

  const title = read('title')?.value
  const slug = read('slug')?.value
  const excerpt = read('excerpt')?.value
  const content = read('content')?.value
  const coverImage = read('coverImage')?.value
  const category = read('category')?.value
  const author = read('author')?.value
  const publishedAt = read('publishedAt')?.value
  const tags = read('tags')
  const metaTitle = read('seo.metaTitle')?.value
  const metaDescription = read('seo.metaDescription')?.value

  const words = countWords(content)
  const readingTime = getReadingTime(words)

  const checks: ReadinessCheck[] = [
    {
      label: 'Identity',
      description: 'Title, slug, and excerpt',
      ready: hasText(title) && hasText(slug) && hasText(excerpt),
    },
    {
      label: 'Article body',
      description: 'At least 120 words of editorial content',
      ready: words >= 120,
    },
    {
      label: 'Media',
      description: 'Primary cover image',
      ready: hasValue(coverImage),
    },
    {
      label: 'Taxonomy',
      description: 'Category and at least one tag',
      ready: hasValue(category) && hasRows(tags),
    },
    {
      label: 'Attribution',
      description: 'Assigned article author',
      ready: hasValue(author),
    },
    {
      label: 'Publication',
      description: 'Publication date is configured',
      ready: hasValue(publishedAt),
    },
    {
      label: 'SEO',
      description: 'Search title and description',
      ready: hasText(metaTitle) && hasText(metaDescription),
    },
  ]

  const readyCount = checks.filter((check) => check.ready).length
  const score = Math.round((readyCount / checks.length) * 100)
  const slugValue = hasText(slug) ? String(slug).trim() : ''
  const style = {
    '--article-readiness-score': `${score}%`,
  } as CSSProperties

  return (
    <aside className="portfolio-admin-article-readiness" style={style}>
      <header className="portfolio-admin-article-readiness__header">
        <div>
          <span>Article readiness</span>
          <strong>{score}%</strong>
        </div>

        <span
          aria-label={`${readyCount} of ${checks.length} checks complete`}
          className="portfolio-admin-article-readiness__meter"
          role="img"
        >
          <i />
        </span>
      </header>

      <div className="portfolio-admin-article-readiness__reading">
        <span>
          <strong>{readingTime}</strong>
          <small>min read</small>
        </span>
        <span>
          <strong>{words}</strong>
          <small>words</small>
        </span>
      </div>

      <p className="portfolio-admin-article-readiness__summary">
        {readyCount === checks.length
          ? 'All core editorial checks are complete.'
          : `${checks.length - readyCount} editorial ${
              checks.length - readyCount === 1 ? 'check needs' : 'checks need'
            } attention.`}
      </p>

      <div className="portfolio-admin-article-readiness__checks">
        {checks.map((check) => (
          <div
            className={`portfolio-admin-article-readiness__check${check.ready ? ' is-ready' : ''}`}
            key={check.label}
          >
            <span aria-hidden="true">
              {check.ready ? (
                <svg fill="none" viewBox="0 0 20 20">
                  <path
                    d="m5 10.2 3.1 3.1L15.4 6"
                    stroke="currentColor"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    strokeWidth="1.7"
                  />
                </svg>
              ) : (
                <svg fill="none" viewBox="0 0 20 20">
                  <circle cx="10" cy="10" r="3.2" stroke="currentColor" strokeWidth="1.5" />
                </svg>
              )}
            </span>

            <div>
              <strong>{check.label}</strong>
              <small>{check.description}</small>
            </div>
          </div>
        ))}
      </div>

      {slugValue ? (
        <Link
          className="portfolio-admin-article-readiness__preview"
          href={`/articles/${encodeURIComponent(slugValue)}`}
          target="_blank"
        >
          Preview article
          <span aria-hidden="true">↗</span>
        </Link>
      ) : (
        <span className="portfolio-admin-article-readiness__preview is-disabled">
          Add a slug to enable preview
        </span>
      )}
    </aside>
  )
}

====================================================================================================
FILE: src/components/admin/blog-posts/BlogPostCells.tsx
====================================================================================================
// Portfolio Admin Experience - Blog Post Cells v1
'use client'

import Link from 'next/link'

import type { DefaultCellComponentProps } from 'payload'

type UnknownRecord = Record<string, unknown>

function asRecord(value: unknown): UnknownRecord {
  return value && typeof value === 'object' ? (value as UnknownRecord) : {}
}

function asText(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function getMedia(value: unknown): UnknownRecord | null {
  if (!value || typeof value !== 'object') {
    return null
  }

  return value as UnknownRecord
}

function getCategoryTitle(value: unknown): string {
  if (typeof value === 'string' || typeof value === 'number') {
    return 'Assigned'
  }

  const category = asRecord(value)
  return asText(category.title, asText(category.name, 'Assigned'))
}

function collectText(value: unknown, result: string[]): void {
  if (typeof value === 'string') {
    result.push(value)
    return
  }

  if (Array.isArray(value)) {
    value.forEach((item) => collectText(item, result))
    return
  }

  if (!value || typeof value !== 'object') {
    return
  }

  const record = value as UnknownRecord

  if (typeof record.text === 'string') {
    result.push(record.text)
  }

  Object.entries(record).forEach(([key, nested]) => {
    if (key !== 'text' && key !== 'type' && key !== 'version') {
      collectText(nested, result)
    }
  })
}

function estimateReadingTime(content: unknown, excerpt: unknown): number {
  const fragments: string[] = []
  collectText(content, fragments)

  if (fragments.length === 0 && typeof excerpt === 'string') {
    fragments.push(excerpt)
  }

  const words = fragments.join(' ').trim().split(/\s+/u).filter(Boolean).length

  return Math.max(1, Math.ceil(words / 220))
}

function initials(title: string): string {
  return title
    .split(/\s+/u)
    .filter(Boolean)
    .slice(0, 2)
    .map((part) => part[0]?.toUpperCase())
    .join('')
}

export function BlogPostTitleCell({ cellData, rowData }: DefaultCellComponentProps) {
  const row = asRecord(rowData)
  const title = asText(cellData, 'Untitled article')
  const slug = asText(row.slug, 'slug-not-set')
  const documentID = typeof row.id === 'string' || typeof row.id === 'number' ? String(row.id) : ''
  const cover = getMedia(row.coverImage)
  const coverURL = asText(cover?.thumbnailURL, asText(cover?.url))
  const safeCoverURL = coverURL.replace(/"/g, '%22')
  const readingTime = estimateReadingTime(row.content, row.excerpt)
  const editURL = documentID
    ? `/admin/collections/blog-posts/${encodeURIComponent(documentID)}`
    : '/admin/collections/blog-posts'

  return (
    <Link aria-label={`Edit article ${title}`} className="portfolio-admin-blog-cell" href={editURL}>
      <span
        aria-hidden="true"
        className={`portfolio-admin-blog-cell__preview${safeCoverURL ? ' has-image' : ''}`}
        style={safeCoverURL ? { backgroundImage: `url("${safeCoverURL}")` } : undefined}
      >
        {safeCoverURL ? null : initials(title)}
      </span>

      <span className="portfolio-admin-blog-cell__copy">
        <strong>{title}</strong>
        <span>
          <code>{slug}</code>
          <i aria-hidden="true" />
          <small>{readingTime} min read</small>
        </span>
      </span>

      <span aria-hidden="true" className="portfolio-admin-blog-cell__open">
        ↗
      </span>
    </Link>
  )
}

export function BlogPostStatusCell({ cellData }: DefaultCellComponentProps) {
  const status = asText(cellData, 'draft').toLowerCase()
  const labels: Record<string, string> = {
    archived: 'Archived',
    draft: 'Draft',
    published: 'Published',
  }

  return (
    <span className={`portfolio-admin-blog-status is-${status}`}>
      <i aria-hidden="true" />
      {labels[status] || status}
    </span>
  )
}

export function BlogPostCategoryCell({ cellData }: DefaultCellComponentProps) {
  if (!cellData) {
    return <span className="portfolio-admin-blog-category is-empty">Uncategorized</span>
  }

  return <span className="portfolio-admin-blog-category">{getCategoryTitle(cellData)}</span>
}

export function BlogPostPublishedCell({ cellData, rowData }: DefaultCellComponentProps) {
  const row = asRecord(rowData)
  const status = asText(row.status, 'draft').toLowerCase()
  const rawDate = typeof cellData === 'string' ? cellData : ''
  const date = rawDate ? new Date(rawDate) : null
  const validDate = date && !Number.isNaN(date.getTime()) ? date : null

  const label = status === 'published' ? 'Published' : status === 'archived' ? 'Archived' : 'Draft'

  return (
    <span className={`portfolio-admin-blog-date is-${status}`}>
      <i aria-hidden="true" />
      <span>
        <strong>{label}</strong>
        <small>
          {validDate
            ? new Intl.DateTimeFormat('en-GB', {
                day: '2-digit',
                month: 'short',
                year: 'numeric',
              }).format(validDate)
            : 'No publication date'}
        </small>
      </span>
    </span>
  )
}

====================================================================================================
FILE: src/components/admin/blog-posts/BlogPostsListHeader.tsx
====================================================================================================
// Portfolio Admin Experience - Blog Posts List Header v1

import Link from 'next/link'

import type { BeforeListServerProps, Where } from 'payload'

type Tone = 'amber' | 'cyan' | 'green' | 'purple'

type MetricProps = {
  code: string
  description: string
  label: string
  tone: Tone
  value: number
}

async function countPosts(
  payload: BeforeListServerProps['payload'],
  user: BeforeListServerProps['user'],
  where?: Where,
): Promise<number> {
  try {
    const result = await payload.count({
      collection: 'blog-posts',
      overrideAccess: false,
      user,
      ...(where ? { where } : {}),
    })

    return result.totalDocs
  } catch {
    return 0
  }
}

function Metric({ code, description, label, tone, value }: MetricProps) {
  return (
    <article className={`portfolio-admin-blog-stat is-${tone}`}>
      <div className="portfolio-admin-blog-stat__topline">
        <span>{label}</span>
        <code>{code}</code>
      </div>

      <strong>{value}</strong>
      <p>{description}</p>

      <span aria-hidden="true" className="portfolio-admin-blog-stat__signal">
        <i />
        <i />
        <i />
      </span>
    </article>
  )
}

export default async function BlogPostsListHeader({
  hasCreatePermission,
  newDocumentURL,
  payload,
  user,
}: BeforeListServerProps) {
  const [total, published, drafts, archived, needsAttention] = await Promise.all([
    countPosts(payload, user),
    countPosts(payload, user, {
      status: {
        equals: 'published',
      },
    }),
    countPosts(payload, user, {
      status: {
        equals: 'draft',
      },
    }),
    countPosts(payload, user, {
      status: {
        equals: 'archived',
      },
    }),
    countPosts(payload, user, {
      or: [
        {
          coverImage: {
            exists: false,
          },
        },
        {
          category: {
            exists: false,
          },
        },
        {
          author: {
            exists: false,
          },
        },
        {
          and: [
            {
              status: {
                equals: 'published',
              },
            },
            {
              publishedAt: {
                exists: false,
              },
            },
          ],
        },
      ],
    }),
  ])

  return (
    <section className="portfolio-admin-blog-overview">
      <header className="portfolio-admin-blog-overview__header">
        <div>
          <span className="portfolio-admin-blog-overview__eyebrow">Editorial pipeline</span>
          <h2>Publishing desk</h2>
          <p>
            Track editorial state, taxonomy, media coverage, and publication readiness across{' '}
            {total} {total === 1 ? 'article' : 'articles'}.
          </p>
        </div>

        {hasCreatePermission && newDocumentURL ? (
          <Link className="portfolio-admin-button is-primary" href={newDocumentURL}>
            <span aria-hidden="true">+</span>
            New article
          </Link>
        ) : null}
      </header>

      <div className="portfolio-admin-blog-overview__metrics">
        <Metric
          code="PUB"
          description="Visible in the public journal"
          label="Published"
          tone="cyan"
          value={published}
        />
        <Metric
          code="DFT"
          description="Still inside the editorial workflow"
          label="Drafts"
          tone="purple"
          value={drafts}
        />
        <Metric
          code="ARC"
          description="Removed from active publication"
          label="Archived"
          tone="green"
          value={archived}
        />
        <Metric
          code="REV"
          description="Missing media, taxonomy, author, or date"
          label="Needs attention"
          tone={needsAttention > 0 ? 'amber' : 'green'}
          value={needsAttention}
        />
      </div>
    </section>
  )
}

====================================================================================================
FILE: src/components/admin/project-versions/ProjectVersionCells.tsx
====================================================================================================
// Portfolio Admin Experience - Project Version Cells v1
'use client'

import Link from 'next/link'
import type { DefaultCellComponentProps } from 'payload'

type RecordValue = Record<string, unknown>

function record(value: unknown): RecordValue {
  return value && typeof value === 'object' ? (value as RecordValue) : {}
}

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function relationshipLabel(value: unknown): string {
  if (typeof value === 'string' || typeof value === 'number') {
    return 'Assigned project'
  }

  const project = record(value)

  return text(project.title, text(project.name, text(project.slug, 'Assigned project')))
}

function formatDate(value: unknown): string {
  if (typeof value !== 'string' || !value) {
    return 'Unscheduled'
  }

  const date = new Date(value)

  if (Number.isNaN(date.getTime())) {
    return 'Invalid date'
  }

  return new Intl.DateTimeFormat('en-GB', {
    day: '2-digit',
    month: 'short',
    year: 'numeric',
  }).format(date)
}

function BooleanBadge({
  active,
  activeLabel,
  inactiveLabel,
  tone,
}: {
  active: boolean
  activeLabel: string
  inactiveLabel: string
  tone: 'green' | 'purple'
}) {
  return (
    <span className={`portfolio-admin-release-badge is-${tone}${active ? ' is-active' : ''}`}>
      <i aria-hidden="true" />
      {active ? activeLabel : inactiveLabel}
    </span>
  )
}

export function ProjectVersionIdentityCell({ cellData, rowData }: DefaultCellComponentProps) {
  const row = record(rowData)
  const version = text(cellData, 'Version not set')
  const title = text(row.title, 'Untitled release')
  const summary = text(row.summary, 'No release summary')
  const id = typeof row.id === 'string' || typeof row.id === 'number' ? String(row.id) : ''
  const href = id
    ? `/admin/collections/project-versions/${encodeURIComponent(id)}`
    : '/admin/collections/project-versions'

  return (
    <Link className="portfolio-admin-release-cell" href={href}>
      <span className="portfolio-admin-release-cell__mark">
        {version.slice(0, 3).toUpperCase()}
      </span>
      <span className="portfolio-admin-release-cell__copy">
        <strong>
          <code>{version}</code>
          <span>{title}</span>
        </strong>
        <small>{summary}</small>
      </span>
      <span aria-hidden="true" className="portfolio-admin-release-cell__arrow">
        ↗
      </span>
    </Link>
  )
}

export function ProjectVersionProjectCell({ cellData }: DefaultCellComponentProps) {
  return (
    <span className={`portfolio-admin-release-project${cellData ? '' : ' is-missing'}`}>
      <i aria-hidden="true" />
      {cellData ? relationshipLabel(cellData) : 'Project missing'}
    </span>
  )
}

export function ProjectVersionDateCell({ cellData }: DefaultCellComponentProps) {
  return (
    <span className={`portfolio-admin-release-date${cellData ? '' : ' is-missing'}`}>
      <i aria-hidden="true" />
      {formatDate(cellData)}
    </span>
  )
}

export function ProjectVersionCurrentCell({ cellData }: DefaultCellComponentProps) {
  return (
    <BooleanBadge
      active={cellData === true}
      activeLabel="Current"
      inactiveLabel="Previous"
      tone="purple"
    />
  )
}

export function ProjectVersionStableCell({ cellData }: DefaultCellComponentProps) {
  return (
    <BooleanBadge
      active={cellData === true}
      activeLabel="Stable"
      inactiveLabel="Unstable"
      tone="green"
    />
  )
}

export function ProjectVersionBreakingCell({ cellData }: DefaultCellComponentProps) {
  const count = Array.isArray(cellData) ? cellData.length : 0

  return (
    <span className={`portfolio-admin-release-breaking${count > 0 ? ' has-breaking' : ''}`}>
      <i aria-hidden="true">{count > 0 ? '!' : '✓'}</i>
      {count > 0 ? `${count} ${count === 1 ? 'change' : 'changes'}` : 'No breaking changes'}
    </span>
  )
}

====================================================================================================
FILE: src/components/admin/project-versions/ProjectVersionEditor.tsx
====================================================================================================
// Portfolio Admin Experience - Project Version Editor v1
'use client'

import { useFormFields } from '@payloadcms/ui'
import type { CSSProperties, ReactNode } from 'react'

type FieldState = {
  rows?: unknown[]
  value?: unknown
}

type ReleaseValues = {
  breakingChanges: unknown[]
  highlights: unknown[]
  isCurrent: boolean
  isStable: boolean
  project: unknown
  releaseDate: string
  summary: string
  title: string
  version: string
}

function asField(value: unknown): FieldState | undefined {
  return value && typeof value === 'object' ? (value as FieldState) : undefined
}

function asText(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function asRows(value: unknown): unknown[] {
  const state = asField(value)

  if (Array.isArray(state?.rows)) return state.rows
  if (Array.isArray(state?.value)) return state.value

  return []
}

function projectLabel(value: unknown): string {
  if (typeof value === 'string' || typeof value === 'number') {
    return 'Assigned project'
  }

  if (!value || typeof value !== 'object') {
    return 'Project not linked'
  }

  const project = value as Record<string, unknown>

  return asText(project.title, asText(project.name, asText(project.slug, 'Assigned project')))
}

function formatDate(value: string): string {
  if (!value) return 'Release date not scheduled'

  const date = new Date(value)

  if (Number.isNaN(date.getTime())) return 'Invalid release date'

  return new Intl.DateTimeFormat('en-GB', {
    day: '2-digit',
    month: 'short',
    year: 'numeric',
  }).format(date)
}

function useReleaseValues(): ReleaseValues {
  return useFormFields(([fields]) => ({
    breakingChanges: asRows(fields.breakingChanges),
    highlights: asRows(fields.highlights),
    isCurrent: fields.isCurrent?.value === true,
    isStable: fields.isStable?.value !== false,
    project: fields.project?.value,
    releaseDate: asText(fields.releaseDate?.value),
    summary: asText(fields.summary?.value),
    title: asText(fields.title?.value),
    version: asText(fields.version?.value),
  }))
}

function Badge({
  active,
  children,
  tone,
}: {
  active: boolean
  children: ReactNode
  tone: 'amber' | 'green' | 'purple'
}) {
  return (
    <span className={`portfolio-admin-version-status is-${tone}${active ? ' is-active' : ''}`}>
      <i aria-hidden="true" />
      {children}
    </span>
  )
}

export function ReleaseWorkspace() {
  const state = useReleaseValues()
  const version = state.version || 'Version not set'
  const title = state.title || 'Untitled release'
  const project = projectLabel(state.project)

  return (
    <section className="portfolio-admin-version-workspace">
      <div className="portfolio-admin-version-workspace__copy">
        <span className="portfolio-admin-version-workspace__eyebrow">Release profile</span>

        <div className="portfolio-admin-version-workspace__title-row">
          <code>{version}</code>
          <h2>{title}</h2>
        </div>

        <div className="portfolio-admin-version-workspace__statuses">
          <Badge active={state.isCurrent} tone="purple">
            {state.isCurrent ? 'Current' : 'Previous'}
          </Badge>
          <Badge active={state.isStable} tone="green">
            {state.isStable ? 'Stable' : 'Unstable'}
          </Badge>
          <Badge active={state.breakingChanges.length > 0} tone="amber">
            {state.breakingChanges.length > 0
              ? `${state.breakingChanges.length} breaking`
              : 'No breaking changes'}
          </Badge>
        </div>

        <p>
          {state.summary ||
            'Add a concise summary that explains what changed and why this release matters.'}
        </p>

        <dl>
          <div>
            <dt>Project</dt>
            <dd>{project}</dd>
          </div>
          <div>
            <dt>Release date</dt>
            <dd>{formatDate(state.releaseDate)}</dd>
          </div>
          <div>
            <dt>Highlights</dt>
            <dd>{state.highlights.length}</dd>
          </div>
          <div>
            <dt>Migration risk</dt>
            <dd>{state.breakingChanges.length > 0 ? 'Requires attention' : 'Low'}</dd>
          </div>
        </dl>
      </div>

      <div className="portfolio-admin-version-orbit">
        <span className="portfolio-admin-version-orbit__label">Release signal</span>
        <i aria-hidden="true" className="is-outer" />
        <i aria-hidden="true" className="is-inner" />

        <div className="portfolio-admin-version-orbit__core">
          <span>{version}</span>
          <strong>{title}</strong>
          <small>{project}</small>
        </div>

        <div className="portfolio-admin-version-orbit__timeline">
          <span className="is-complete">Identity</span>
          <i aria-hidden="true" />
          <span className={state.highlights.length ? 'is-complete' : ''}>Highlights</span>
          <i aria-hidden="true" />
          <span className={state.breakingChanges.length ? 'has-risk' : 'is-complete'}>Risk</span>
        </div>
      </div>
    </section>
  )
}

export function ReleaseHighlightsSummary() {
  const state = useReleaseValues()

  return (
    <section className="portfolio-admin-version-summary is-highlights">
      <header>
        <div>
          <span>Release highlights</span>
          <strong>
            {state.highlights.length
              ? `${state.highlights.length} documented`
              : 'No highlights documented yet'}
          </strong>
        </div>
        <code>{state.version || 'DRAFT'}</code>
      </header>
      <p>
        {state.highlights.length
          ? 'The changelog has a clear delivery summary.'
          : 'Add the most important capabilities, improvements, or milestones delivered in this version.'}
      </p>
    </section>
  )
}

export function ReleaseRiskSummary() {
  const state = useReleaseValues()
  const hasRisk = state.breakingChanges.length > 0

  return (
    <section className={`portfolio-admin-version-summary is-risk${hasRisk ? ' has-risk' : ''}`}>
      <header>
        <div>
          <span>Migration assessment</span>
          <strong>
            {hasRisk
              ? `${state.breakingChanges.length} migration warning${
                  state.breakingChanges.length === 1 ? '' : 's'
                }`
              : 'No breaking changes registered'}
          </strong>
        </div>
        <code>{hasRisk ? 'ATTN' : 'SAFE'}</code>
      </header>
      <p>
        {hasRisk
          ? 'Add clear upgrade instructions before publication.'
          : state.isStable
            ? 'This stable release currently presents a low migration risk.'
            : 'The release is unstable, but no explicit breaking changes are documented.'}
      </p>
    </section>
  )
}

function Check({
  complete,
  description,
  label,
}: {
  complete: boolean
  description: string
  label: string
}) {
  return (
    <li className={complete ? 'is-complete' : ''}>
      <span aria-hidden="true">{complete ? '✓' : '○'}</span>
      <div>
        <strong>{label}</strong>
        <small>{description}</small>
      </div>
    </li>
  )
}

export function ReleaseReadiness() {
  const state = useReleaseValues()
  const checks = [
    {
      complete: Boolean(state.project),
      description: 'Linked portfolio project',
      label: 'Project',
    },
    {
      complete: Boolean(state.version && state.title),
      description: 'Version label and release title',
      label: 'Identity',
    },
    {
      complete: Boolean(state.summary),
      description: 'Concise release explanation',
      label: 'Summary',
    },
    {
      complete: Boolean(state.releaseDate),
      description: 'Release date configured',
      label: 'Schedule',
    },
    {
      complete: state.highlights.length > 0,
      description: 'At least one delivery highlight',
      label: 'Highlights',
    },
  ]
  const completed = checks.filter((check) => check.complete).length
  const score = Math.round((completed / checks.length) * 100)
  const remaining = checks.length - completed

  return (
    <aside
      className="portfolio-admin-version-readiness"
      style={
        {
          '--version-readiness': `${score}%`,
        } as CSSProperties
      }
    >
      <header>
        <div>
          <span>Release readiness</span>
          <strong>{score}%</strong>
        </div>
        <i aria-hidden="true">
          <b />
        </i>
      </header>

      <p>
        {remaining === 0
          ? 'All core release checks are complete.'
          : `${remaining} ${remaining === 1 ? 'check needs' : 'checks need'} attention.`}
      </p>

      <ul>
        {checks.map((check) => (
          <Check key={check.label} {...check} />
        ))}
      </ul>
    </aside>
  )
}

====================================================================================================
FILE: src/components/admin/project-versions/ProjectVersionHeader.tsx
====================================================================================================
// Portfolio Admin Experience - Project Version Header v2
'use client'

import { useDocumentInfo, useFormFields } from '@payloadcms/ui'
import { createPortal } from 'react-dom'
import { useEffect, useRef, useState } from 'react'

type FieldState = {
  rows?: unknown[]
  value?: unknown
}

type HeaderValues = {
  breakingChanges: unknown[]
  isCurrent: boolean
  isStable: boolean
  project: unknown
  releaseDate: string
  title: string
  version: string
}

type MountedHeader = {
  host: HTMLElement
  nativeTitle: HTMLElement
  nativeTitleDisplay: string
  hiddenCreateLabels: Array<{
    display: string
    element: HTMLElement
  }>
}

function field(value: unknown): FieldState | undefined {
  return value && typeof value === 'object' ? (value as FieldState) : undefined
}

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function rows(value: unknown): unknown[] {
  const state = field(value)

  if (Array.isArray(state?.rows)) {
    return state.rows
  }

  if (Array.isArray(state?.value)) {
    return state.value
  }

  return []
}

function relationshipLabel(value: unknown): string {
  if (typeof value === 'string' || typeof value === 'number') {
    return 'Assigned project'
  }

  if (!value || typeof value !== 'object') {
    return 'Project not linked'
  }

  const project = value as Record<string, unknown>

  return text(project.title, text(project.name, text(project.slug, 'Assigned project')))
}

function formatDate(value: string): string {
  if (!value) {
    return 'Unscheduled'
  }

  const date = new Date(value)

  if (Number.isNaN(date.getTime())) {
    return 'Invalid date'
  }

  return new Intl.DateTimeFormat('en-GB', {
    day: '2-digit',
    month: 'short',
    year: 'numeric',
  }).format(date)
}

function useHeaderValues(): HeaderValues {
  return useFormFields(([fields]) => ({
    breakingChanges: rows(fields.breakingChanges),
    isCurrent: fields.isCurrent?.value === true,
    isStable: fields.isStable?.value !== false,
    project: fields.project?.value,
    releaseDate: text(fields.releaseDate?.value),
    title: text(fields.title?.value),
    version: text(fields.version?.value),
  }))
}

function HeaderBadge({
  active,
  label,
  tone,
}: {
  active: boolean
  label: string
  tone: 'amber' | 'green' | 'purple'
}) {
  return (
    <span
      className={`portfolio-admin-version-header__badge is-${tone}${active ? ' is-active' : ''}`}
    >
      <i aria-hidden="true" />
      {label}
    </span>
  )
}

function HeaderContent({ isEditing, values }: { isEditing: boolean; values: HeaderValues }) {
  const { breakingChanges, isCurrent, isStable, project, releaseDate, title, version } = values

  const primaryTitle =
    version || title ? [version, title].filter(Boolean).join(' · ') : 'New project version'

  return (
    <section className="portfolio-admin-version-header">
      <div className="portfolio-admin-version-header__identity">
        <span className="portfolio-admin-version-header__eyebrow">
          {isEditing ? 'Release command' : 'Release draft'}
        </span>

        <div className="portfolio-admin-version-header__title-row">
          <h1>{primaryTitle}</h1>

          <span className="portfolio-admin-version-header__project">
            {relationshipLabel(project)}
          </span>
        </div>
      </div>

      <div className="portfolio-admin-version-header__signals">
        <HeaderBadge active={isCurrent} label={isCurrent ? 'Current' : 'Previous'} tone="purple" />

        <HeaderBadge active={isStable} label={isStable ? 'Stable' : 'Unstable'} tone="green" />

        <HeaderBadge
          active={breakingChanges.length > 0}
          label={
            breakingChanges.length > 0 ? `${breakingChanges.length} breaking` : 'Safe migration'
          }
          tone="amber"
        />

        <time dateTime={releaseDate || undefined}>{formatDate(releaseDate)}</time>
      </div>
    </section>
  )
}

function findNativeTitle(): HTMLElement | null {
  const headings = Array.from(document.querySelectorAll('h1'))

  const untitled = headings.find(
    (heading) =>
      !heading.closest('.portfolio-admin-version-header') &&
      heading.textContent?.trim() === '[Untitled]',
  )

  if (untitled instanceof HTMLElement) {
    return untitled
  }

  const fallback = headings.find(
    (heading) =>
      !heading.closest('.portfolio-admin-version-header') &&
      heading.closest('main, .template-default, .collection-edit'),
  )

  return fallback instanceof HTMLElement ? fallback : null
}

function hideCreateLabels(): MountedHeader['hiddenCreateLabels'] {
  const hidden: MountedHeader['hiddenCreateLabels'] = []

  document.querySelectorAll<HTMLElement>('div, p, span, strong').forEach((element) => {
    if (
      element.children.length === 0 &&
      element.textContent?.trim() === 'Creating new Project Version'
    ) {
      hidden.push({
        display: element.style.display,
        element,
      })
      element.style.display = 'none'
    }
  })

  return hidden
}

export function ProjectVersionHeader() {
  const markerRef = useRef<HTMLSpanElement>(null)
  const mountedRef = useRef<MountedHeader | null>(null)
  const [portalHost, setPortalHost] = useState<HTMLElement | null>(null)
  const { isEditing = false } = useDocumentInfo()
  const values = useHeaderValues()

  useEffect(() => {
    let cancelled = false
    let observer: MutationObserver | null = null

    const mount = (): boolean => {
      if (cancelled || mountedRef.current) {
        return Boolean(mountedRef.current)
      }

      const marker = markerRef.current

      if (!marker || !document.body.contains(marker)) {
        return false
      }

      const nativeTitle = findNativeTitle()

      if (!nativeTitle?.parentElement) {
        return false
      }

      const nativeTitleDisplay = nativeTitle.style.display
      nativeTitle.style.display = 'none'

      const host = document.createElement('div')
      host.className = 'portfolio-admin-version-header-host'
      nativeTitle.parentElement.prepend(host)

      mountedRef.current = {
        hiddenCreateLabels: isEditing ? [] : hideCreateLabels(),
        host,
        nativeTitle,
        nativeTitleDisplay,
      }

      setPortalHost(host)
      observer?.disconnect()

      return true
    }

    if (!mount()) {
      observer = new MutationObserver(mount)
      observer.observe(document.body, {
        childList: true,
        subtree: true,
      })
    }

    return () => {
      cancelled = true
      observer?.disconnect()

      const mounted = mountedRef.current

      if (mounted) {
        mounted.hiddenCreateLabels.forEach(({ display, element }) => {
          element.style.display = display
        })

        mounted.nativeTitle.style.display = mounted.nativeTitleDisplay
        mounted.host.remove()
        mountedRef.current = null
      }

      setPortalHost(null)
    }
  }, [isEditing])

  return (
    <>
      <span aria-hidden="true" className="portfolio-admin-version-header-marker" ref={markerRef} />

      {portalHost
        ? createPortal(<HeaderContent isEditing={isEditing} values={values} />, portalHost)
        : null}
    </>
  )
}

====================================================================================================
FILE: src/components/admin/project-versions/ProjectVersionsListHeader.tsx
====================================================================================================
// Portfolio Admin Experience - Project Versions List Header v1
import Link from 'next/link'
import type { BeforeListServerProps, Where } from 'payload'

type Tone = 'amber' | 'cyan' | 'green' | 'purple'

async function countVersions(
  payload: BeforeListServerProps['payload'],
  user: BeforeListServerProps['user'],
  where?: Where,
): Promise<number> {
  try {
    const result = await payload.count({
      collection: 'project-versions',
      overrideAccess: false,
      user,
      ...(where ? { where } : {}),
    })

    return result.totalDocs
  } catch {
    return 0
  }
}

function Metric({
  code,
  description,
  label,
  tone,
  value,
}: {
  code: string
  description: string
  label: string
  tone: Tone
  value: number
}) {
  return (
    <article className={`portfolio-admin-release-metric is-${tone}`}>
      <header>
        <span>{label}</span>
        <code>{code}</code>
      </header>
      <strong>{value}</strong>
      <small>{description}</small>
      <i aria-hidden="true" />
    </article>
  )
}

export default async function ProjectVersionsListHeader({
  hasCreatePermission,
  newDocumentURL,
  payload,
  user,
}: BeforeListServerProps) {
  const [total, current, stable, breaking] = await Promise.all([
    countVersions(payload, user),
    countVersions(payload, user, {
      isCurrent: { equals: true },
    }),
    countVersions(payload, user, {
      isStable: { equals: true },
    }),
    countVersions(payload, user, {
      'breakingChanges.title': { exists: true },
    }),
  ])

  return (
    <section className="portfolio-admin-release-overview">
      <header className="portfolio-admin-release-overview__header">
        <div>
          <span className="portfolio-admin-release-overview__eyebrow">Release registry</span>
          <h1>Version history</h1>
          <p>
            Track release identity, stability, current visibility, and migration risk across {total}{' '}
            {total === 1 ? 'project version' : 'project versions'}.
          </p>
        </div>

        {hasCreatePermission && newDocumentURL ? (
          <Link className="portfolio-admin-button is-primary" href={newDocumentURL}>
            <span aria-hidden="true">+</span>
            New version
          </Link>
        ) : null}
      </header>

      <div className="portfolio-admin-release-overview__metrics">
        <Metric
          code="ALL"
          description="All registered releases"
          label="Total versions"
          tone="cyan"
          value={total}
        />
        <Metric
          code="CUR"
          description="Current visible releases"
          label="Current"
          tone="purple"
          value={current}
        />
        <Metric
          code="STB"
          description="Marked production-stable"
          label="Stable"
          tone="green"
          value={stable}
        />
        <Metric
          code="BRK"
          description="Contain migration warnings"
          label="Breaking"
          tone={breaking > 0 ? 'amber' : 'green'}
          value={breaking}
        />
      </div>
    </section>
  )
}

====================================================================================================
FILE: src/components/admin/projects/ProjectCells.tsx
====================================================================================================
// Portfolio Admin Experience — Projects list cells
'use client'

import Link from 'next/link'
import type { DefaultCellComponentProps } from 'payload'

type UnknownRecord = Record<string, unknown>

type MediaPreview = {
  alt?: string | null
  thumbnailURL?: string | null
  url?: string | null
}

const stageLabels: Record<string, string> = {
  archived: 'Archived',
  development: 'Development',
  idea: 'Idea',
  maintenance: 'Maintenance',
  planning: 'Planning',
  released: 'Released',
  testing: 'Testing',
}

function asRecord(value: unknown): UnknownRecord {
  return value && typeof value === 'object' ? (value as UnknownRecord) : {}
}

function asText(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function getPreview(value: unknown): MediaPreview | null {
  return value && typeof value === 'object' ? (value as MediaPreview) : null
}

function initialsFromTitle(title: string): string {
  const initials = title
    .split(/\s+/)
    .filter(Boolean)
    .slice(0, 2)
    .map((part) => part[0]?.toUpperCase())
    .join('')

  return initials || 'PR'
}

// Portfolio Admin Experience - project edit link v1
export function ProjectTitleCell({ cellData, rowData }: DefaultCellComponentProps) {
  const row = asRecord(rowData)
  const title = asText(cellData, 'Untitled project')
  const slug = asText(row.slug, 'slug-not-set')
  const documentID = typeof row.id === 'string' || typeof row.id === 'number' ? String(row.id) : ''
  const preview = getPreview(row.coverImage)
  const previewURL = preview?.thumbnailURL || preview?.url || null
  const safePreviewURL = previewURL?.replace(/"/g, '%22')
  const editURL = documentID
    ? `/admin/collections/projects/${encodeURIComponent(documentID)}`
    : '/admin/collections/projects'

  return (
    <Link
      aria-label={`Edit project ${title}`}
      className="portfolio-admin-project-cell"
      href={editURL}
    >
      <span
        aria-hidden="true"
        className={`portfolio-admin-project-cell__preview${safePreviewURL ? ' has-image' : ''}`}
        style={safePreviewURL ? { backgroundImage: `url("${safePreviewURL}")` } : undefined}
      >
        {safePreviewURL ? null : initialsFromTitle(title)}
      </span>

      <span className="portfolio-admin-project-cell__copy">
        <strong>{title}</strong>
        <code>{slug}</code>
      </span>

      <span aria-hidden="true" className="portfolio-admin-project-cell__open">
        <svg fill="none" viewBox="0 0 20 20">
          <path
            d="M7 5.5h7.5V13M14.2 5.8 6 14"
            stroke="currentColor"
            strokeLinecap="round"
            strokeLinejoin="round"
            strokeWidth="1.5"
          />
        </svg>
      </span>
    </Link>
  )
}

export function ProjectStageCell({ cellData }: DefaultCellComponentProps) {
  const stage = asText(cellData, 'idea').toLowerCase()

  return (
    <span className={`portfolio-admin-stage is-${stage}`}>
      <i aria-hidden="true" />
      {stageLabels[stage] || stage}
    </span>
  )
}

export function ProjectProgressCell({ cellData }: DefaultCellComponentProps) {
  const rawValue = typeof cellData === 'number' ? cellData : Number(cellData)
  const progress = Number.isFinite(rawValue) ? Math.min(100, Math.max(0, rawValue)) : 0

  return (
    <div className="portfolio-admin-progress-cell">
      <span className="portfolio-admin-progress-cell__track">
        <span style={{ width: `${progress}%` }} />
      </span>
      <code>{Math.round(progress)}%</code>
    </div>
  )
}

export function ProjectFeaturedCell({ cellData }: DefaultCellComponentProps) {
  const featured = cellData === true

  return (
    <span
      aria-label={featured ? 'Featured project' : 'Standard project'}
      className={`portfolio-admin-featured-cell${featured ? ' is-featured' : ''}`}
      role="img"
      title={featured ? 'Featured project' : 'Standard project'}
    >
      <svg aria-hidden="true" fill="none" viewBox="0 0 24 24">
        <path
          d="m12 3 2.7 5.47 6.04.88-4.37 4.26 1.03 6.02L12 16.8l-5.4 2.83 1.03-6.02-4.37-4.26 6.04-.88L12 3Z"
          stroke="currentColor"
          strokeLinejoin="round"
          strokeWidth="1.7"
        />
      </svg>
    </span>
  )
}

export function ProjectPublishedCell({ cellData }: DefaultCellComponentProps) {
  const dateValue = typeof cellData === 'string' ? cellData : null
  const parsedDate = dateValue ? new Date(dateValue) : null
  const isValid = parsedDate && !Number.isNaN(parsedDate.getTime())
  const formatted = isValid
    ? new Intl.DateTimeFormat('en-GB', {
        day: '2-digit',
        month: 'short',
        year: 'numeric',
      }).format(parsedDate)
    : null

  return (
    <span className={`portfolio-admin-publication-cell${formatted ? ' is-published' : ''}`}>
      <i aria-hidden="true" />
      <span>
        <strong>{formatted ? 'Published' : 'Draft'}</strong>
        <small>{formatted || 'Publication date not set'}</small>
      </span>
    </span>
  )
}

====================================================================================================
FILE: src/components/admin/projects/ProjectDocumentControls.tsx
====================================================================================================
// Portfolio Admin Experience - Project Document Controls v1
'use client'

import Link from 'next/link'
import { useDocumentInfo, useFormFields } from '@payloadcms/ui'

type State = { value?: unknown }

const labels: Record<string, string> = {
  archived: 'Archived',
  development: 'Development',
  idea: 'Idea',
  maintenance: 'Maintenance',
  planning: 'Planning',
  released: 'Released',
  testing: 'Testing',
}

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function updatedAt(value: unknown): string | null {
  if (typeof value !== 'string' && typeof value !== 'number' && !(value instanceof Date)) {
    return null
  }

  const date = value instanceof Date ? value : new Date(value)
  if (Number.isNaN(date.getTime())) return null

  return new Intl.DateTimeFormat('en-GB', {
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
    month: 'short',
  }).format(date)
}

export default function ProjectDocumentControls() {
  const { isEditing, lastUpdateTime } = useDocumentInfo()
  const data = useFormFields(([fields]) => ({
    slug: (fields.slug as State | undefined)?.value,
    stage: (fields.stage as State | undefined)?.value,
    version: (fields.currentVersion as State | undefined)?.value,
  }))

  const stage = text(data.stage, 'idea').toLowerCase()
  const slug = text(data.slug)
  const version = text(data.version, 'Unversioned')
  const updated = updatedAt(lastUpdateTime)

  return (
    <div className="portfolio-admin-project-command">
      <span className={`portfolio-admin-project-command__stage is-${stage}`}>
        <i aria-hidden="true" />
        {labels[stage] || stage}
      </span>

      <span className="portfolio-admin-project-command__version">
        <small>Version</small>
        <code>{version}</code>
      </span>

      {isEditing && updated ? (
        <span className="portfolio-admin-project-command__updated">
          Updated <time>{updated}</time>
        </span>
      ) : null}

      {slug ? (
        <Link
          className="portfolio-admin-project-command__preview"
          href={`/projects/${encodeURIComponent(slug)}`}
          target="_blank"
        >
          Preview <span aria-hidden="true">↗</span>
        </Link>
      ) : (
        <span className="portfolio-admin-project-command__preview is-disabled">Preview</span>
      )}
    </div>
  )
}

====================================================================================================
FILE: src/components/admin/projects/ProjectHealthPanel.tsx
====================================================================================================
// Portfolio Admin Experience - Project Health Panel v1
'use client'

import Link from 'next/link'
import { useFormFields } from '@payloadcms/ui'

type FieldState = {
  rows?: unknown[]
  value?: unknown
}

type HealthCheck = {
  description: string
  label: string
  ready: boolean
}

function asField(value: unknown): FieldState | undefined {
  return value && typeof value === 'object' ? (value as FieldState) : undefined
}

function hasText(value: unknown): boolean {
  return typeof value === 'string' && value.trim().length > 0
}

function hasValue(value: unknown): boolean {
  if (value === null || value === undefined || value === '') {
    return false
  }

  if (Array.isArray(value)) {
    return value.length > 0
  }

  return true
}

function hasRows(field: FieldState | undefined): boolean {
  return Array.isArray(field?.rows) && field.rows.length > 0
}

function hasRichText(value: unknown): boolean {
  if (!value || typeof value !== 'object') {
    return false
  }

  try {
    const serialized = JSON.stringify(value)
      .replace(/"type":"[^"]+"/g, '')
      .replace(/["{}\[\],:]/g, '')
      .trim()

    return serialized.length > 12
  } catch {
    return false
  }
}

export default function ProjectHealthPanel() {
  const fields = useFormFields(([formFields]) => formFields)

  const read = (path: string): FieldState | undefined => asField(fields[path])

  const title = read('title')?.value
  const slug = read('slug')?.value
  const excerpt = read('excerpt')?.value
  const description = read('description')?.value
  const coverImage = read('coverImage')?.value
  const category = read('category')?.value
  const techStack = read('techStack')?.value
  const highlights = read('highlights')
  const links = read('links')
  const githubURL = read('github.url')?.value
  const metaTitle = read('seo.metaTitle')?.value
  const metaDescription = read('seo.metaDescription')?.value

  const checks: HealthCheck[] = [
    {
      label: 'Identity',
      description: 'Title, slug, and summary',
      ready: hasText(title) && hasText(slug) && hasText(excerpt),
    },
    {
      label: 'Case study',
      description: 'Long-form story and highlights',
      ready: hasRichText(description) && hasRows(highlights),
    },
    {
      label: 'Media',
      description: 'Primary project cover',
      ready: hasValue(coverImage),
    },
    {
      label: 'Classification',
      description: 'Category and technology stack',
      ready: hasValue(category) && hasValue(techStack),
    },
    {
      label: 'Destinations',
      description: 'Repository or project links',
      ready: hasText(githubURL) || hasRows(links),
    },
    {
      label: 'SEO',
      description: 'Search title and description',
      ready: hasText(metaTitle) && hasText(metaDescription),
    },
  ]

  const readyCount = checks.filter((check) => check.ready).length
  const score = Math.round((readyCount / checks.length) * 100)
  const slugValue = hasText(slug) ? String(slug).trim() : ''

  return (
    <aside className="portfolio-admin-project-health">
      <header className="portfolio-admin-project-health__header">
        <div>
          <span>Project readiness</span>
          <strong>{score}%</strong>
        </div>

        <span
          aria-label={`${readyCount} of ${checks.length} checks complete`}
          className="portfolio-admin-project-health__meter"
          role="img"
          style={{ '--project-health-score': `${score}%` } as React.CSSProperties}
        >
          <i />
        </span>
      </header>

      <p className="portfolio-admin-project-health__summary">
        {readyCount === checks.length
          ? 'All core publishing checks are complete.'
          : `${checks.length - readyCount} publishing ${
              checks.length - readyCount === 1 ? 'check needs' : 'checks need'
            } attention.`}
      </p>

      <div className="portfolio-admin-project-health__checks">
        {checks.map((check) => (
          <div
            className={`portfolio-admin-project-health__check${check.ready ? ' is-ready' : ''}`}
            key={check.label}
          >
            <span aria-hidden="true">
              {check.ready ? (
                <svg fill="none" viewBox="0 0 20 20">
                  <path
                    d="m5 10.2 3.1 3.1L15.4 6"
                    stroke="currentColor"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    strokeWidth="1.7"
                  />
                </svg>
              ) : (
                <svg fill="none" viewBox="0 0 20 20">
                  <circle cx="10" cy="10" r="3.2" stroke="currentColor" strokeWidth="1.5" />
                </svg>
              )}
            </span>
            <div>
              <strong>{check.label}</strong>
              <small>{check.description}</small>
            </div>
          </div>
        ))}
      </div>

      {slugValue ? (
        <Link
          className="portfolio-admin-project-health__preview"
          href={`/projects/${encodeURIComponent(slugValue)}`}
          target="_blank"
        >
          Preview project
          <span aria-hidden="true">↗</span>
        </Link>
      ) : (
        <span className="portfolio-admin-project-health__preview is-disabled">
          Add a slug to enable preview
        </span>
      )}
    </aside>
  )
}

====================================================================================================
FILE: src/components/admin/projects/ProjectProgressField.tsx
====================================================================================================
// Portfolio Admin Experience - Project Progress Field v1
'use client'

import type { CSSProperties } from 'react'
import { useField } from '@payloadcms/ui'

type Props = { path: string }

function clamp(value: unknown): number {
  const number = typeof value === 'number' ? value : Number(value)
  return Number.isFinite(number) ? Math.min(100, Math.max(0, Math.round(number))) : 0
}

export default function ProjectProgressField({ path }: Props) {
  const { readOnly, setValue, value } = useField<number>({ path })
  const progress = clamp(value)
  const style = { '--project-progress': `${progress}%` } as CSSProperties
  const update = (next: unknown) => {
    if (!readOnly) setValue(clamp(next))
  }

  return (
    <div className="portfolio-admin-project-progress-field" style={style}>
      <div className="portfolio-admin-project-progress-field__head">
        <label htmlFor={`${path}-number`}>Progress</label>
        <output htmlFor={`${path}-range ${path}-number`}>{progress}%</output>
      </div>

      <div className="portfolio-admin-project-progress-field__controls">
        <input
          aria-label="Project progress"
          disabled={readOnly}
          id={`${path}-range`}
          max={100}
          min={0}
          onChange={(event) => update(event.currentTarget.value)}
          step={1}
          type="range"
          value={progress}
        />

        <label className="portfolio-admin-project-progress-field__number">
          <span className="sr-only">Exact progress percentage</span>
          <input
            disabled={readOnly}
            id={`${path}-number`}
            max={100}
            min={0}
            onChange={(event) => update(event.currentTarget.value)}
            step={1}
            type="number"
            value={progress}
          />
          <span aria-hidden="true">%</span>
        </label>
      </div>

      <p>Track delivery completion from 0 to 100.</p>
    </div>
  )
}

====================================================================================================
FILE: src/components/admin/projects/ProjectsListHeader.tsx
====================================================================================================
// Portfolio Admin Experience — Projects list overview
import Link from 'next/link'
import type { BeforeListServerProps, Where } from 'payload'

type MetricProps = {
  code: string
  description: string
  label: string
  tone: 'cyan' | 'green' | 'purple' | 'amber'
  value: number
}

async function countProjects(
  payload: BeforeListServerProps['payload'],
  user: BeforeListServerProps['user'],
  where?: Where,
): Promise<number> {
  try {
    const result = await payload.count({
      collection: 'projects',
      overrideAccess: false,
      user,
      ...(where ? { where } : {}),
    })

    return result.totalDocs
  } catch {
    return 0
  }
}

function Metric({ code, description, label, tone, value }: MetricProps) {
  return (
    <article className={`portfolio-admin-project-stat is-${tone}`}>
      <div className="portfolio-admin-project-stat__topline">
        <span>{label}</span>
        <code>{code}</code>
      </div>
      <strong>{value}</strong>
      <p>{description}</p>
      <span aria-hidden="true" className="portfolio-admin-project-stat__signal">
        <i />
        <i />
        <i />
      </span>
    </article>
  )
}

export default async function ProjectsListHeader({
  hasCreatePermission,
  newDocumentURL,
  payload,
  user,
}: BeforeListServerProps) {
  const [total, published, active, featured, missingCover] = await Promise.all([
    countProjects(payload, user),
    countProjects(payload, user, {
      publishedAt: {
        exists: true,
      },
    }),
    countProjects(payload, user, {
      stage: {
        in: ['planning', 'development', 'testing'],
      },
    }),
    countProjects(payload, user, {
      isFeatured: {
        equals: true,
      },
    }),
    countProjects(payload, user, {
      coverImage: {
        exists: false,
      },
    }),
  ])

  return (
    <section className="portfolio-admin-projects-overview">
      <header className="portfolio-admin-projects-overview__header">
        <div>
          <span className="portfolio-admin-projects-overview__eyebrow">Project registry</span>
          <h2>Delivery portfolio</h2>
          <p>
            Track publication readiness, delivery stage, featured placement, and media health across{' '}
            {total} {total === 1 ? 'project' : 'projects'}.
          </p>
        </div>

        {hasCreatePermission && newDocumentURL ? (
          <Link className="portfolio-admin-button is-primary" href={newDocumentURL}>
            <span aria-hidden="true">+</span>
            New project
          </Link>
        ) : null}
      </header>

      <div className="portfolio-admin-projects-overview__metrics">
        <Metric
          code="PUB"
          description="Have a publication date"
          label="Published"
          tone="cyan"
          value={published}
        />
        <Metric
          code="RUN"
          description="Planning, development, or testing"
          label="In progress"
          tone="purple"
          value={active}
        />
        <Metric
          code="FTR"
          description="Selected for prominent placement"
          label="Featured"
          tone="green"
          value={featured}
        />
        <Metric
          code="MED"
          description="Missing a primary cover image"
          label="Needs media"
          tone={missingCover > 0 ? 'amber' : 'green'}
          value={missingCover}
        />
      </div>
    </section>
  )
}

====================================================================================================
FILE: src/components/admin/taxonomy/CategoriesListHeader.tsx
====================================================================================================
// Portfolio Admin Experience - Categories List Header v1
import Link from 'next/link'
import type { BeforeListServerProps, Where } from 'payload'

type Tone = 'amber' | 'cyan' | 'green' | 'purple'

async function countCategories(
  payload: BeforeListServerProps['payload'],
  user: BeforeListServerProps['user'],
  where?: Where,
): Promise<number> {
  try {
    const result = await payload.count({
      collection: 'categories',
      overrideAccess: false,
      user,
      ...(where ? { where } : {}),
    })

    return result.totalDocs
  } catch {
    return 0
  }
}

function Metric({
  code,
  description,
  label,
  tone,
  value,
}: {
  code: string
  description: string
  label: string
  tone: Tone
  value: number
}) {
  return (
    <article className={`portfolio-admin-taxonomy-metric is-${tone}`}>
      <header>
        <span>{label}</span>
        <code>{code}</code>
      </header>
      <strong>{value}</strong>
      <small>{description}</small>
      <i aria-hidden="true" />
    </article>
  )
}

export default async function CategoriesListHeader({
  hasCreatePermission,
  newDocumentURL,
  payload,
  user,
}: BeforeListServerProps) {
  const [total, project, blog, reusable] = await Promise.all([
    countCategories(payload, user),
    countCategories(payload, user, {
      type: { equals: 'project' },
    }),
    countCategories(payload, user, {
      type: { equals: 'blog' },
    }),
    countCategories(payload, user, {
      or: [
        { type: { equals: 'tech-stack' } },
        { type: { equals: 'shared' } },
      ],
    }),
  ])

  return (
    <section className="portfolio-admin-taxonomy-overview is-categories">
      <header className="portfolio-admin-taxonomy-overview__header">
        <div>
          <span className="portfolio-admin-taxonomy-overview__eyebrow">
            Taxonomy architecture
          </span>
          <h1>Category map</h1>
          <p>
            Organize project, editorial, and reusable classification
            across {total} {total === 1 ? 'category' : 'categories'}.
          </p>
        </div>

        {hasCreatePermission && newDocumentURL ? (
          <Link
            className="portfolio-admin-button is-primary"
            href={newDocumentURL}
          >
            <span aria-hidden="true">+</span>
            New category
          </Link>
        ) : null}
      </header>

      <div className="portfolio-admin-taxonomy-overview__metrics">
        <Metric
          code="ALL"
          description="All taxonomy records"
          label="Total categories"
          tone="cyan"
          value={total}
        />
        <Metric
          code="PRJ"
          description="Project classification"
          label="Project"
          tone="purple"
          value={project}
        />
        <Metric
          code="BLG"
          description="Editorial classification"
          label="Blog"
          tone="green"
          value={blog}
        />
        <Metric
          code="USE"
          description="Technology and shared taxonomy"
          label="Reusable"
          tone="amber"
          value={reusable}
        />
      </div>
    </section>
  )
}

====================================================================================================
FILE: src/components/admin/taxonomy/CategoryCells.tsx
====================================================================================================
// Portfolio Admin Experience - Category Cells v1
'use client'

import Link from 'next/link'
import type { DefaultCellComponentProps } from 'payload'

type RecordValue = Record<string, unknown>

function record(value: unknown): RecordValue {
  return value && typeof value === 'object'
    ? (value as RecordValue)
    : {}
}

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim()
    ? value.trim()
    : fallback
}

function relationshipLabel(value: unknown): string {
  if (typeof value === 'string' || typeof value === 'number') {
    return 'Assigned category'
  }

  const category = record(value)
  return text(category.title, text(category.name, 'Assigned category'))
}

export function CategoryTitleCell({
  cellData,
  rowData,
}: DefaultCellComponentProps) {
  const row = record(rowData)
  const title = text(cellData, 'Untitled category')
  const slug = text(row.slug, 'slug-not-set')
  const description = text(row.description, 'No description')
  const id =
    typeof row.id === 'string' || typeof row.id === 'number'
      ? String(row.id)
      : ''
  const href = id
    ? `/admin/collections/categories/${encodeURIComponent(id)}`
    : '/admin/collections/categories'

  return (
    <Link className="portfolio-admin-category-cell" href={href}>
      <span className="portfolio-admin-category-cell__mark">
        {title.slice(0, 2).toUpperCase()}
      </span>
      <span className="portfolio-admin-category-cell__copy">
        <strong>{title}</strong>
        <small>
          <code>{slug}</code>
          <span aria-hidden="true"> · </span>
          {description}
        </small>
      </span>
      <span
        aria-hidden="true"
        className="portfolio-admin-category-cell__arrow"
      >
        ↗
      </span>
    </Link>
  )
}

export function CategoryTypeCell({
  cellData,
}: DefaultCellComponentProps) {
  const type = text(cellData, 'shared').toLowerCase()
  const labels: Record<string, string> = {
    blog: 'Blog',
    project: 'Project',
    shared: 'Shared',
    'tech-stack': 'Tech Stack',
  }

  return (
    <span className={`portfolio-admin-taxonomy-badge is-${type}`}>
      <i aria-hidden="true" />
      {labels[type] || type}
    </span>
  )
}

export function CategoryParentCell({
  cellData,
}: DefaultCellComponentProps) {
  return (
    <span
      className={`portfolio-admin-taxonomy-parent${
        cellData ? '' : ' is-root'
      }`}
    >
      <i aria-hidden="true" />
      {cellData ? relationshipLabel(cellData) : 'Root category'}
    </span>
  )
}

export function CategorySortCell({
  cellData,
}: DefaultCellComponentProps) {
  const order = Number(cellData)

  return (
    <code className="portfolio-admin-taxonomy-order">
      {Number.isFinite(order) ? order : 0}
    </code>
  )
}

====================================================================================================
FILE: src/components/admin/taxonomy/CategoryEditor.tsx
====================================================================================================
// Portfolio Admin Experience - Category Editor v1
'use client'

import { useFormFields } from '@payloadcms/ui'
import type { CSSProperties, ReactNode } from 'react'

type CategoryType = 'blog' | 'project' | 'shared' | 'tech-stack'

type CategoryValues = {
  description: string
  parent: unknown
  slug: string
  sortOrder: number
  title: string
  type: CategoryType
}

const TYPE_META: Record<
  CategoryType,
  {
    code: string
    description: string
    label: string
    tone: string
  }
> = {
  blog: {
    code: 'BLG',
    description: 'Editorial taxonomy for articles, journal entries, and publishing.',
    label: 'Blog',
    tone: 'green',
  },
  project: {
    code: 'PRJ',
    description: 'Portfolio taxonomy for case studies, products, and delivery work.',
    label: 'Project',
    tone: 'purple',
  },
  shared: {
    code: 'SHR',
    description: 'Reusable taxonomy that may be shared across multiple content areas.',
    label: 'Shared',
    tone: 'amber',
  },
  'tech-stack': {
    code: 'TEC',
    description: 'Technology taxonomy for frameworks, infrastructure, and tooling.',
    label: 'Tech Stack',
    tone: 'cyan',
  },
}

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function number(value: unknown, fallback = 0): number {
  const parsed = Number(value)

  return Number.isFinite(parsed) ? parsed : fallback
}

function categoryType(value: unknown): CategoryType {
  return value === 'blog' || value === 'project' || value === 'shared' || value === 'tech-stack'
    ? value
    : 'shared'
}

function parentLabel(value: unknown): string {
  if (typeof value === 'string' || typeof value === 'number') {
    return 'Assigned parent'
  }

  if (!value || typeof value !== 'object') {
    return 'Root'
  }

  const parent = value as Record<string, unknown>

  return text(parent.title, text(parent.name, text(parent.label, 'Assigned parent')))
}

function useCategoryValues(): CategoryValues {
  return useFormFields(([fields]) => ({
    description: text(fields.description?.value),
    parent: fields.parent?.value,
    slug: text(fields.slug?.value),
    sortOrder: number(fields.sortOrder?.value),
    title: text(fields.title?.value),
    type: categoryType(fields.type?.value),
  }))
}

function StatusPill({ children, tone }: { children: ReactNode; tone: string }) {
  return (
    <span className={`portfolio-admin-category-status is-${tone}`}>
      <i aria-hidden="true" />
      {children}
    </span>
  )
}

export function CategoryWorkspace() {
  const { description, parent, slug, sortOrder, title, type } = useCategoryValues()
  const meta = TYPE_META[type]
  const resolvedTitle = title || 'Untitled category'
  const resolvedSlug = slug || 'slug-not-generated'
  const resolvedParent = parentLabel(parent)

  return (
    <section className="portfolio-admin-category-workspace">
      <div className="portfolio-admin-category-workspace__copy">
        <span className="portfolio-admin-category-workspace__eyebrow">Taxonomy node</span>

        <div className="portfolio-admin-category-workspace__title-row">
          <h2>{resolvedTitle}</h2>
          <StatusPill tone={meta.tone}>{meta.label}</StatusPill>
        </div>

        <code>/{resolvedSlug}</code>

        <p>
          {description ||
            'Add a concise description so editors understand when this category should be used.'}
        </p>

        <dl>
          <div>
            <dt>Parent</dt>
            <dd>{resolvedParent}</dd>
          </div>
          <div>
            <dt>Order</dt>
            <dd>{sortOrder}</dd>
          </div>
          <div>
            <dt>Scope</dt>
            <dd>{meta.code}</dd>
          </div>
        </dl>
      </div>

      <div
        aria-label={`${resolvedTitle} taxonomy node preview`}
        className={`portfolio-admin-category-node is-${meta.tone}`}
      >
        <span className="portfolio-admin-category-node__parent">{resolvedParent}</span>
        <i aria-hidden="true" />
        <div>
          <small>{meta.code}</small>
          <strong>{resolvedTitle}</strong>
          <code>{resolvedSlug}</code>
        </div>
        <span className="portfolio-admin-category-node__scope">{meta.label}</span>
      </div>
    </section>
  )
}

export function CategoryHierarchy() {
  const { parent, title, type } = useCategoryValues()
  const meta = TYPE_META[type]
  const resolvedParent = parentLabel(parent)
  const resolvedTitle = title || 'Current category'

  return (
    <section className="portfolio-admin-category-hierarchy">
      <header>
        <div>
          <span>Hierarchy preview</span>
          <strong>{parent ? 'Nested taxonomy node' : 'Root taxonomy node'}</strong>
        </div>
        <StatusPill tone={meta.tone}>{meta.code}</StatusPill>
      </header>

      <div className="portfolio-admin-category-hierarchy__path">
        <span className="is-root">Taxonomy</span>
        <i aria-hidden="true">/</i>
        {parent ? (
          <>
            <span>{resolvedParent}</span>
            <i aria-hidden="true">/</i>
          </>
        ) : null}
        <strong>{resolvedTitle}</strong>
      </div>

      <p>{meta.description}</p>
    </section>
  )
}

function ReadinessCheck({
  complete,
  description,
  label,
}: {
  complete: boolean
  description: string
  label: string
}) {
  return (
    <li className={complete ? 'is-complete' : ''}>
      <span aria-hidden="true">{complete ? '✓' : '○'}</span>
      <div>
        <strong>{label}</strong>
        <small>{description}</small>
      </div>
    </li>
  )
}

export function CategoryReadiness() {
  const { description, slug, title, type } = useCategoryValues()

  const checks = [
    {
      complete: Boolean(title),
      description: 'Readable category name',
      label: 'Title',
    },
    {
      complete: Boolean(slug),
      description: 'Stable URL identifier',
      label: 'Slug',
    },
    {
      complete: Boolean(type),
      description: 'Content usage scope',
      label: 'Classification',
    },
    {
      complete: Boolean(description),
      description: 'Editorial usage guidance',
      label: 'Description',
    },
  ]

  const complete = checks.filter((check) => check.complete).length
  const percentage = Math.round((complete / checks.length) * 100)
  const remaining = checks.length - complete

  return (
    <aside
      className="portfolio-admin-category-readiness"
      style={
        {
          '--category-readiness': `${percentage}%`,
        } as CSSProperties
      }
    >
      <header>
        <div>
          <span>Category readiness</span>
          <strong>{percentage}%</strong>
        </div>
        <i aria-hidden="true">
          <b />
        </i>
      </header>

      <p>
        {remaining === 0
          ? 'This taxonomy node is ready to use.'
          : `${remaining} ${remaining === 1 ? 'check needs' : 'checks need'} attention.`}
      </p>

      <ul>
        {checks.map((check) => (
          <ReadinessCheck key={check.label} {...check} />
        ))}
      </ul>
    </aside>
  )
}

====================================================================================================
FILE: src/components/admin/taxonomy/TechStackCells.tsx
====================================================================================================
// Portfolio Admin Experience - Tech Stack Cells v1
'use client'

import Link from 'next/link'
import type { CSSProperties } from 'react'
import type { DefaultCellComponentProps } from 'payload'

type RecordValue = Record<string, unknown>

function record(value: unknown): RecordValue {
  return value && typeof value === 'object'
    ? (value as RecordValue)
    : {}
}

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim()
    ? value.trim()
    : fallback
}

function safeColor(value: unknown): string {
  const color = text(value)
  return /^#[0-9a-f]{3,8}$/iu.test(color)
    ? color
    : 'var(--portfolio-admin-cyan)'
}

function relationshipLabel(value: unknown): string {
  if (typeof value === 'string' || typeof value === 'number') {
    return 'Assigned'
  }

  const category = record(value)
  return text(category.title, text(category.name, 'Assigned'))
}

export function TechnologyNameCell({
  cellData,
  rowData,
}: DefaultCellComponentProps) {
  const row = record(rowData)
  const name = text(cellData, 'Unnamed technology')
  const slug = text(row.slug, 'icon-key-not-set')
  const description = text(row.description, 'No description')
  const color = safeColor(row.color)
  const id =
    typeof row.id === 'string' || typeof row.id === 'number'
      ? String(row.id)
      : ''
  const href = id
    ? `/admin/collections/tech-stack/${encodeURIComponent(id)}`
    : '/admin/collections/tech-stack'

  return (
    <Link className="portfolio-admin-technology-cell" href={href}>
      <span
        className="portfolio-admin-technology-cell__mark"
        style={
          {
            '--technology-color': color,
          } as CSSProperties
        }
      >
        {name.slice(0, 2).toUpperCase()}
      </span>
      <span className="portfolio-admin-technology-cell__copy">
        <strong>{name}</strong>
        <small>
          <code>{slug}</code>
          <span aria-hidden="true"> · </span>
          {description}
        </small>
      </span>
      <span
        aria-hidden="true"
        className="portfolio-admin-technology-cell__arrow"
      >
        ↗
      </span>
    </Link>
  )
}

export function TechnologyCategoryCell({
  cellData,
}: DefaultCellComponentProps) {
  return (
    <span
      className={`portfolio-admin-technology-category${
        cellData ? '' : ' is-missing'
      }`}
    >
      {cellData ? relationshipLabel(cellData) : 'Uncategorized'}
    </span>
  )
}

function BooleanBadge({
  active,
  activeLabel,
  inactiveLabel,
  tone,
}: {
  active: boolean
  activeLabel: string
  inactiveLabel: string
  tone: 'green' | 'purple'
}) {
  return (
    <span
      className={`portfolio-admin-boolean-badge is-${tone}${
        active ? ' is-active' : ''
      }`}
    >
      <i aria-hidden="true" />
      {active ? activeLabel : inactiveLabel}
    </span>
  )
}

export function TechnologyFeaturedCell({
  cellData,
}: DefaultCellComponentProps) {
  return (
    <BooleanBadge
      active={cellData === true}
      activeLabel="Featured"
      inactiveLabel="Standard"
      tone="purple"
    />
  )
}

export function TechnologyVisibleCell({
  cellData,
}: DefaultCellComponentProps) {
  return (
    <BooleanBadge
      active={cellData === true}
      activeLabel="Visible"
      inactiveLabel="Hidden"
      tone="green"
    />
  )
}

export function TechnologyLinksCell({
  cellData,
  rowData,
}: DefaultCellComponentProps) {
  const row = record(rowData)
  const officialURL = text(cellData)
  const documentationURL = text(row.documentationUrl)

  return (
    <span className="portfolio-admin-technology-links">
      {officialURL ? (
        <a href={officialURL} rel="noreferrer" target="_blank">
          Official ↗
        </a>
      ) : (
        <span>Missing website</span>
      )}
      {documentationURL ? (
        <a href={documentationURL} rel="noreferrer" target="_blank">
          Docs ↗
        </a>
      ) : (
        <span>Missing docs</span>
      )}
    </span>
  )
}

export function TechnologySortCell({
  cellData,
}: DefaultCellComponentProps) {
  const order = Number(cellData)

  return (
    <code className="portfolio-admin-taxonomy-order">
      {Number.isFinite(order) ? order : 0}
    </code>
  )
}

====================================================================================================
FILE: src/components/admin/taxonomy/TechStackEditor.tsx
====================================================================================================
// Portfolio Admin Experience - Tech Stack Editor v1
'use client'

import { useFormFields } from '@payloadcms/ui'
import type { CSSProperties, ReactNode } from 'react'

type TechnologyValues = {
  category: unknown
  color: string
  description: string
  documentationUrl: string
  featured: boolean
  name: string
  officialUrl: string
  slug: string
  sortOrder: number
  visible: boolean
}

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function number(value: unknown, fallback = 0): number {
  const parsed = Number(value)

  return Number.isFinite(parsed) ? parsed : fallback
}

function boolean(value: unknown, fallback = false): boolean {
  return typeof value === 'boolean' ? value : fallback
}

function relationshipLabel(value: unknown): string {
  if (typeof value === 'string' || typeof value === 'number') {
    return 'Assigned category'
  }

  if (!value || typeof value !== 'object') {
    return 'Uncategorized'
  }

  const category = value as Record<string, unknown>

  return text(category.title, text(category.name, text(category.label, 'Assigned category')))
}

function safeColor(value: unknown): string {
  const color = text(value)

  return /^#[0-9a-f]{3,8}$/iu.test(color) ? color : '#32d7e7'
}

function isValidURL(value: string): boolean {
  if (!value) {
    return false
  }

  try {
    const url = new URL(value)

    return url.protocol === 'http:' || url.protocol === 'https:'
  } catch {
    return false
  }
}

function useTechnologyValues(): TechnologyValues {
  return useFormFields(([fields]) => ({
    category: fields.category?.value,
    color: text(fields.color?.value),
    description: text(fields.description?.value),
    documentationUrl: text(fields.documentationUrl?.value),
    featured: boolean(fields.featured?.value),
    name: text(fields.name?.value),
    officialUrl: text(fields.officialUrl?.value),
    slug: text(fields.slug?.value),
    sortOrder: number(fields.sortOrder?.value),
    visible: boolean(fields.visible?.value, true),
  }))
}

function StatusPill({
  active,
  children,
  tone,
}: {
  active: boolean
  children: ReactNode
  tone: 'cyan' | 'green' | 'purple'
}) {
  return (
    <span className={`portfolio-admin-technology-status is-${tone}${active ? ' is-active' : ''}`}>
      <i aria-hidden="true" />
      {children}
    </span>
  )
}

function ResourceLink({ href, label }: { href: string; label: string }) {
  const valid = isValidURL(href)

  return valid ? (
    <a href={href} rel="noreferrer" target="_blank">
      <span>{label}</span>
      <i aria-hidden="true">↗</i>
    </a>
  ) : (
    <span className="is-missing">
      <span>{label}</span>
      <i aria-hidden="true">—</i>
    </span>
  )
}

export function TechnologyWorkspace() {
  const {
    category,
    color,
    description,
    documentationUrl,
    featured,
    name,
    officialUrl,
    slug,
    sortOrder,
    visible,
  } = useTechnologyValues()

  const resolvedName = name || 'Unnamed technology'
  const resolvedSlug = slug || 'icon-key-not-set'
  const resolvedColor = safeColor(color)
  const resolvedCategory = relationshipLabel(category)
  const initials = resolvedName.slice(0, 2).toUpperCase()

  return (
    <section
      className="portfolio-admin-technology-workspace"
      style={
        {
          '--technology-brand': resolvedColor,
        } as CSSProperties
      }
    >
      <div className="portfolio-admin-technology-workspace__copy">
        <span className="portfolio-admin-technology-workspace__eyebrow">Technology profile</span>

        <div className="portfolio-admin-technology-workspace__title-row">
          <h2>{resolvedName}</h2>

          <div className="portfolio-admin-technology-workspace__statuses">
            <StatusPill active={visible} tone="green">
              {visible ? 'Visible' : 'Hidden'}
            </StatusPill>

            <StatusPill active={featured} tone="purple">
              {featured ? 'Featured' : 'Standard'}
            </StatusPill>
          </div>
        </div>

        <code>{resolvedSlug}</code>

        <p>
          {description ||
            'Add a concise explanation so editors and visitors understand where this technology fits.'}
        </p>

        <dl>
          <div>
            <dt>Category</dt>
            <dd>{resolvedCategory}</dd>
          </div>

          <div>
            <dt>Brand</dt>
            <dd>{color || resolvedColor}</dd>
          </div>

          <div>
            <dt>Order</dt>
            <dd>{sortOrder}</dd>
          </div>
        </dl>

        <div className="portfolio-admin-technology-workspace__links">
          <ResourceLink href={officialUrl} label="Official" />
          <ResourceLink href={documentationUrl} label="Documentation" />
        </div>
      </div>

      <div
        aria-label={`${resolvedName} brand preview`}
        className="portfolio-admin-technology-orbit"
      >
        <span className="portfolio-admin-technology-orbit__label">Brand signal</span>

        <i aria-hidden="true" className="is-outer" />
        <i aria-hidden="true" className="is-inner" />

        <div className="portfolio-admin-technology-orbit__core">
          <span>{initials}</span>
          <strong>{resolvedName}</strong>
          <code>{resolvedSlug}</code>
        </div>

        <span className="portfolio-admin-technology-orbit__category">{resolvedCategory}</span>
      </div>
    </section>
  )
}

export function TechnologyClassification() {
  const { category, featured, name, visible } = useTechnologyValues()
  const resolvedCategory = relationshipLabel(category)

  return (
    <section className="portfolio-admin-technology-classification">
      <header>
        <div>
          <span>Registry placement</span>
          <strong>{name || 'Current technology'}</strong>
        </div>

        <code>{category ? 'ASSIGNED' : 'UNSORTED'}</code>
      </header>

      <div className="portfolio-admin-technology-classification__path">
        <span>Technology registry</span>
        <i aria-hidden="true">/</i>
        <strong>{resolvedCategory}</strong>
        <i aria-hidden="true">/</i>
        <b>{name || 'Current technology'}</b>
      </div>

      <div className="portfolio-admin-technology-classification__states">
        <StatusPill active={visible} tone="green">
          {visible ? 'Publicly visible' : 'Hidden from public pages'}
        </StatusPill>

        <StatusPill active={featured} tone="purple">
          {featured ? 'Homepage highlight' : 'Standard catalog item'}
        </StatusPill>
      </div>
    </section>
  )
}

export function TechnologyResources() {
  const { documentationUrl, officialUrl } = useTechnologyValues()
  const officialReady = isValidURL(officialUrl)
  const documentationReady = isValidURL(documentationUrl)

  return (
    <section className="portfolio-admin-technology-resources">
      <header>
        <div>
          <span>Resource diagnostics</span>
          <strong>
            {officialReady && documentationReady
              ? 'Official references complete'
              : 'Complete the technology references'}
          </strong>
        </div>

        <code>{[officialReady, documentationReady].filter(Boolean).length}/2</code>
      </header>

      <div className="portfolio-admin-technology-resources__grid">
        <article className={officialReady ? 'is-ready' : ''}>
          <span>Official website</span>
          <strong>{officialReady ? 'Valid external destination' : 'Missing or invalid URL'}</strong>
          <ResourceLink href={officialUrl} label="Open official site" />
        </article>

        <article className={documentationReady ? 'is-ready' : ''}>
          <span>Documentation</span>
          <strong>
            {documentationReady ? 'Valid documentation destination' : 'Missing or invalid URL'}
          </strong>
          <ResourceLink href={documentationUrl} label="Open documentation" />
        </article>
      </div>
    </section>
  )
}

function ReadinessCheck({
  complete,
  description,
  label,
}: {
  complete: boolean
  description: string
  label: string
}) {
  return (
    <li className={complete ? 'is-complete' : ''}>
      <span aria-hidden="true">{complete ? '✓' : '○'}</span>

      <div>
        <strong>{label}</strong>
        <small>{description}</small>
      </div>
    </li>
  )
}

export function TechnologyReadiness() {
  const { category, color, description, documentationUrl, name, officialUrl, slug } =
    useTechnologyValues()

  const checks = [
    {
      complete: Boolean(name && slug),
      description: 'Name and icon key',
      label: 'Identity',
    },
    {
      complete: Boolean(description),
      description: 'Tooltip-friendly explanation',
      label: 'Description',
    },
    {
      complete: /^#[0-9a-f]{3,8}$/iu.test(color),
      description: 'Valid hexadecimal brand color',
      label: 'Brand color',
    },
    {
      complete: Boolean(category),
      description: 'Technology or shared category',
      label: 'Classification',
    },
    {
      complete: isValidURL(officialUrl),
      description: 'Official website destination',
      label: 'Official URL',
    },
    {
      complete: isValidURL(documentationUrl),
      description: 'Documentation destination',
      label: 'Documentation',
    },
  ]

  const completed = checks.filter((check) => check.complete).length
  const percentage = Math.round((completed / checks.length) * 100)
  const remaining = checks.length - completed

  return (
    <aside
      className="portfolio-admin-technology-readiness"
      style={
        {
          '--technology-readiness': `${percentage}%`,
        } as CSSProperties
      }
    >
      <header>
        <div>
          <span>Stack readiness</span>
          <strong>{percentage}%</strong>
        </div>

        <i aria-hidden="true">
          <b />
        </i>
      </header>

      <p>
        {remaining === 0
          ? 'This technology profile is complete.'
          : `${remaining} ${remaining === 1 ? 'check needs' : 'checks need'} attention.`}
      </p>

      <ul>
        {checks.map((check) => (
          <ReadinessCheck key={check.label} {...check} />
        ))}
      </ul>
    </aside>
  )
}

====================================================================================================
FILE: src/components/admin/taxonomy/TechStackListHeader.tsx
====================================================================================================
// Portfolio Admin Experience - Tech Stack List Header v1
import Link from 'next/link'
import type { BeforeListServerProps, Where } from 'payload'

type Tone = 'amber' | 'cyan' | 'green' | 'purple'

async function countTechnologies(
  payload: BeforeListServerProps['payload'],
  user: BeforeListServerProps['user'],
  where?: Where,
): Promise<number> {
  try {
    const result = await payload.count({
      collection: 'tech-stack',
      overrideAccess: false,
      user,
      ...(where ? { where } : {}),
    })

    return result.totalDocs
  } catch {
    return 0
  }
}

function Metric({
  code,
  description,
  label,
  tone,
  value,
}: {
  code: string
  description: string
  label: string
  tone: Tone
  value: number
}) {
  return (
    <article className={`portfolio-admin-taxonomy-metric is-${tone}`}>
      <header>
        <span>{label}</span>
        <code>{code}</code>
      </header>
      <strong>{value}</strong>
      <small>{description}</small>
      <i aria-hidden="true" />
    </article>
  )
}

export default async function TechStackListHeader({
  hasCreatePermission,
  newDocumentURL,
  payload,
  user,
}: BeforeListServerProps) {
  const [total, visible, featured, needsLinks] = await Promise.all([
    countTechnologies(payload, user),
    countTechnologies(payload, user, {
      visible: { equals: true },
    }),
    countTechnologies(payload, user, {
      featured: { equals: true },
    }),
    countTechnologies(payload, user, {
      or: [
        { officialUrl: { exists: false } },
        { officialUrl: { equals: '' } },
        { documentationUrl: { exists: false } },
        { documentationUrl: { equals: '' } },
      ],
    }),
  ])

  return (
    <section className="portfolio-admin-taxonomy-overview is-tech-stack">
      <header className="portfolio-admin-taxonomy-overview__header">
        <div>
          <span className="portfolio-admin-taxonomy-overview__eyebrow">
            Technology registry
          </span>
          <h1>Stack catalog</h1>
          <p>
            Maintain technology identity, classification, visibility,
            and official resources across {total}{' '}
            {total === 1 ? 'technology' : 'technologies'}.
          </p>
        </div>

        {hasCreatePermission && newDocumentURL ? (
          <Link
            className="portfolio-admin-button is-primary"
            href={newDocumentURL}
          >
            <span aria-hidden="true">+</span>
            Add technology
          </Link>
        ) : null}
      </header>

      <div className="portfolio-admin-taxonomy-overview__metrics">
        <Metric
          code="ALL"
          description="Registered technologies"
          label="Total stack"
          tone="cyan"
          value={total}
        />
        <Metric
          code="VIS"
          description="Available on public pages"
          label="Visible"
          tone="green"
          value={visible}
        />
        <Metric
          code="FTR"
          description="Highlighted technologies"
          label="Featured"
          tone="purple"
          value={featured}
        />
        <Metric
          code="URL"
          description="Missing website or documentation"
          label="Needs links"
          tone={needsLinks > 0 ? 'amber' : 'green'}
          value={needsLinks}
        />
      </div>
    </section>
  )
}

====================================================================================================
FILE: src/components/admin/testimonials/TestimonialCells.tsx
====================================================================================================
// Portfolio Admin Experience - Testimonial Cells v1
'use client'

import Link from 'next/link'

import type { DefaultCellComponentProps } from 'payload'

type UnknownRecord = Record<string, unknown>

function asRecord(value: unknown): UnknownRecord {
  return value && typeof value === 'object' ? (value as UnknownRecord) : {}
}

function asText(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

function avatarURL(value: unknown): string {
  const media = asRecord(value)
  return asText(media.thumbnailURL, asText(media.url))
}

function initials(name: string): string {
  return name
    .split(/\s+/u)
    .filter(Boolean)
    .slice(0, 2)
    .map((part) => part[0]?.toUpperCase())
    .join('')
}

export function TestimonialIdentityCell({ cellData, rowData }: DefaultCellComponentProps) {
  const row = asRecord(rowData)
  const name = asText(cellData, 'Anonymous client')
  const role = asText(row.role)
  const company = asText(row.company)
  const context = [role, company].filter(Boolean).join(' · ') || 'Client testimonial'
  const message = asText(row.message)
  const id = typeof row.id === 'string' || typeof row.id === 'number' ? String(row.id) : ''
  const image = avatarURL(row.avatar).replace(/"/g, '%22')
  const href = id
    ? `/admin/collections/testimonials/${encodeURIComponent(id)}`
    : '/admin/collections/testimonials'

  return (
    <Link
      aria-label={`Review testimonial from ${name}`}
      className="portfolio-admin-testimonial-cell"
      href={href}
    >
      <span
        aria-hidden="true"
        className={`portfolio-admin-testimonial-cell__avatar${image ? ' has-image' : ''}`}
        style={image ? { backgroundImage: `url("${image}")` } : undefined}
      >
        {image ? null : initials(name)}
      </span>

      <span className="portfolio-admin-testimonial-cell__copy">
        <strong>{name}</strong>
        <small>{context}</small>
        {message ? <q>{message}</q> : null}
      </span>

      <span aria-hidden="true" className="portfolio-admin-testimonial-cell__open">
        ↗
      </span>
    </Link>
  )
}

export function TestimonialStatusCell({ cellData }: DefaultCellComponentProps) {
  const status = asText(cellData, 'pending').toLowerCase()

  return (
    <span className={`portfolio-admin-moderation-status is-${status}`}>
      <i aria-hidden="true" />
      {status}
    </span>
  )
}

export function TestimonialRatingCell({ cellData }: DefaultCellComponentProps) {
  const raw = typeof cellData === 'number' ? cellData : Number(cellData)
  const rating = Number.isFinite(raw) ? Math.min(5, Math.max(0, Math.round(raw))) : 0

  if (rating === 0) {
    return <span className="portfolio-admin-testimonial-rating is-empty">Unrated</span>
  }

  return (
    <span aria-label={`${rating} of 5 stars`} className="portfolio-admin-testimonial-rating">
      <span aria-hidden="true">
        {Array.from({ length: 5 }, (_, index) => (
          <i className={index < rating ? 'is-active' : ''} key={index}>
            ★
          </i>
        ))}
      </span>
      <code>{rating}.0</code>
    </span>
  )
}

export function TestimonialSourceCell({ cellData }: DefaultCellComponentProps) {
  const source = asText(cellData, 'Direct').replace(/[-_]+/gu, ' ')

  return (
    <span className="portfolio-admin-source-badge" title={source}>
      {source}
    </span>
  )
}

export function TestimonialApprovedCell({ cellData, rowData }: DefaultCellComponentProps) {
  const row = asRecord(rowData)
  const status = asText(row.status, 'pending').toLowerCase()
  const raw = typeof cellData === 'string' ? cellData : ''
  const date = raw ? new Date(raw) : null
  const valid = date && !Number.isNaN(date.getTime()) ? date : null

  return (
    <span className={`portfolio-admin-moderation-date is-${status}`}>
      <i aria-hidden="true" />
      <span>
        <strong>
          {status === 'approved' ? 'Approved' : status === 'rejected' ? 'Rejected' : 'Pending'}
        </strong>
        <small>
          {valid
            ? new Intl.DateTimeFormat('en-GB', {
                day: '2-digit',
                month: 'short',
                year: 'numeric',
              }).format(valid)
            : status === 'approved'
              ? 'Date not set'
              : 'Awaiting review'}
        </small>
      </span>
    </span>
  )
}

====================================================================================================
FILE: src/components/admin/testimonials/TestimonialDocumentControls.tsx
====================================================================================================
// Portfolio Admin Experience - Testimonial Document Controls v1
'use client'

import { useFormFields } from '@payloadcms/ui'

type FieldState = {
  value?: unknown
}

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

export default function TestimonialDocumentControls() {
  const state = useFormFields(([fields]) => ({
    rating: (fields.rating as FieldState | undefined)?.value,
    source: (fields.source as FieldState | undefined)?.value,
    status: (fields.status as FieldState | undefined)?.value,
  }))

  const status = text(state.status, 'pending').toLowerCase()
  const source = text(state.source, 'Direct')
  const ratingValue = Number(state.rating)
  const rating = Number.isFinite(ratingValue) ? Math.min(5, Math.max(0, ratingValue)) : 0

  return (
    <div className="portfolio-admin-testimonial-command">
      <span className={`portfolio-admin-testimonial-command__status is-${status}`}>
        <i aria-hidden="true" />
        {status}
      </span>

      <span className="portfolio-admin-testimonial-command__rating">
        <strong>{rating > 0 ? rating : '—'}</strong>
        <small>/ 5</small>
      </span>

      <span className="portfolio-admin-testimonial-command__source" title={source}>
        {source}
      </span>
    </div>
  )
}

====================================================================================================
FILE: src/components/admin/testimonials/TestimonialModerationPanel.tsx
====================================================================================================
// Portfolio Admin Experience - Testimonial Moderation Panel v1
'use client'

import { useAllFormFields } from '@payloadcms/ui'

type FieldState = {
  value?: unknown
}

type ModerationStatus = 'approved' | 'pending' | 'rejected'

function text(value: unknown, fallback = ''): string {
  return typeof value === 'string' && value.trim() ? value.trim() : fallback
}

export default function TestimonialModerationPanel() {
  const [fields, dispatchFields] = useAllFormFields()
  const status = text(
    (fields.status as FieldState | undefined)?.value,
    'pending',
  ) as ModerationStatus
  const approvedAt = (fields.approvedAt as FieldState | undefined)?.value
  const source = text((fields.source as FieldState | undefined)?.value, 'Direct')
  const ratingValue = Number((fields.rating as FieldState | undefined)?.value)
  const rating = Number.isFinite(ratingValue) ? Math.min(5, Math.max(0, ratingValue)) : 0

  const setStatus = (nextStatus: ModerationStatus) => {
    dispatchFields({
      path: 'status',
      type: 'UPDATE',
      value: nextStatus,
    })

    dispatchFields({
      path: 'approvedAt',
      type: 'UPDATE',
      value:
        nextStatus === 'approved'
          ? typeof approvedAt === 'string' && approvedAt
            ? approvedAt
            : new Date().toISOString()
          : null,
    })
  }

  return (
    <aside className={`portfolio-admin-testimonial-moderation is-${status}`}>
      <header className="portfolio-admin-testimonial-moderation__header">
        <div>
          <span>Moderation state</span>
          <strong>{status}</strong>
        </div>
        <i aria-hidden="true" />
      </header>

      <div className="portfolio-admin-testimonial-moderation__meta">
        <span>
          <small>Rating</small>
          <strong>{rating > 0 ? `${rating}/5` : 'Unrated'}</strong>
        </span>
        <span>
          <small>Source</small>
          <strong title={source}>{source}</strong>
        </span>
      </div>

      <div className="portfolio-admin-testimonial-moderation__actions">
        <button
          className={status === 'pending' ? 'is-active' : ''}
          onClick={() => setStatus('pending')}
          type="button"
        >
          Pending
        </button>
        <button
          className={status === 'approved' ? 'is-active is-approve' : 'is-approve'}
          onClick={() => setStatus('approved')}
          type="button"
        >
          Approve
        </button>
        <button
          className={status === 'rejected' ? 'is-active is-reject' : 'is-reject'}
          onClick={() => setStatus('rejected')}
          type="button"
        >
          Reject
        </button>
      </div>

      <p>Quick actions update the form. Use Save to persist the moderation decision.</p>
    </aside>
  )
}

====================================================================================================
FILE: src/components/admin/testimonials/TestimonialStatusField.tsx
====================================================================================================
// Portfolio Admin Experience - Testimonial Status Field v2
'use client'

import { useField } from '@payloadcms/ui'

type Status = 'approved' | 'pending' | 'rejected'
type Props = { path: string }

const options: Array<{ label: string; value: Status }> = [
  { label: 'Pending', value: 'pending' },
  { label: 'Approved', value: 'approved' },
  { label: 'Rejected', value: 'rejected' },
]

function normalize(value: unknown): Status {
  return value === 'approved' || value === 'rejected' ? value : 'pending'
}

export default function TestimonialStatusField({ path }: Props) {
  const { readOnly, setValue, value } = useField<Status>({ path })
  const status = normalize(value)
  const id = `${path}-native-status`

  return (
    <div className={`portfolio-admin-custom-status-field is-testimonial is-${status}`}>
      <label htmlFor={id}>
        Status <span aria-hidden="true">*</span>
      </label>

      <div className="portfolio-admin-custom-status-field__control">
        <i aria-hidden="true" />

        <select
          disabled={readOnly}
          id={id}
          onChange={(event) => setValue(normalize(event.currentTarget.value))}
          value={status}
        >
          {options.map((option) => (
            <option key={option.value} value={option.value}>
              {option.label}
            </option>
          ))}
        </select>

        <svg aria-hidden="true" fill="none" viewBox="0 0 20 20">
          <path
            d="m6 8 4 4 4-4"
            stroke="currentColor"
            strokeLinecap="round"
            strokeLinejoin="round"
            strokeWidth="1.5"
          />
        </svg>
      </div>
    </div>
  )
}

====================================================================================================
FILE: src/components/admin/testimonials/TestimonialsListHeader.tsx
====================================================================================================
// Portfolio Admin Experience - Testimonials Moderation Header v1

import Link from 'next/link'

import type { BeforeListServerProps, Where } from 'payload'

type Tone = 'cyan' | 'green' | 'red' | 'purple'

type MetricProps = {
  code: string
  description: string
  label: string
  tone: Tone
  value: number
}

async function countTestimonials(
  payload: BeforeListServerProps['payload'],
  user: BeforeListServerProps['user'],
  where?: Where,
): Promise<number> {
  try {
    const result = await payload.count({
      collection: 'testimonials',
      overrideAccess: false,
      user,
      ...(where ? { where } : {}),
    })

    return result.totalDocs
  } catch {
    return 0
  }
}

function Metric({ code, description, label, tone, value }: MetricProps) {
  return (
    <article className={`portfolio-admin-moderation-stat is-${tone}`}>
      <div>
        <span>{label}</span>
        <code>{code}</code>
      </div>
      <strong>{value}</strong>
      <p>{description}</p>
      <i aria-hidden="true" />
    </article>
  )
}

export default async function TestimonialsListHeader({
  hasCreatePermission,
  newDocumentURL,
  payload,
  user,
}: BeforeListServerProps) {
  const [total, pending, approved, rejected, unrated] = await Promise.all([
    countTestimonials(payload, user),
    countTestimonials(payload, user, {
      status: {
        equals: 'pending',
      },
    }),
    countTestimonials(payload, user, {
      status: {
        equals: 'approved',
      },
    }),
    countTestimonials(payload, user, {
      status: {
        equals: 'rejected',
      },
    }),
    countTestimonials(payload, user, {
      rating: {
        exists: false,
      },
    }),
  ])

  return (
    <section className="portfolio-admin-moderation-overview">
      <header className="portfolio-admin-moderation-overview__header">
        <div>
          <span className="portfolio-admin-moderation-overview__eyebrow">Trust moderation</span>
          <h2>Testimonial queue</h2>
          <p>
            Review client feedback, source context, ratings, and approval state across {total}{' '}
            {total === 1 ? 'testimonial' : 'testimonials'}.
          </p>
        </div>

        {hasCreatePermission && newDocumentURL ? (
          <Link className="portfolio-admin-button is-primary" href={newDocumentURL}>
            <span aria-hidden="true">+</span>
            Add testimonial
          </Link>
        ) : null}
      </header>

      <div className="portfolio-admin-moderation-overview__metrics">
        <Metric
          code="PND"
          description="Waiting for moderation"
          label="Pending"
          tone="purple"
          value={pending}
        />
        <Metric
          code="APR"
          description="Visible in trust sections"
          label="Approved"
          tone="green"
          value={approved}
        />
        <Metric
          code="REJ"
          description="Excluded from publication"
          label="Rejected"
          tone="red"
          value={rejected}
        />
        <Metric
          code="NRT"
          description="Feedback without a rating"
          label="Unrated"
          tone={unrated > 0 ? 'cyan' : 'green'}
          value={unrated}
        />
      </div>
    </section>
  )
}

